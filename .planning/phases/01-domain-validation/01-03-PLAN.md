---
phase: 01-domain-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - backend/internal/service/transaction_create_test.go
  - backend/internal/service/transaction_update_test.go
  - backend/internal/service/transaction_delete_test.go
  - backend/cmd/scripts/seed/main.go
autonomous: true
requirements:
  - DOM-01
  - DOM-02
  - DOM-03
  - VAL-01
  - VAL-02
  - VAL-03
  - VAL-04
  - VAL-05
  - VAL-06

must_haves:
  truths:
    - "The entire backend compiles without errors: go build ./... exits 0"
    - "All test files that construct RecurrenceSettings use CurrentInstallment and TotalInstallments (no Repetitions or EndDate field references)"
    - "The TestCreateRecurringExpenseWithEndDate test is removed (EndDate-based recurrence no longer exists as a concept)"
    - "The TestCreateRecurringExpenseWithRepetitions test is updated to use TotalInstallments and CurrentInstallment=1"
    - "The seed script compiles: it constructs RecurrenceSettings with CurrentInstallment and TotalInstallments"
    - "RecurrenceFromSettings call sites pass only 2 arguments (recurrenceSettings, userID) — startDate argument removed"
  artifacts:
    - path: "backend/internal/service/transaction_create_test.go"
      provides: "Compiling test file using new RecurrenceSettings fields"
      contains: "TotalInstallments"
    - path: "backend/internal/service/transaction_update_test.go"
      provides: "Compiling test file using new RecurrenceSettings fields"
      contains: "TotalInstallments"
    - path: "backend/internal/service/transaction_delete_test.go"
      provides: "Compiling test file using new RecurrenceSettings fields"
      contains: "TotalInstallments"
    - path: "backend/cmd/scripts/seed/main.go"
      provides: "Compiling seed script using new RecurrenceSettings fields"
      contains: "TotalInstallments"
  key_links:
    - from: "backend/internal/service/transaction_create.go createRecurrence"
      to: "domain.RecurrenceFromSettings"
      via: "2-argument call"
      pattern: "RecurrenceFromSettings.*userID"
    - from: "backend/internal/service/transaction_update.go"
      to: "domain.RecurrenceFromSettings"
      via: "2-argument call"
      pattern: "RecurrenceFromSettings.*userID"
---

<objective>
Fix all compile-breaking callers of `RecurrenceSettings` across test files, the seed script, and any production call sites that pass `startDate` to `RecurrenceFromSettings`. After this plan, `go build ./...` exits 0 and the codebase is in a fully compiling state.

Purpose: Phase 1 ends at a compiler-verified boundary. Tests will pass or fail based on existing test logic (Phase 4 adds new tests for the new behavior). For now, existing tests that used EndDate-based recurrence must be removed or replaced with minimal stubs that compile; existing tests that used `Repetitions` are updated to use `TotalInstallments: N, CurrentInstallment: 1`.

Output: All Go files in the backend compile. No `Repetitions` or `EndDate` references remain in `RecurrenceSettings` struct literals anywhere in the codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-domain-validation/01-01-SUMMARY.md
@.planning/phases/01-domain-validation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RecurrenceFromSettings call sites in production service files</name>
  <files>backend/internal/service/transaction_create.go, backend/internal/service/transaction_update.go</files>
  <read_first>
    - backend/internal/service/transaction_create.go (lines 532-540: createRecurrence function that calls domain.RecurrenceFromSettings)
    - backend/internal/service/transaction_update.go (lines 810-816: call to domain.RecurrenceFromSettings)
    - backend/internal/domain/transaction.go (the new RecurrenceFromSettings signature from plan 01: 2 parameters)
  </read_first>
  <action>
After plan 01, `RecurrenceFromSettings` takes 2 parameters: `(recurrenceSettings RecurrenceSettings, userID int)`. The old signature had a third `startDate time.Time` parameter. Find and fix every call site.

**In backend/internal/service/transaction_create.go**, locate the `createRecurrence` function (approximately lines 532-540):

CURRENT:
```go
func (s *transactionService) createRecurrence(ctx context.Context, userID int, recurrenceSettings domain.RecurrenceSettings, startDate time.Time) (*domain.TransactionRecurrence, error) {
	tr := domain.RecurrenceFromSettings(recurrenceSettings, userID, startDate)
	...
}
```

Update the wrapper function signature and call:
```go
func (s *transactionService) createRecurrence(ctx context.Context, userID int, recurrenceSettings domain.RecurrenceSettings) (*domain.TransactionRecurrence, error) {
	tr := domain.RecurrenceFromSettings(recurrenceSettings, userID)
	...
}
```

Then find every call to `s.createRecurrence(ctx, ...)` in transaction_create.go and remove the `startDate` argument. There are 3 call sites:

1. Around line 196 in `createTransactions`:
   CURRENT: `recurrence, err := s.createRecurrence(ctx, userID, *req.RecurrenceSettings, req.Date)`
   NEW: `recurrence, err := s.createRecurrence(ctx, userID, *req.RecurrenceSettings)`

2. Around line 377 in `injectLinkedTransactions` (same-user transfer branch):
   CURRENT: `r, err := s.createRecurrence(ctx, userID, *recurrenceSettings, transaction.Date)`
   NEW: `r, err := s.createRecurrence(ctx, userID, *recurrenceSettings)`

3. Around line 448 in `injectLinkedTransactions` (split branch):
   CURRENT: `r, err := s.createRecurrence(ctx, connection.ToUserID, *recurrenceSettings, transaction.Date)`
   NEW: `r, err := s.createRecurrence(ctx, connection.ToUserID, *recurrenceSettings)`

**In backend/internal/service/transaction_update.go**, locate the call to `domain.RecurrenceFromSettings` (approximately line 813):

CURRENT: `recurrence := domain.RecurrenceFromSettings(*data.req.RecurrenceSettings, userID, *date)`
NEW: `recurrence := domain.RecurrenceFromSettings(*data.req.RecurrenceSettings, userID)`

NOTE: The `date` variable declared above the call (around line 810: `date := lo.CoalesceOrEmpty(req.Date, &transaction.Date)`) may now be unused if it was only used for this call. Check the surrounding code — `date` may be used for other things in the same function block. If `date` becomes unused after removing the argument, the Go compiler will error. Search the surrounding lines to determine if `date` is used elsewhere in that scope. If it is not, remove the `date` declaration too. Read the function carefully before modifying.
  </action>
  <verify>
    <automated>cd /workspace/backend && grep -rn "RecurrenceFromSettings\|createRecurrence" internal/service/ | grep -v "_test.go"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "RecurrenceFromSettings" backend/internal/service/transaction_create.go` shows 1 match with 2 arguments: `domain.RecurrenceFromSettings(recurrenceSettings, userID)`
    - `grep "RecurrenceFromSettings" backend/internal/service/transaction_update.go` shows 1 match with 2 arguments: `domain.RecurrenceFromSettings(*data.req.RecurrenceSettings, userID)`
    - `grep "createRecurrence(ctx," backend/internal/service/transaction_create.go` shows 3 matches, each with 3 arguments (ctx, userID, recurrenceSettings) — NOT 4 arguments
    - `grep "func.*createRecurrence" backend/internal/service/transaction_create.go` shows signature with 3 parameters (ctx, userID, recurrenceSettings) — no startDate
    - `cd /workspace/backend && go vet ./internal/service/transaction_create.go ./internal/service/transaction_update.go 2>&1 | grep -v "_test.go"` — no errors from the non-test files (test files may still error at this point)
  </acceptance_criteria>
  <done>All production RecurrenceFromSettings and createRecurrence call sites use the new 2/3 parameter signatures; no startDate argument passed</done>
</task>

<task type="auto">
  <name>Task 2: Update test files and seed script to use new RecurrenceSettings fields</name>
  <files>backend/internal/service/transaction_create_test.go, backend/internal/service/transaction_update_test.go, backend/internal/service/transaction_delete_test.go, backend/cmd/scripts/seed/main.go</files>
  <read_first>
    - backend/internal/service/transaction_create_test.go (full file — grep for all RecurrenceSettings struct literals to understand scope of changes)
    - backend/internal/service/transaction_update_test.go (full file — grep for all RecurrenceSettings struct literals)
    - backend/internal/service/transaction_delete_test.go (lines 170-250: the RecurrenceSettings usages found earlier)
    - backend/cmd/scripts/seed/main.go (lines 455-467: the RecurrenceSettings usage in seed)
  </read_first>
  <action>
The goal is to make the entire backend compile. Every `domain.RecurrenceSettings{ ... }` struct literal that references `Repetitions` or `EndDate` must be updated.

**Rule for all Repetitions replacements:**
`Repetitions: lo.ToPtr(N)` → `CurrentInstallment: 1, TotalInstallments: N`
`Repetitions: &installments` → `CurrentInstallment: 1, TotalInstallments: installments`

**Rule for EndDate replacements:**
`EndDate: lo.ToPtr(date)` → `CurrentInstallment: 1, TotalInstallments: N` where N is a reasonable value. Since EndDate-based recurrence is gone, replace with a fixed TotalInstallments value that is semantically reasonable:
- Daily + EndDate(d + 30 days) → `TotalInstallments: 30`
- Weekly + EndDate(d + 28 days) → `TotalInstallments: 4`
- Monthly + EndDate(d + 3 months) → `TotalInstallments: 3`
- Yearly + EndDate(d + 3 years) → `TotalInstallments: 3`

**transaction_create_test.go — changes:**

1. All `Repetitions: lo.ToPtr(N)` literals: replace with `CurrentInstallment: 1, TotalInstallments: N` (covers usages at approximately lines 281, 522, 524, 541, 543, 661, 673, 686, 700, etc.).

2. `TestCreateRecurringExpenseWithRepetitions` function: update the loop and assertion. The current assertion at approximately line 724:
   ```go
   suite.Assert().Len(transactionsDB, lo.SumBy(transactions, func(transaction domain.TransactionCreateRequest) int {
       return lo.FromPtr(transaction.RecurrenceSettings.Repetitions)
   }))
   ```
   Replace with:
   ```go
   suite.Assert().Len(transactionsDB, lo.SumBy(transactions, func(transaction domain.TransactionCreateRequest) int {
       return transaction.RecurrenceSettings.TotalInstallments
   }))
   ```

3. `TestCreateRecurringExpenseWithEndDate` function (approximately lines 791-900+): This entire test function must be REMOVED. EndDate-based recurrence is gone. Do not replace it with a stub — just delete the entire test function. Phase 4 will add new tests for the new behavior.

   IMPORTANT: Also remove the `lo.ToPtr` import usage for EndDate if `lo` is otherwise still used in the file. Keep `lo` import — it is used throughout the file.

4. The function name `TestCreateRecurringExpenseWithRepetitions` can be renamed to `TestCreateRecurringExpenseWithTotalInstallments` to reflect the new concept, but this is OPTIONAL — focus on compile correctness first.

**transaction_update_test.go — changes:**

All `Repetitions: lo.ToPtr(N)` in RecurrenceSettings struct literals: replace each with `CurrentInstallment: 1, TotalInstallments: N`.

There are approximately 18 such occurrences based on the grep output. The value `N` in each: replace `lo.ToPtr(3)` → `TotalInstallments: 3`, `lo.ToPtr(5)` → `TotalInstallments: 5`, `lo.ToPtr(2)` → `TotalInstallments: 2`. Keep `CurrentInstallment: 1` in all cases.

The `lo` import is still needed throughout. Keep it.

**transaction_delete_test.go — changes:**

All `Repetitions: lo.ToPtr(installments)` in RecurrenceSettings struct literals: replace with `CurrentInstallment: 1, TotalInstallments: installments`.

There are approximately 5 such occurrences.

**cmd/scripts/seed/main.go — changes:**

Locate:
```go
RecurrenceSettings: &domain.RecurrenceSettings{
    Type:        domain.RecurrenceTypeMonthly,
    Repetitions: &installments,
},
```
Replace with:
```go
RecurrenceSettings: &domain.RecurrenceSettings{
    Type:              domain.RecurrenceTypeMonthly,
    CurrentInstallment: 1,
    TotalInstallments: installments,
},
```

**Final verification step:** After all changes, run:
```bash
cd /workspace/backend && go build ./...
```
If there are remaining compile errors referencing `Repetitions`, `EndDate` on `RecurrenceSettings`, or wrong number of arguments to `RecurrenceFromSettings` or `createRecurrence`, fix them before completing this task. Use the error output to identify any missed locations.
  </action>
  <verify>
    <automated>cd /workspace/backend && go build ./... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspace/backend && go build ./...` exits with code 0 (zero compile errors)
    - `grep -rn "\.Repetitions\b" backend/internal/ backend/cmd/` returns 0 matches (no remaining Repetitions field access on RecurrenceSettings)
    - `grep -rn "RecurrenceSettings{" backend/ | grep -v "_test.go" | grep "EndDate\|Repetitions"` returns 0 matches
    - `grep -rn "RecurrenceSettings{" backend/ | grep "EndDate\|Repetitions"` returns 0 matches (test files also clean)
    - `grep "TestCreateRecurringExpenseWithEndDate" backend/internal/service/transaction_create_test.go` returns 0 matches (function removed)
    - `grep "TotalInstallments" backend/internal/service/transaction_create_test.go` returns at least 5 matches
    - `grep "TotalInstallments" backend/internal/service/transaction_update_test.go` returns at least 10 matches
    - `grep "TotalInstallments" backend/cmd/scripts/seed/main.go` returns at least 1 match
  </acceptance_criteria>
  <done>go build ./... exits 0; all RecurrenceSettings struct literals in the entire backend use CurrentInstallment and TotalInstallments; no Repetitions or EndDate references remain; TestCreateRecurringExpenseWithEndDate is removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test→Service | Test code constructs RecurrenceSettings; same validation rules apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Test data with CurrentInstallment=1 hardcoded | accept | Tests use minimal valid values; no PII or sensitive data in test fixtures |
| T-03-02 | Tampering | Seed script Repetitions→TotalInstallments migration | accept | Seed is dev/test tooling only, not production path |
</threat_model>

<verification>
After both tasks complete:
1. `cd /workspace/backend && go build ./...` — exits code 0
2. `grep -rn "\.Repetitions\b\|RecurrenceSettings.*EndDate" backend/` — zero matches
3. `grep -rn "RecurrenceFromSettings" backend/internal/service/` — all calls use 2 arguments
4. `cd /workspace/backend && go vet ./...` — no vet errors
</verification>

<success_criteria>
- `go build ./...` exits 0 — the entire backend compiles
- No `Repetitions` or `EndDate` field references remain on `RecurrenceSettings` struct literals in any file
- `TestCreateRecurringExpenseWithEndDate` function is removed from transaction_create_test.go
- All remaining test cases use `TotalInstallments: N, CurrentInstallment: 1` for their recurrence settings
- Seed script uses new field names
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-validation/01-03-SUMMARY.md`
</output>
