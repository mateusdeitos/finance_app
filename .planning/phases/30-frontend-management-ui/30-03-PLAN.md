---
phase: 30-frontend-management-ui
plan: 03
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - frontend/src/testIds/transactions.ts
  - frontend/src/components/transactions/templates/SaveAsTemplateDrawer.tsx
  - frontend/src/components/transactions/form/TransactionFormFooter.tsx
  - frontend/src/components/transactions/form/TransactionForm.tsx
autonomous: true
requirements: [MNG-02]

must_haves:
  truths:
    - "The create transaction form shows a 'Salvar como modelo' action; it is disabled when the user already has 3 templates"
    - "Clicking it snapshots the current form values, builds a TemplatePayload, and opens a mini drawer with the name pre-filled from the description (editable) and a confirm button"
    - "Confirming creates the template via the Plan-01 create mutation and refreshes the chip row via useTransactionTemplates().invalidate"
    - "The action only appears in create mode (not the edit/update form)"
  artifacts:
    - path: "frontend/src/components/transactions/templates/SaveAsTemplateDrawer.tsx"
      provides: "confirm-name mini drawer that creates a template from a prepared payload"
      contains: "useCreateTransactionTemplate"
  key_links:
    - from: "Salvar como modelo button"
      to: "SaveAsTemplateDrawer"
      via: "renderDrawer with getValues()-derived payload + suggested name"
      pattern: "SaveAsTemplateDrawer"
---

<objective>
Add "Save as template" to the create transaction form: a button (disabled at the 3-template cap) that snapshots the current form values, builds a `TemplatePayload`, and opens a mini drawer to confirm/edit the name (pre-filled from the description) before creating the template. On success the chip row refreshes.

Purpose: MNG-02 — save the current form state as a new template.
Output: mini drawer + footer button + create-form wiring + testids.
</objective>

<execution_context>
@/home/user/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-frontend-management-ui/30-CONTEXT.md
@.planning/phases/30-frontend-management-ui/30-01-SUMMARY.md
@.planning/phases/30-frontend-management-ui/30-02-SUMMARY.md
@frontend/CLAUDE.md

<interfaces>
From Plan 01: `buildTemplatePayloadFromForm(values, tags)`, `useCreateTransactionTemplate({ onSuccess })`. From Phase 29: `useTransactionTemplates()` → `{ query, invalidate }`; `TransactionForm` already consumes `useTransactionTemplates()` + `useTags()` and has `showTemplateChips` (create-mode gate) + `getValues` via `useFormContext`.

`TransactionForm.tsx` — `useFormContext<TransactionFormValues>()` exposes `getValues`. `showTemplateChips` (Phase 29) marks create mode. Field name for the name suggestion: `description`.

`TransactionFormFooter.tsx` — desktop `Group` with "Salvar e criar outra" + "Salvar" (`BtnSave`). It is a presentational footer; add optional props rather than calling hooks inside it.

Drawer mirror: `src/components/accounts/AccountDrawer.tsx` (useDrawerContext + ResponsiveDrawer + create mutation + close). `renderDrawer<T>` from `src/utils/renderDrawer.tsx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SaveAsTemplateDrawer (confirm name -> create)</name>
  <read_first>
    - frontend/src/components/accounts/AccountDrawer.tsx (drawer + create mutation shape)
    - frontend/src/utils/renderDrawer.tsx
    - frontend/src/hooks/useTransactionTemplates.ts (create hook + invalidate)
    - frontend/src/types/transactions.ts (TemplatePayload)
  </read_first>
  <action>
    Create `src/components/transactions/templates/SaveAsTemplateDrawer.tsx`:
    - Props: `{ payload: Transactions.TemplatePayload; suggestedName: string }`.
    - `const { opened, close, reject } = useDrawerContext<Transactions.Template | void>()`.
    - Local RHF (or a simple controlled `TextInput`) for the name, default `suggestedName.slice(0, 100)`; require non-empty.
    - `const { invalidate } = useTransactionTemplates(); const { mutation } = useCreateTransactionTemplate({ onSuccess: async (t) => { await invalidate(); close(t) } });`
    - Confirm button (`TemplateBtnConfirmSaveAsTemplate`) → `mutation.mutate({ name, payload })`. Show a general error `Alert` (`SaveAsTemplateError`) on failure (e.g. 409 duplicate name / cap).
    - `ResponsiveDrawer opened onClose={reject} title="Salvar como modelo" data-testid={SaveAsTemplateDrawer}` with a name `TextInput` (`SaveAsTemplateInputName`).
    No `any`, no new `useEffect`.
  </action>
  <verify><automated>cd frontend && npx tsc --noEmit 2>&1 | tail -6 && npm run lint 2>&1 | tail -5</automated></verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/transactions/templates/SaveAsTemplateDrawer.tsx`
    - `grep -q 'useCreateTransactionTemplate' frontend/src/components/transactions/templates/SaveAsTemplateDrawer.tsx`
    - `grep -q 'invalidate' frontend/src/components/transactions/templates/SaveAsTemplateDrawer.tsx`
    - new testids added (SaveAsTemplateDrawer, SaveAsTemplateInputName, TemplateBtnConfirmSaveAsTemplate, SaveAsTemplateError)
    - `cd frontend && npx tsc --noEmit` exits 0; `npm run lint` no new errors
  </acceptance_criteria>
  <done>Mini drawer confirms the name and creates the template; success invalidates the templates query.</done>
</task>

<task type="auto">
  <name>Task 2: Footer button + create-form wiring (create mode, cap-disabled)</name>
  <read_first>
    - frontend/src/components/transactions/form/TransactionFormFooter.tsx (props + button group)
    - frontend/src/components/transactions/form/TransactionForm.tsx (useFormContext getValues, useTags, useTransactionTemplates, showTemplateChips, where the footer is rendered)
  </read_first>
  <action>
    In `TransactionFormFooter.tsx`, add optional props `onSaveAsTemplate?: () => void` and `saveAsTemplateDisabled?: boolean`. When `onSaveAsTemplate` is provided, render a secondary "Salvar como modelo" button (`TransactionsTestIds.BtnSaveAsTemplate`) in the footer group, `disabled={saveAsTemplateDisabled}` with a tooltip "Você já tem 3 modelos" when disabled. Keep it additive — when the prop is absent (edit form), nothing changes.

    In `TransactionForm.tsx`, when `showTemplateChips` (create mode):
    - Compute `const atCap = templates.length >= 3;` (templates already available from Phase 29).
    - Build a handler:
      ```tsx
      const handleSaveAsTemplate = () => {
        const values = getValues();
        const payload = buildTemplatePayloadFromForm(values, tags);
        void renderDrawer(() => (
          <SaveAsTemplateDrawer payload={payload} suggestedName={values.description ?? ""} />
        ));
      };
      ```
      (Add `getValues` to the `useFormContext` destructure; import `buildTemplatePayloadFromForm`, `renderDrawer`, `SaveAsTemplateDrawer`.)
    - Pass `onSaveAsTemplate={handleSaveAsTemplate}` and `saveAsTemplateDisabled={atCap}` to `<TransactionFormFooter .../>`. Only in create mode (guard with `showTemplateChips` so edit passes nothing).
    - No new `useEffect`; no `any`; do not refactor the file's pre-existing divergences.
  </action>
  <verify><automated>cd frontend && npx tsc --noEmit 2>&1 | tail -8 && npm run lint 2>&1 | tail -5 && npx vitest run src/components/transactions/form/ 2>&1 | tail -8</automated></verify>
  <acceptance_criteria>
    - `grep -q 'onSaveAsTemplate' frontend/src/components/transactions/form/TransactionFormFooter.tsx`
    - `grep -q 'BtnSaveAsTemplate' frontend/src/testIds/transactions.ts`
    - `grep -q 'buildTemplatePayloadFromForm' frontend/src/components/transactions/form/TransactionForm.tsx`
    - `grep -q 'SaveAsTemplateDrawer' frontend/src/components/transactions/form/TransactionForm.tsx`
    - `grep -q 'getValues' frontend/src/components/transactions/form/TransactionForm.tsx`
    - `grep -c 'useEffect' frontend/src/components/transactions/form/TransactionForm.tsx` did NOT increase
    - `cd frontend && npx tsc --noEmit` exits 0; `npm run lint` no new errors; `npx vitest run src/components/transactions/form/` green
  </acceptance_criteria>
  <done>Create form shows a cap-aware "Salvar como modelo" button that opens the confirm-name drawer; edit form is unaffected; typecheck/lint/tests pass with no new useEffect/any.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0; `npm run lint` no new errors; `npx vitest run src/components/transactions/form/` green.
- MNG-02 mapping: create-form action (Task 2) → confirm-name mini drawer (Task 1) → create mutation + invalidate; name suggested from description; disabled at cap; create-mode only.
</verification>

<success_criteria>
- "Save as template" in the create form (create mode only), disabled at 3 templates, opens a confirm-name drawer (name from description) and creates the template, refreshing the chip row.
</success_criteria>

<output>After completion, create `.planning/phases/30-frontend-management-ui/30-03-SUMMARY.md`.</output>
