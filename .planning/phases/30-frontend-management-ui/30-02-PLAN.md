---
phase: 30-frontend-management-ui
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - frontend/src/testIds/transactions.ts
  - frontend/src/components/transactions/templates/templateFormSchema.ts
  - frontend/src/components/transactions/templates/TemplateFormDrawer.tsx
  - frontend/src/components/transactions/templates/TemplatesManagementDrawer.tsx
  - frontend/src/pages/TransactionsPage.tsx
autonomous: true
requirements: [MNG-01]

must_haves:
  truths:
    - "A 'Gerenciar modelos' item in the transactions 'more options' menu opens TemplatesManagementDrawer via renderDrawer"
    - "The drawer lists the user's templates showing name + type + account; '+ Novo' opens TemplateFormDrawer to create; each row can edit and delete"
    - "Create/edit go through the Plan-01 mutations and buildTemplatePayloadFromForm; on success the chip row + list refresh via useTransactionTemplates().invalidate"
    - "Delete asks for confirmation before calling the delete mutation"
    - "Split is edited via SplitSettingsFields in templateMode (no R$0,00); no amount/date fields in the template form"
  artifacts:
    - path: "frontend/src/components/transactions/templates/TemplateFormDrawer.tsx"
      provides: "create/edit template form inside a renderDrawer shell"
      contains: "buildTemplatePayloadFromForm"
    - path: "frontend/src/components/transactions/templates/TemplatesManagementDrawer.tsx"
      provides: "list + create/edit/delete management surface"
      contains: "TemplatesManagementDrawer"
  key_links:
    - from: "TransactionsPage more-options menu"
      to: "TemplatesManagementDrawer"
      via: "renderDrawer on the new Menu.Item"
      pattern: "TemplatesManagementDrawer"
    - from: "create/edit success"
      to: "chip + list refresh"
      via: "useTransactionTemplates().invalidate in onSuccess"
      pattern: "invalidate"
---

<objective>
Build the template management surface: a `TemplatesManagementDrawer` (list of templates with name/type/account, opened from the transactions "more options" menu) that can create, edit, and delete templates, and a `TemplateFormDrawer` (create/edit form reusing the transaction field building blocks + `SplitSettingsFields` in `templateMode`). Wire mutations so the chip row and list refresh immediately.

Purpose: MNG-01 — the dedicated management surface.
Output: schema + 2 drawer components + menu entry point + testids.
</objective>

<execution_context>
@/home/user/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-frontend-management-ui/30-CONTEXT.md
@.planning/phases/30-frontend-management-ui/30-01-SUMMARY.md
@frontend/CLAUDE.md

<interfaces>
From Plan 01: `useCreate/Update/DeleteTransactionTemplate` (`{ mutation }` + caller `onSuccess`), `buildTemplatePayloadFromForm(values, tags)`. From Phase 29: `useTransactionTemplates()` → `{ query, invalidate }`, `Transactions.Template`/`TemplatePayload`, `buildTemplateFormPatch` (payload→form, for edit prefill).

Mirror the ACCOUNTS flow:
- `src/pages/AccountsPage.tsx` — list dispatch (`handleAdd`/`handleEdit` via `renderDrawer`), `const { invalidate } = useAccounts()`.
- `src/components/accounts/AccountDrawer.tsx` — `useDrawerContext<Transactions.Template | void>()`, `ResponsiveDrawer opened onClose={reject}`, create: `mutate(body, { onSuccess: async (t) => { await invalidate(); close(t) } })`, edit: `mutate({id, body}, { onSuccess: async () => { await invalidate(); close() } })`.
- `src/components/categories/DeleteCategoryModal.tsx` — confirm-before-delete pattern.

Field JSX to COPY (inline in the transaction form — see the map in 30-CONTEXT):
- Type SegmentedControl (`TransactionForm.tsx`), account Select (`useGroupedAccountOptions`), category Select (`useFlattenCategories`), TagsInput (`useTags`), `DescriptionAutocomplete` (standalone), `SplitSettingsFields` with `templateMode` (standalone).

Entry point: `src/pages/TransactionsPage.tsx` — the `IconDots` `Menu.Dropdown` (has "Importar transações", `TransactionsTestIds.MenuItemImportTransactions`). Add a sibling `Menu.Item`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: templateFormSchema + TemplateFormDrawer (create/edit)</name>
  <read_first>
    - frontend/src/components/transactions/form/transactionFormSchema.ts (baseTransactionFields + applySharedRefinements; reuse the split/type/account pieces, DROP amount/date/recurrence, ADD name)
    - frontend/src/components/accounts/AccountDrawer.tsx (drawer shell + create/edit mutation wiring)
    - frontend/src/components/transactions/form/TransactionForm.tsx (the field Controller JSX to copy)
    - frontend/src/components/transactions/form/SplitSettingsFields.tsx (pass templateMode)
    - frontend/src/components/transactions/form/DescriptionAutocomplete.tsx
    - frontend/src/hooks/useTags.ts, useAccounts.ts, useFlattenCategories (or useCategories)
  </read_first>
  <action>
    Create `src/components/transactions/templates/templateFormSchema.ts`: a zod schema `templateFormSchema` for `{ name: string(min1), transaction_type, description(min1), account_id, category_id, destination_account_id, tags: string[], split_settings }` — reuse the split/type refinements from `transactionFormSchema` where practical, but OMIT `amount`, `date`, and recurrence. Export `TemplateFormValues = z.infer<...>`. Since the split-row inputs and `SplitSettingsFields` field names are `split_settings`/`tags`/`account_id`/etc., keep those names identical so the copied Controllers and `SplitSettingsFields` bind correctly.

    Create `src/components/transactions/templates/TemplateFormDrawer.tsx` (mirror `AccountDrawer`):
    - Props: `{ template?: Transactions.Template }` (present = edit).
    - `const { opened, close, reject } = useDrawerContext<Transactions.Template | void>()`.
    - `useForm<TemplateFormValues>({ resolver: zodResolver(templateFormSchema), defaultValues })`. For edit, build defaults from `template.payload` (reuse `buildTemplateFormPatch(template.payload, {accounts, categories, tags})` for the field values, plus `name: template.name`). For create, sensible blanks (`transaction_type:"expense"`, empty tags/split, etc.).
    - Wrap fields in `FormProvider`; render the copied Controllers (type/account/category/tags/description) + `<SplitSettingsFields templateMode />`. Add a name `TextInput` (testid `TemplateInputName`).
    - `const { invalidate } = useTransactionTemplates()`. On submit: `const payload = buildTemplatePayloadFromForm(values, tags); const body = { name: values.name, payload };` then create or update mutation with `onSuccess: async (...) => { await invalidate(); close(...) }`.
    - Surface a general error `Alert` with a stable testid (`TemplateFormError`) on mutation failure (mirror the transaction form's error alert; the backend returns 409 tags for cap/duplicate — show a message).
    - `ResponsiveDrawer opened onClose={reject} title={template ? "Editar modelo" : "Novo modelo"} data-testid={TemplateFormDrawer}`.
    Keep it under ~200 lines; if fields make it large, extract a `TemplateFormFields` subcomponent. No new `useEffect`, no `any`.
  </action>
  <verify><automated>cd frontend && npx tsc --noEmit 2>&1 | tail -8 && npm run lint 2>&1 | tail -5</automated></verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/transactions/templates/TemplateFormDrawer.tsx`
    - `grep -q 'buildTemplatePayloadFromForm' frontend/src/components/transactions/templates/TemplateFormDrawer.tsx`
    - `grep -q 'templateMode' frontend/src/components/transactions/templates/TemplateFormDrawer.tsx`
    - `grep -q 'invalidate' frontend/src/components/transactions/templates/TemplateFormDrawer.tsx`
    - new testids added to `src/testIds/transactions.ts` (TemplateFormDrawer, TemplateInputName, TemplateBtnSave, TemplateFormError)
    - `cd frontend && npx tsc --noEmit` exits 0; `npm run lint` no new errors
  </acceptance_criteria>
  <done>Create/edit template form drawer works via the Plan-01 mutations; split uses templateMode; success invalidates; typecheck/lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: TemplatesManagementDrawer (list + delete)</name>
  <read_first>
    - frontend/src/pages/AccountsPage.tsx (list + renderDrawer dispatch)
    - frontend/src/components/categories/DeleteCategoryModal.tsx (confirm-delete)
    - frontend/src/utils/renderDrawer.tsx
    - frontend/src/hooks/useTransactionTemplates.ts (query + invalidate + delete hook)
  </read_first>
  <action>
    Create `src/components/transactions/templates/TemplatesManagementDrawer.tsx`:
    - `const { opened, close, reject } = useDrawerContext<void>()`; `const { query, invalidate } = useTransactionTemplates()`; `const templates = query.data ?? []`.
    - `ResponsiveDrawer opened onClose={reject} title="Modelos" data-testid={TemplatesManagementDrawer}`.
    - Header row with a "+ Novo" button (`TemplateBtnNew`), disabled when `templates.length >= 3` (tooltip "Você já tem 3 modelos"): `onClick={() => void renderDrawer(() => <TemplateFormDrawer />).then(() => invalidate())}`. (TemplateFormDrawer already invalidates on success; the extra invalidate is harmless/defensive — or omit and rely on the drawer's own invalidate.)
    - List: for each template, a row (`TemplateRow(id)`) showing `name` (primary), a type badge, and the account name (resolve `payload.account_id` via `useAccounts`), with an edit button (`TemplateBtnEdit(id)` → `renderDrawer(() => <TemplateFormDrawer template={t} />)`) and a delete button (`TemplateBtnDelete(id)`).
    - Delete: on delete click, show a confirmation (inline `Modal`/confirm mirroring `DeleteCategoryModal`, or a `renderDrawer` confirm) then `useDeleteTransactionTemplate({ onSuccess: () => invalidate() }).mutation.mutate(t.id)`. Confirm button testid `TemplateBtnConfirmDelete`.
    - Empty state when `templates.length === 0` ("Nenhum modelo ainda").
    Extract a `TemplateListRow` subcomponent if the file exceeds ~200 lines. No `any`, no new `useEffect`.
  </action>
  <verify><automated>cd frontend && npx tsc --noEmit 2>&1 | tail -8 && npm run lint 2>&1 | tail -5</automated></verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/transactions/templates/TemplatesManagementDrawer.tsx`
    - `grep -q 'TemplateFormDrawer' frontend/src/components/transactions/templates/TemplatesManagementDrawer.tsx`
    - `grep -q 'useDeleteTransactionTemplate' frontend/src/components/transactions/templates/TemplatesManagementDrawer.tsx`
    - `grep -q 'templates.length >= 3' frontend/src/components/transactions/templates/TemplatesManagementDrawer.tsx` (or an equivalent cap-disable)
    - delete requires a confirm step before mutating
    - `cd frontend && npx tsc --noEmit` exits 0; `npm run lint` no new errors
  </acceptance_criteria>
  <done>Management drawer lists templates, creates/edits via TemplateFormDrawer, deletes with confirm, respects the 3-cap; refreshes via invalidate.</done>
</task>

<task type="auto">
  <name>Task 3: Entry point in the transactions toolbar menu</name>
  <read_first>
    - frontend/src/pages/TransactionsPage.tsx (the IconDots Menu.Dropdown with "Importar transações")
    - frontend/src/testIds/transactions.ts
  </read_first>
  <action>
    In `src/pages/TransactionsPage.tsx`, add a `Menu.Item` "Gerenciar modelos" (with an icon, e.g. `IconLayoutGrid`/`IconTemplate` from tabler) as a sibling of "Importar transações" inside the existing `IconDots` `Menu.Dropdown`, `data-testid={TransactionsTestIds.MenuItemManageTemplates}`, `onClick={() => void renderDrawer(() => <TemplatesManagementDrawer />)}`. Import `TemplatesManagementDrawer` and `renderDrawer`.
    Add `MenuItemManageTemplates: 'menu_item_manage_templates'` to `src/testIds/transactions.ts`.
    Do NOT refactor the rest of TransactionsPage (it is a known-divergence fat file) — additive change only.
  </action>
  <verify><automated>cd frontend && npx tsc --noEmit 2>&1 | tail -6 && npm run lint 2>&1 | tail -5 && npx vitest run src/components/transactions/ 2>&1 | tail -8</automated></verify>
  <acceptance_criteria>
    - `grep -q 'MenuItemManageTemplates' frontend/src/pages/TransactionsPage.tsx`
    - `grep -q 'TemplatesManagementDrawer' frontend/src/pages/TransactionsPage.tsx`
    - `grep -q "MenuItemManageTemplates: 'menu_item_manage_templates'" frontend/src/testIds/transactions.ts`
    - `cd frontend && npx tsc --noEmit` exits 0; `npm run lint` no new errors; existing transactions vitest still green
  </acceptance_criteria>
  <done>The management drawer is reachable from the transactions "more options" menu; typecheck/lint/tests clean.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0; `npm run lint` no new errors; `npx vitest run src/components/transactions/` green.
- MNG-01 mapping: manage surface (Task 2) reachable from transactions area (Task 3), create/edit (Task 1) + delete (Task 2), list shows name/type/account, chip+list refresh via invalidate.
</verification>

<success_criteria>
- Users open a management drawer from the transactions area; list shows name/type/account; create/edit/delete all work and refresh the chip row immediately; split edited in templateMode; 3-cap respected in the UI.
</success_criteria>

<output>After completion, create `.planning/phases/30-frontend-management-ui/30-02-SUMMARY.md`.</output>
