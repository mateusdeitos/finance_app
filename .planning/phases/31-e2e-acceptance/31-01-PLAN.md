---
phase: 31-e2e-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/pages/TransactionTemplatesPage.ts
  - frontend/e2e/tests/transaction-templates.spec.ts
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-03, TMPL-04, TMPL-05, APPLY-01, APPLY-02, APPLY-03, APPLY-04, MNG-01, MNG-02, MNG-03, SAFE-01, SAFE-02]

must_haves:
  truths:
    - "A TransactionTemplatesPage page object drives the management drawer, template form, chip apply, and save-as-template flows using ONLY testid selectors and formFields.ts classes scoped to the correct drawer"
    - "The spec covers manage create/edit/delete, chip apply, stale-reference degradation, save-as-template, and cap enforcement — each with a fresh user (getAuthTokenForUser + apiFetchAs + openAuthedPage + page.close())"
    - "The spec compiles (tsc / playwright test --list); full execution is delegated to CI (no Docker in sandbox)"
  artifacts:
    - path: "frontend/e2e/tests/transaction-templates.spec.ts"
      provides: "Playwright acceptance suite for the templates lifecycle"
      contains: "getAuthTokenForUser"
    - path: "frontend/e2e/pages/TransactionTemplatesPage.ts"
      provides: "page object for template drawers + chip apply + save-as-template"
      contains: "TransactionTemplatesPage"
---

<objective>
Add a Playwright acceptance suite for transaction templates: a `TransactionTemplatesPage` page object and `transaction-templates.spec.ts` covering management CRUD, chip apply (incl. stale-reference degradation), save-as-template, and cap enforcement. Follow the per-user isolation + testid-only conventions. Compilation-verify locally; execution runs in CI.

Purpose: Phase 31 acceptance gate for v1.7.
Output: 1 page object + 1 spec. No product code changes (unless a real bug surfaces).
</objective>

<execution_context>
@/home/user/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-e2e-acceptance/31-CONTEXT.md
@frontend/CLAUDE.md

<interfaces>
Helpers (`frontend/e2e/helpers/`):
- `api.ts`: `getAuthTokenForUser(email): Promise<string>`, `apiFetchAs(token, path, opts?): Promise<Response>` (throws on non-2xx), `openAuthedPage(browser, token): Promise<Page>`, `apiListTransactions(month, year, {token})`.
- `createUserAndPartner.ts`: `createUserAndPartner(prefix)` → `{ userToken, userConnAccountId, connectionId, ... }` (accepted connection) for split cases.
- `formFields.ts`: `TextField`, `CurrencyField`, `SelectField`, `SegmentedField`, `TagsField`, ... constructed as `new X(rootLocator, testid)`; `SelectField.pick(optionTestId)`, `SegmentedField.pick(optionTestId)`.

Page object to mirror: `frontend/e2e/pages/TransactionsPage.ts` (drawer-scoped fields, `openCreateForm`, `submitForm`, `assertNoFormErrors`).

Spec pattern to mirror: `frontend/e2e/tests/shared-account-expenses.spec.ts` (`{ browser }` fixture, fresh user, `await page.close()`).

Testids (import from `@/testIds` → `TransactionsTestIds`): see 31-CONTEXT §"Template testids". Key: `BtnMoreOptions`, `MenuItemManageTemplates`, `TemplatesManagementDrawer`, `TemplateBtnNew`, `TemplateRow(id)`, `TemplateBtnEdit(id)`, `TemplateBtnDelete(id)`, `TemplateBtnConfirmDelete(id)`, `TemplateFormDrawer`, `TemplateInputName`, `TemplateBtnSave`, `TemplateFormError`, `TemplateChipsRow`, `TemplateChip(id)`, `BtnSaveAsTemplate`, `SaveAsTemplateDrawer`, `SaveAsTemplateInputName`, `TemplateBtnConfirmSaveAsTemplate`, `SaveAsTemplateError`, `DrawerCreate`, `InputAmount`, `SegmentedTransactionType`, `SegmentTransactionType(t)`, `SelectAccount`, `OptionAccount(id)`, `SelectCategory`, `OptionCategory(id)`, `TagsInput`.

Backend endpoints (via apiFetchAs): `POST /api/accounts {name,initial_balance}`, `POST /api/categories {name}`, `POST /api/tags {name}`, `POST /api/transaction-templates {name, payload}`, `GET /api/transaction-templates`, `DELETE /api/transaction-templates/:id`, `DELETE /api/accounts/:id`. `payload` = `{ type, account_id?, category_id?, destination_account_id?, description, tag_ids?, split_settings? }` (no amount/date).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TransactionTemplatesPage page object</name>
  <read_first>
    - frontend/e2e/pages/TransactionsPage.ts (structure, drawer-scoped fields, formFields usage, submit/assert helpers)
    - frontend/e2e/helpers/formFields.ts (field classes + signatures)
    - frontend/src/testIds/transactions.ts (exact template testids)
    - frontend/src/components/transactions/templates/TemplateFormDrawer.tsx + TemplateListRow.tsx + TemplatesManagementDrawer.tsx + SaveAsTemplateDrawer.tsx (confirm the DOM: which fields render, the drawer/root testids, delete-confirm being inline)
  </read_first>
  <action>
    Create `frontend/e2e/pages/TransactionTemplatesPage.ts` exporting a `TransactionTemplatesPage` class constructed with `(page: Page)`. Cache drawer locators: `managementDrawer = page.getByTestId(TemplatesManagementDrawer)`, `formDrawer = page.getByTestId(TemplateFormDrawer)`, `saveAsDrawer = page.getByTestId(SaveAsTemplateDrawer)`, `createDrawer = page.getByTestId(DrawerCreate)`. Methods (scope every field lookup to the right drawer locator; use formFields.ts classes; testid-only):
    - `openManagementDrawer()` — click `BtnMoreOptions`, click `MenuItemManageTemplates`, `await expect(managementDrawer).toBeVisible()`.
    - `openNewTemplateForm()` — click `TemplateBtnNew` (in managementDrawer), `await expect(formDrawer).toBeVisible()`.
    - `openEditTemplateForm(id)` — click `TemplateBtnEdit(id)`, expect formDrawer visible.
    - `fillTemplateForm({ name?, type?, accountId?, categoryId?, description?, tags? })` — `new TextField(formDrawer, TemplateInputName).fill(name)`; `new SegmentedField(formDrawer, SegmentedTransactionType).pick(SegmentTransactionType(type))`; `new SelectField(formDrawer, SelectAccount).pick(OptionAccount(accountId))`; category via `SelectField(formDrawer, SelectCategory).pick(OptionCategory(categoryId))`; description via the description field testid (check TemplateFormFields for the exact id — likely `InputDescription`); tags via `TagsField(formDrawer, TagsInput).add(tags)`. Only touch provided fields.
    - `saveTemplateForm()` — click `TemplateBtnSave` in formDrawer; assert `formDrawer.getByTestId(TemplateFormError)` not visible; `await expect(formDrawer).not.toBeVisible()`.
    - `deleteTemplate(id)` — click `TemplateBtnDelete(id)`, then `TemplateBtnConfirmDelete(id)`; `await expect(page.getByTestId(TemplateRow(id))).not.toBeVisible()` (or the caller asserts).
    - `expectTemplateRow(id, { name? })` — assert `TemplateRow(id)` visible (and name text if given).
    - `applyChip(id)` — assumes the create form is open; `await expect(page.getByTestId(TemplateChip(id))).toBeVisible()`, click it.
    - `saveCurrentFormAsTemplate(name?)` — click `BtnSaveAsTemplate` (in createDrawer), expect `saveAsDrawer` visible, optionally overwrite `SaveAsTemplateInputName`, click `TemplateBtnConfirmSaveAsTemplate`, assert `SaveAsTemplateError` not visible + `saveAsDrawer` hides.
    - Small getters: `chip(id)`, `newTemplateButton()`, `saveAsTemplateButton()` returning locators for enabled/disabled assertions.
    No product-code changes. If a testid the plan names doesn't exist (e.g. description id), read the component and use the actual id; do NOT invent selectors.
  </action>
  <verify><automated>cd frontend && npx tsc --noEmit -p e2e/tsconfig.json 2>&1 | tail -8 || npx tsc --noEmit 2>&1 | tail -8</automated></verify>
  <acceptance_criteria>
    - `test -f frontend/e2e/pages/TransactionTemplatesPage.ts`
    - `grep -q 'class TransactionTemplatesPage' frontend/e2e/pages/TransactionTemplatesPage.ts`
    - selectors are testid-based only (no `.mantine-`, `getByRole` as primary, or class probes)
    - the page object typechecks (e2e tsconfig or root tsc)
  </acceptance_criteria>
  <done>Page object drives all template flows via testids + formFields classes, scoped per drawer; typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: transaction-templates.spec.ts acceptance suite</name>
  <read_first>
    - frontend/e2e/tests/shared-account-expenses.spec.ts (fresh-user pattern, page.close, split setup via createUserAndPartner)
    - frontend/e2e/tests/transactions.spec.ts (assertion helpers, apiListTransactions usage) — but DO NOT copy its beforeAll shared-user pattern
    - frontend/e2e/helpers/api.ts + createUserAndPartner.ts (setup helpers + signatures)
    - frontend/e2e/pages/TransactionsPage.ts (openCreateForm/fill/submit) + the new TransactionTemplatesPage
  </read_first>
  <action>
    Create `frontend/e2e/tests/transaction-templates.spec.ts`. Each test: a fresh user via `getAuthTokenForUser(\`e2e-templates-<case>-${Date.now()}@financeapp.local\`)`, seed prerequisites via `apiFetchAs`, `const page = await openAuthedPage(browser, token)`, drive via `TransactionsPage` + `TransactionTemplatesPage`, assert via testids + `apiFetchAs('GET /api/transaction-templates')`, `await page.close()` at the end. Use `{ browser }`. Implement these tests (mirror the 31-CONTEXT scenarios):
    1. **manage: create** — seed account+category; open management → new → fill → save → `expectTemplateRow`; `GET` returns 1 template with the right name/payload.
    2. **manage: edit** — seed a template via API; open management → edit → change name → save → row shows new name; `GET` confirms.
    3. **manage: delete** — seed a template; open management → delete + confirm → row gone; `GET` empty.
    4. **chip apply** — seed a template (account+category+description); `txPage.openCreateForm()`; `templatesPage.applyChip(id)`; assert the account Select shows the value (e.g. `OptionAccount`/select input) and the amount input is empty and focused (`await expect(createDrawer.getByTestId(InputAmount)).toBeFocused()`); assert description filled.
    5. **stale-ref apply (APPLY-04)** — seed a template referencing an account; `DELETE /api/accounts/:id`; open create form; apply chip; assert NO form error is visible and the form is usable (description still filled, account field cleared) — no crash.
    6. **save as template (MNG-02)** — `txPage.openCreateForm()`, fill type/account/category/description (+ amount, ignored by the template); `templatesPage.saveCurrentFormAsTemplate()`; assert `GET` returns a template named from the description; reopen create form and assert the new `TemplateChip(id)` appears.
    7. **cap enforcement (SAFE-01 UI)** — seed 3 templates via API; open management drawer → assert `TemplateBtnNew` is disabled; open create form → assert `BtnSaveAsTemplate` is disabled.
    8. (include if time permits) **split template (TMPL-05)** — `createUserAndPartner`; seed a template with `split_settings:[{connection_id, percentage:50}]`; apply chip; expand split; assert a split row/percentage is present.
    Keep tests independent and hermetic. Prefer API verification (`GET`) as the source of truth, plus one UI assertion per scenario. Add `test.describe('Transaction Templates', ...)`.
  </action>
  <verify><automated>cd frontend && (npx tsc --noEmit -p e2e/tsconfig.json 2>&1 | tail -8 || npx tsc --noEmit 2>&1 | tail -8) && npx playwright test e2e/tests/transaction-templates.spec.ts --list 2>&1 | tail -20</automated></verify>
  <acceptance_criteria>
    - `test -f frontend/e2e/tests/transaction-templates.spec.ts`
    - `grep -q 'getAuthTokenForUser' frontend/e2e/tests/transaction-templates.spec.ts`
    - `grep -q 'await page.close()' frontend/e2e/tests/transaction-templates.spec.ts`
    - no `beforeAll` shared-user pattern; no `.mantine-`/`getByRole`-primary selectors
    - `npx playwright test e2e/tests/transaction-templates.spec.ts --list` lists the tests (compiles) OR tsc on the e2e config passes — full run is delegated to CI (note this in SUMMARY)
  </acceptance_criteria>
  <done>Acceptance spec compiles and lists all template scenarios; per-user isolation + testid-only; execution delegated to CI.</done>
</task>

</tasks>

<verification>
- e2e specs typecheck / `playwright test --list` compiles them; document that the full run happens in CI's `e2e` job (no Docker locally).
- Selectors are testid-only; each test uses a fresh user and `page.close()`.
- Scenarios cover manage CRUD, chip apply, stale-ref, save-as-template, cap.
</verification>

<success_criteria>
- A compiling Playwright acceptance suite for the templates lifecycle, auto-discovered by `testDir: ./e2e/tests`, ready for CI to execute.
</success_criteria>

<output>After completion, create `.planning/phases/31-e2e-acceptance/31-01-SUMMARY.md`.</output>
