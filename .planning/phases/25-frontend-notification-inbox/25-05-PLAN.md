---
phase: 25-frontend-notification-inbox
plan: 05
type: execute
wave: 4
depends_on: [25-02, 25-04]
files_modified:
  - frontend/src/components/DesktopSidebar.tsx
  - frontend/src/components/MobileTabBar.tsx
  - frontend/src/components/MobileMoreDrawer.tsx
  - frontend/e2e/tests/notifications-inbox.spec.ts
autonomous: true
requirements: [INBOX-01, INBOX-02, INBOX-03, INBOX-04]
must_haves:
  truths:
    - "Desktop sidebar shows a 'Notificações' nav link to /notifications with a BLUE unread count badge (cap 9+) when unread exist (INBOX-02)"
    - "Mobile 'Mais' tab shows a blue Indicator dot (no number) when unread exist; opening the MoreDrawer shows a 'Notificações' item with a blue count badge that opens the inbox drawer (INBOX-01/02)"
    - "The blue notification badge is visually distinct from the existing red Cobranças badge"
    - "An authored Playwright spec exercises open-inbox, unread styling, tap-navigate+mark-read, and mark-all (Docker-gated; gate is tsc/build, not execution)"
  artifacts:
    - path: "frontend/src/components/DesktopSidebar.tsx"
      provides: "/notifications nav link + blue unread badge"
      contains: "/notifications"
    - path: "frontend/src/components/MobileTabBar.tsx"
      provides: "blue Indicator dot on the 'Mais' tab when unread > 0"
      contains: "Indicator"
    - path: "frontend/src/components/MobileMoreDrawer.tsx"
      provides: "'Notificações' MoreItem opening NotificationInboxDrawer with blue count badge"
      contains: "NotificationInboxDrawer"
    - path: "frontend/e2e/tests/notifications-inbox.spec.ts"
      provides: "authored Playwright inbox spec (data-testid selectors, route-mocked)"
      contains: "getByTestId"
  key_links:
    - from: "frontend/src/components/DesktopSidebar.tsx + MobileTabBar.tsx + MobileMoreDrawer.tsx"
      to: "useNotificationUnreadCount().query.data.count"
      via: "showBadge / Indicator disabled={unreadCount === 0}; blue color"
      pattern: "useNotificationUnreadCount"
    - from: "frontend/src/components/MobileMoreDrawer.tsx"
      to: "renderDrawer(() => <NotificationInboxDrawer />)"
      via: "MoreItem onSelect opens the inbox bottom sheet"
      pattern: "renderDrawer"
---

<objective>
Wire the inbox into the app's three nav surfaces and author the Playwright e2e spec. This makes the inbox reachable and surfaces the unread count where the user expects it.

Purpose: Per OD-4 (LOCKED): desktop gets a "Notificações" sidebar nav link with a BLUE unread count badge (distinct from the red Cobranças badge); mobile gets a blue `Indicator` dot on the "Mais" tab (no number) plus a "Notificações" item in the MoreDrawer (with a blue count badge) that opens the mobile inbox bottom sheet. Reuse the existing badge/`showBadge` patterns and the global `router`/`renderDrawer` conventions. The e2e spec is authored against `data-testid` selectors with route-mocked API responses; it is Docker-gated (not run this phase) — its gate is that it type-checks/builds.

Output: edits to DesktopSidebar, MobileTabBar, MobileMoreDrawer; a new authored e2e spec.
</objective>

<execution_context>
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/CLAUDE.md
@.planning/phases/25-frontend-notification-inbox/25-UI-SPEC.md
@.planning/phases/25-frontend-notification-inbox/25-02-SUMMARY.md
@.planning/phases/25-frontend-notification-inbox/25-04-SUMMARY.md

<interfaces>
<!-- Verified this session + Plans 02/04. Use directly. -->

useNotificationUnreadCount(select?) (Plan 02) → { query, invalidate }; read count via `useNotificationUnreadCount((d) => d.count)` then `unreadCount = query.data ?? 0`.
NotificationInboxDrawer (Plan 04) — mobile bottom sheet, opened via renderDrawer.
NotificationsTestIds (Plan 02): NavBellDesktop, MaisTabIndicator, MoreDrawerNotificationsItem. CommonTestIds.NavBadge('notifications') → 'nav_badge_notifications' (reused for the count badge on both surfaces).

DesktopSidebar.tsx (verified): `navLinks` array of `{ to, label, icon }`; the map at the nav group renders each Link; existing badge pattern:
  const showBadge = to === "/charges" && pendingCount > 0;
  {showBadge && <Badge size="xs" circle color="red" data-testid={CommonTestIds.NavBadge(to.slice(1))}>{pendingCount}</Badge>}
Imports: Badge from @mantine/core; icons from @tabler/icons-react; CommonTestIds from @/testIds. ADD IconBell import + useNotificationUnreadCount import.

MobileTabBar.tsx (verified): tabs array (4 Links) + a separate "Mais" `UnstyledButton` (lines 78-89) whose icon lives in `<span className={classes.iconWrap}><IconDots size={20} stroke={1.8} /></span>`. WRAP that iconWrap span in a Mantine `Indicator`. Imports: Badge, UnstyledButton from @mantine/core; ADD Indicator + IconBell? (no — keep IconDots for Mais) + useNotificationUnreadCount.

MobileMoreDrawer.tsx (verified): uses global `router` from '@/router', `renderDrawer`/`useDrawerContext`; an `items: MoreItem[]` list; already renders a NotificationToggleRow (Phase 24) above "Sair"; MoreItem rows carry `data-testid={MobileNavTestIds.MoreItem(item.key)}`. ADD a "Notificações" entry positioned above "Sair" that opens the inbox drawer.

Badge color: BLUE for notifications (color="blue"), distinct from the RED charges badge (OD-2/OD-4). Cap display: unreadCount > 9 ? '9+' : unreadCount.

e2e conventions (frontend/CLAUDE.md): one fresh user per test (getAuthTokenForUser + openAuthedPage), data-testid selectors ONLY (no getByRole/getByText as primary), route-mock via page.route().fulfill(). Run cmd: `npm run test:e2e` (Docker) — DO NOT run this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DesktopSidebar /notifications link + blue badge</name>
  <files>frontend/src/components/DesktopSidebar.tsx</files>
  <action>
    Per UI-SPEC Surface 1 (desktop) + OD-4.
    1. Import `IconBell` from '@tabler/icons-react' and `useNotificationUnreadCount` from '@/hooks/useNotificationUnreadCount'.
    2. Add to the `navLinks` array: `{ to: "/notifications", label: "Notificações", icon: IconBell }` (place after "/charges" or per the design's ordering).
    3. In the component, read `const { query: unreadQuery } = useNotificationUnreadCount((d) => d.count); const unreadCount = unreadQuery.data ?? 0;`.
    4. In the navLinks map, generalize the badge: the existing `showBadge` is charges-specific (red). Add a parallel notifications badge: when `to === "/notifications" && unreadCount > 0`, render `<Badge size="xs" circle color="blue" data-testid={CommonTestIds.NavBadge("notifications")} aria-label={\`${unreadCount} notificações não lidas\`}>{unreadCount > 9 ? "9+" : unreadCount}</Badge>`. Keep the charges red badge unchanged. Optionally add `data-testid={NotificationsTestIds.NavBellDesktop}` to the /notifications Link.
    Do NOT change the red Cobranças badge color or the charges logic — blue is reserved for notifications to keep the two counters distinct (OD-2).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <done>The sidebar has a "/notifications" nav link (IconBell, "Notificações") that routes to the desktop page; a BLUE `nav_badge_notifications` badge (cap 9+) renders only when unreadCount > 0; the red charges badge is unchanged; build + lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: MobileTabBar Mais-tab Indicator dot + MobileMoreDrawer Notificações item</name>
  <files>frontend/src/components/MobileTabBar.tsx, frontend/src/components/MobileMoreDrawer.tsx</files>
  <action>
    Per UI-SPEC Surface 1 (mobile) + OD-4 (LOCKED: NO new tab).
    MobileTabBar.tsx:
    1. Import `Indicator` from '@mantine/core' and `useNotificationUnreadCount` from '@/hooks/useNotificationUnreadCount'. Read `unreadCount` as in Task 1.
    2. Wrap the "Mais" `UnstyledButton`'s `<span className={classes.iconWrap}>...<IconDots/></span>` in a Mantine `Indicator`: `color="blue"`, `size={8}`, `disabled={unreadCount === 0}`, `position="top-end"`, `offset={4}`, `data-testid={NotificationsTestIds.MaisTabIndicator}`, `aria-hidden`. Keep `IconDots` as the Mais icon (do NOT swap to IconBell). NO count number on the tab — the numeric badge lives in the drawer item.
    3. Update the "Mais" button `aria-label` to reflect unread state: `aria-label={unreadCount > 0 ? \`Mais — ${unreadCount} notificações não lidas\` : "Mais"}`.
    MobileMoreDrawer.tsx:
    4. Import `IconBell` and `NotificationInboxDrawer` (from '@/components/notifications/NotificationInboxDrawer') and `useNotificationUnreadCount`. Read unreadCount.
    5. Add a `MoreItem` for "Notificações" positioned ABOVE the "Sair" item (and consistent with where the Phase 24 NotificationToggleRow sits): label "Notificações", icon `IconBell`, onSelect opens the inbox bottom sheet via `void renderDrawer(() => <NotificationInboxDrawer />).catch(() => {})` and then closes the MoreDrawer (`close()`), `data-testid={NotificationsTestIds.MoreDrawerNotificationsItem}`. Render a BLUE count badge inside the item when `unreadCount > 0`: `<Badge size="xs" color="blue" data-testid={CommonTestIds.NavBadge("notifications")}>{unreadCount > 9 ? "9+" : unreadCount}</Badge>`.
    Keep the existing items (Invite, Import, NotificationToggleRow, Sair) intact. Use the existing global `router`/`renderDrawer` patterns already in the file.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <done>The "Mais" tab shows a blue `Indicator` dot (no number, `mobile_mais_tab_indicator`) only when unreadCount > 0 and updates its aria-label; the MoreDrawer has a "Notificações" item (above "Sair") with a blue count badge that opens `NotificationInboxDrawer` via renderDrawer and closes the MoreDrawer; no new mobile tab added; build + lint pass.</done>
</task>

<task type="auto">
  <name>Task 3: Authored Playwright e2e spec (Docker-gated, not run)</name>
  <files>frontend/e2e/tests/notifications-inbox.spec.ts</files>
  <action>
    Author a Playwright spec per frontend/CLAUDE.md e2e conventions. It is AUTHORED but NOT RUN this phase (Docker unavailable) — its gate is that the project type-checks/builds; it must use only `data-testid` selectors (no getByRole/getByText as primary) and the per-test fresh-user pattern (`getAuthTokenForUser` + `openAuthedPage` + `apiFetchAs` from e2e/helpers). Mock the inbox API with `page.route('**/api/notifications**', route => route.fulfill({...}))` (and unread-count, charges `id[]`, transactions `by-ids`) so the spec does not depend on real notification data.
    Cover (using NotificationsTestIds + CommonTestIds + MobileNavTestIds):
    1. Desktop: navigate to /notifications; assert the list renders rows (`NotificationsTestIds.Row(id)`); assert an unread row shows its `UnreadDot(id)` and a read row does not; assert the blue `CommonTestIds.NavBadge('notifications')` shows the capped count.
    2. Tap a row → assert URL changed to `/charges` (or `/transactions`) AND the row no longer shows the unread dot (mark-read optimistic) — mock the `POST /:id/read` 204.
    3. Mark-all: click `BtnMarkAllRead` (mock `POST /read-all` 204) → assert the badge disappears and all rows are read; assert the button hides.
    4. Load more: with a mocked `has_more:true` first page and a second page, click `BtnLoadMore` → assert more rows appear; with `has_more:false`, assert the "Você está em dia" end indicator (assert via a container testid if present, else accept the load-more button absent).
    5. Empty: mock an empty notifications response → assert `EmptyState` visible.
    Add page-object helpers under e2e/pages if it keeps the spec clean, but the spec itself must use testid selectors. Do NOT add a phase-completion dependency on running this spec.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
  </verify>
  <done>`e2e/tests/notifications-inbox.spec.ts` exists, uses fresh-user-per-test + route-mocking + `getByTestId`-only selectors, covers open/unread-styling/tap-navigate+mark-read/mark-all/load-more/empty; it type-checks under `tsc -b` and lints clean; it is NOT executed this phase (Docker-gated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav surfaces → unread-count query | Client-rendered badge derived from an IDOR-scoped count endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-14 | Information Disclosure | unread badge count on nav | accept | Count comes from the Phase-23 IDOR-scoped `unread-count` endpoint; capped to '9+' display; no entity data exposed in the badge. |
| T-25-15 | Tampering (navigation) | MoreDrawer item opens inbox drawer | mitigate | Opens a local React drawer via `renderDrawer` (no URL navigation); the inbox row tap navigation is guarded by `deriveDeepLink` (Plan 04). No user-supplied URLs. |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` — clean (tasks 1-2).
- `cd frontend && npx tsc -b` — the e2e spec type-checks (task 3).
- grep confirms: blue badge color on the notifications badge (NOT red); `Indicator` wraps the Mais iconWrap; MoreDrawer opens `NotificationInboxDrawer`; no new entry in the MobileTabBar `tabs` array (still 4 tabs).
- The Playwright spec uses only `getByTestId` (no `.mantine-*`, no `getByRole`/`getByText` as primary).
</verification>

<success_criteria>
- INBOX-01: the inbox is reachable from desktop (sidebar link) and mobile (MoreDrawer item → bottom sheet).
- INBOX-02: a blue unread badge (cap 9+) shows on the desktop link and the MoreDrawer item; a blue dot shows on the "Mais" tab; both distinct from the red charges badge.
- INBOX-03/04: the authored e2e spec exercises tap-navigate+mark-read and mark-all end-to-end (run in CI where Docker exists).
</success_criteria>

<output>
After completion, create `.planning/phases/25-frontend-notification-inbox/25-05-SUMMARY.md`.
</output>
