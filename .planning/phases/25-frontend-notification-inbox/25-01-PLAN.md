---
phase: 25-frontend-notification-inbox
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/domain/charge.go
  - backend/internal/repository/charge_repository.go
  - backend/internal/handler/charge_handler.go
  - backend/internal/handler/charge_handler_test.go
  - backend/internal/repository/charge_repository_test.go
  - backend/internal/handler/transaction_handler.go
  - backend/internal/handler/transaction_handler_test.go
  - backend/cmd/server/main.go
  - backend/docs
autonomous: true
requirements: [INBOX-01, INBOX-02, INBOX-03, INBOX-04]
user_setup: []

must_haves:
  truths:
    - "GET /api/charges?id[]=A&id[]=B returns only the caller's charges among those IDs (IDOR-scoped)"
    - "GET /api/transactions/by-ids?id[]=A&id[]=B returns the caller's transactions across any period (no month/year required)"
    - "An id the caller does not own is absent from the response (not an error) so the frontend can mark it 'missing'"
    - "Empty id[] on /by-ids returns 200 with an empty array (not 400)"
  artifacts:
    - path: "backend/internal/domain/charge.go"
      provides: "ChargeSearchOptions.IDs []int filter field"
      contains: "IDs"
    - path: "backend/internal/repository/charge_repository.go"
      provides: "WHERE id IN clause in Search() when len(options.IDs) > 0"
      contains: "id IN"
    - path: "backend/internal/handler/transaction_handler.go"
      provides: "ListByIDs handler reusing Search(Period{0,0}, TransactionFilter{IDs, WithSettlements:true})"
      contains: "ListByIDs"
    - path: "backend/cmd/server/main.go"
      provides: "transactions/by-ids route registered before /:id"
      contains: "by-ids"
  key_links:
    - from: "GET /api/charges List handler"
      to: "chargeRepository.Search id IN clause"
      via: "ChargeSearchOptions.IDs bound from query:\"id[]\", UserID force-set"
      pattern: "id IN"
    - from: "GET /api/transactions/by-ids ListByIDs"
      to: "transactionService.Search"
      via: "Period{0,0} + TransactionFilter{IDs, WithSettlements:true} + filter.UserID=&userID"
      pattern: "ListByIDs"
---

<objective>
Add the two backend bulk get-by-IDs paths the frontend amount-resolver depends on (D-25-1). Charges gain an `IDs` filter exposed through the EXISTING `GET /api/charges` List handler (IDOR gate already present). Transactions gain a NEW thin `GET /api/transactions/by-ids` handler that reuses `TransactionService.Search` with a zero period — necessary because the existing `Search` HTTP handler hard-requires `month`+`year` (400 otherwise) and cannot do a cross-period batch.

Purpose: bound entity amount resolution to ≤2 requests per inbox page (no N+1), per locked decision D-25-1. This is the dependency spine — the frontend resolver (Plan 03) cannot ship until these land.
Output: charge IDs filter + repo WHERE; transaction by-ids handler + route; swagger regenerated; mockery handler unit tests; authored-not-run charge-repo `id IN` integration test.
</objective>

<execution_context>
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-frontend-notification-inbox/25-CONTEXT.md
@.planning/phases/25-frontend-notification-inbox/25-RESEARCH.md
@backend/CLAUDE.md

<interfaces>
<!-- Verified from live code this planning session. Use directly — no exploration needed. -->

backend/internal/domain/charge.go — ChargeSearchOptions (current, IDs field is MISSING — add it):
```go
type ChargeSearchOptions struct {
    UserID       int          `json:"user_id"       query:"user_id"`
    Direction    string       `json:"direction"     query:"direction"`
    Status       ChargeStatus `json:"status"        query:"status"`
    ConnectionID int          `json:"connection_id" query:"connection_id"`
    Limit        int          `json:"limit"         query:"limit"`
    Offset       int          `json:"offset"        query:"offset"`
}
```
`Charge.Amount` is `*int64` (serialized with omitempty — an amount-less/soft-deleted charge serializes without the field).

backend/internal/repository/charge_repository.go — Search() filter order (add id IN AFTER the existing filters, BEFORE Order):
```go
func (r *chargeRepository) Search(ctx context.Context, options domain.ChargeSearchOptions) ([]*domain.Charge, error) {
    // ... Limit/Offset ...
    if options.UserID > 0 { /* charger_user_id / payer_user_id WHERE (IDOR gate) */ }
    if options.Status != "" { query = query.Where("status = ?", options.Status) }
    if options.ConnectionID > 0 { query = query.Where("connection_id = ?", options.ConnectionID) }
    // <-- ADD: if len(options.IDs) > 0 { query = query.Where("id IN ?", options.IDs) }
    query = query.Order("created_at DESC")
}
```

backend/internal/handler/charge_handler.go — List already binds ChargeSearchOptions via c.Bind(&options) and force-sets options.UserID = userID. No logic change — only a swagger @Param annotation.

backend/internal/handler/transaction_handler.go — GetByID precedent (lines ~170-185) already uses the exact Search pattern to reuse for ListByIDs:
```go
t, err := h.transactionService.Search(ctx, userID, domain.Period{Month: 0, Year: 0},
    domain.TransactionFilter{IDs: []int{id}, WithSettlements: true})
```
`Search` HTTP handler (line ~110) runs `period.IsValid()` and returns 400 if month/year invalid — DO NOT route /by-ids through it. `domain.TransactionFilter` already has `IDs []int query:"id[]"` and `UserID *int`.

backend/cmd/server/main.go — current transaction route order (register /by-ids adjacent to the static GETs, BEFORE /:id):
```go
transactions.GET("", h.Search)
transactions.POST("", h.Create)
transactions.GET("/balance", h.GetBalance)
transactions.GET("/suggestions", h.Suggestions)
transactions.DELETE("/:id", h.Delete)
transactions.GET("/:id", h.GetByID)   // <-- /by-ids MUST be registered before this
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Charges filter-by-IDs via existing List handler</name>
  <files>backend/internal/domain/charge.go, backend/internal/repository/charge_repository.go, backend/internal/handler/charge_handler.go, backend/internal/handler/charge_handler_test.go, backend/internal/repository/charge_repository_test.go</files>
  <behavior>
    - charge_handler_test: binding `id[]=10&id[]=20` populates `options.IDs == []int{10,20}` AND `options.UserID` is force-set to the authenticated user (IDOR) — assert via mockery `mock.MatchedBy` on the `ChargeService.List` call. Use the existing handler-test harness (`appcontext.WithUserID`, httptest, mockery) per backend/CLAUDE.md.
    - charge_repository_test: an `id IN` integration test (ServiceTestWithDBSuite / testcontainers) — seed two charges owned by user A and one owned by user B; query with all three IDs as user A; assert only A's two come back. This test is `-short`-skipped (Docker) → AUTHORED, NOT RUN this phase. Its gate is `go build` + `go vet`, not execution.
  </behavior>
  <action>
    1. Add `IDs []int \`json:"-" query:"id[]"\`` to `domain.ChargeSearchOptions` (per D-25-1; the existing struct has UserID/Direction/Status/ConnectionID/Limit/Offset only).
    2. In `chargeRepository.Search()`, after the UserID/Status/ConnectionID filters and BEFORE `query.Order("created_at DESC")`, add: `if len(options.IDs) > 0 { query = query.Where("id IN ?", options.IDs) }`. GORM-parameterized — never string-interpolate (SQLi guard). The IDOR owner WHERE runs first, so an id the caller does not own is filtered out → not returned → frontend marks `missing`.
    3. In `charge_handler.go` `List`, add ONLY a swagger annotation (no logic change — `c.Bind(&options)` already maps `id[]` to the tag, and `options.UserID = userID` already runs): `// @Param id[] query []int false "Filter by charge IDs" collectionFormat(multi)`.
    4. Write the two tests described in <behavior>. Mark the repo integration test clearly as Docker-gated (build-level only).
    5. Regenerate swagger: replicate the `just generate-docs` recipe — run `swag` (at /root/go/bin/swag; add `$(go env GOPATH)/bin` to PATH) against the handler dir so `backend/docs/*` reflects the new @Param. Do NOT run `just generate-mocks` — `ChargeService.List(ctx, options)` signature is unchanged (only the options struct gained a field, no interface change).
  </action>
  <verify>
    <automated>cd backend && export PATH="$PATH:$(go env GOPATH)/bin" && go build ./... && go vet -tags=integration ./internal/... && go test -short ./internal/handler/ -run TestChargeHandler_List</automated>
  </verify>
  <done>`ChargeSearchOptions.IDs` exists; `Search()` has the `id IN` clause; List has the `id[]` swagger @Param; handler test asserts `id[]` binds + UserID forced; repo integration test authored (Docker-skipped); `go build`/`go vet`/`go test -short` pass; swagger regenerated; no generate-mocks run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transactions by-IDs thin handler + route</name>
  <files>backend/internal/handler/transaction_handler.go, backend/internal/handler/transaction_handler_test.go, backend/cmd/server/main.go</files>
  <behavior>
    - transaction_handler_test (mockery, no Docker): (a) empty `id[]` → 200 with empty JSON array, NOT 400; (b) `id[]=5&id[]=9` present → `transactionService.Search` is called with `Period{Month:0, Year:0}`, `filter.IDs == []int{5,9}`, `filter.UserID == &userID` (IDOR), and `filter.WithSettlements == true` — assert via `mock.MatchedBy`; (c) route resolution — a request to `GET /api/transactions/by-ids` reaches `ListByIDs`, NOT `GetByID` with `id="by-ids"` (Pitfall 2). Use the existing handler-test harness.
  </behavior>
  <action>
    1. Add a `ListByIDs(c echo.Context) error` handler to `transaction_handler.go` mirroring `GetByID` but for a slice (per D-25-1 / RESEARCH skeleton B2): extract `userID` from context; `c.Bind(&filter domain.TransactionFilter)` (binds `id[]`); if `len(filter.IDs) == 0` return `c.JSON(200, []domain.Transaction{})` (empty, no 400); set `filter.UserID = &userID` (IDOR scope, mirrors Search) and `filter.WithSettlements = true`; call `h.transactionService.Search(ctx, userID, domain.Period{Month:0, Year:0}, filter)`; on error `pkgErrors.ToHTTPError(err)`; else `c.JSON(200, txs)`. Add the godoc/swagger block (@Router /api/transactions/by-ids [get], @Param id[] query []int, document the ≤20 ids cap). NO new service method — reuse `Search`.
    2. In `main.go`, register `transactions.GET("/by-ids", h.ListByIDs)` adjacent to the static GETs (`/balance`, `/suggestions`) and BEFORE `transactions.GET("/:id", h.GetByID)` to avoid `:id` shadowing `by-ids` (Echo prioritizes static, but place it before for clarity + the route-resolution test settles it).
    3. Write the three test cases in <behavior>.
    4. Regenerate swagger via the `swag` invocation (as in Task 1). No generate-mocks (reuses `Search`, no interface change).
  </action>
  <verify>
    <automated>cd backend && export PATH="$PATH:$(go env GOPATH)/bin" && go build ./... && go vet -tags=integration ./internal/... && go test -short ./internal/handler/ -run TestTransactionHandler_ListByIDs</automated>
  </verify>
  <done>`ListByIDs` exists and reuses `Search(Period{0,0}, filter{IDs,UserID,WithSettlements:true})`; `/by-ids` registered before `/:id`; handler test covers empty→200[], ids→correct Search args, and route resolves to ListByIDs not GetByID; `go build`/`go vet`/`go test -short` pass; swagger regenerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/charges, /api/transactions/by-ids | Untrusted `id[]` query params cross here from an authenticated user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure / Elevation | charges List `id IN` filter | mitigate | Existing handler force-sets `options.UserID`; repo applies owner WHERE (`charger_user_id`/`payer_user_id`) BEFORE the `id IN` filter, so unowned ids fall out. Covered by charge_handler_test IDOR assertion + repo integration test (Docker-gated). |
| T-25-02 | Information Disclosure / Elevation | transactions `ListByIDs` | mitigate | Handler MUST set `filter.UserID = &userID` (mirrors `Search` line 137) before calling the service. Covered by transaction_handler_test asserting `filter.UserID == &userID`. |
| T-25-03 | Tampering (SQLi) | `id IN` clause | mitigate | GORM parameterized `query.Where("id IN ?", options.IDs)` — never string-interpolated. `id[]` bound as `[]int` by Echo (non-int → bind error → 400). |
| T-25-04 | Denial of Service | unbounded `id[]` list | accept | Frontend bounds to one inbox page's distinct ids (limit 20); cap documented in swagger. Low risk — authenticated, indexed `id IN` query. |
</threat_model>

<verification>
- `cd backend && export PATH="$PATH:$(go env GOPATH)/bin" && go build ./... && go vet -tags=integration ./internal/... && go test -short ./internal/...` all pass.
- `backend/docs` regenerated (swagger reflects `id[]` on charges List and the new `/api/transactions/by-ids`).
- No `just generate-mocks` was run (no interface change).
- Charge-repo `id IN` integration test + transaction route-resolution test are authored; integration test is Docker-skipped (honest limit).
</verification>

<success_criteria>
- `GET /api/charges?id[]=…` returns the caller's charges among those IDs only.
- `GET /api/transactions/by-ids?id[]=…` returns the caller's transactions across periods; empty ids → 200 empty array; unowned ids absent.
- Handler unit tests green under `go test -short`.
</success_criteria>

<output>
After completion, create `.planning/phases/25-frontend-notification-inbox/25-01-SUMMARY.md`
</output>
