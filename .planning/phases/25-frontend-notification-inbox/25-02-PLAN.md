---
phase: 25-frontend-notification-inbox
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/notifications.ts
  - frontend/src/utils/queryKeys.ts
  - frontend/src/api/notifications.ts
  - frontend/src/testIds/notifications.ts
  - frontend/src/hooks/useNotificationInbox.ts
  - frontend/src/hooks/useNotificationUnreadCount.ts
  - frontend/src/components/notifications/describeNotification.ts
  - frontend/src/components/notifications/describeNotification.test.ts
autonomous: true
requirements: [INBOX-01, INBOX-02]
must_haves:
  truths:
    - "The inbox cursor list can be fetched and paginated (newest-first) via a typed hook"
    - "The unread count can be fetched and polls every 60s when focused"
    - "describeNotification produces the correct pt-BR copy per type and amountState"
  artifacts:
    - path: "frontend/src/types/notifications.ts"
      provides: "Notifications namespace (Notification, NotificationListResponse, UnreadCountResponse, types)"
      contains: "namespace Notifications"
    - path: "frontend/src/api/notifications.ts"
      provides: "fetchNotifications, fetchNotificationUnreadCount, markNotificationRead, markAllNotificationsRead"
      exports: ["fetchNotifications", "fetchNotificationUnreadCount", "markNotificationRead", "markAllNotificationsRead"]
    - path: "frontend/src/hooks/useNotificationInbox.ts"
      provides: "useInfiniteQuery cursor list hook returning { query, invalidate }"
      contains: "useInfiniteQuery"
    - path: "frontend/src/hooks/useNotificationUnreadCount.ts"
      provides: "unread-count query hook with 60s refetchInterval, returns { query, invalidate }"
      contains: "refetchInterval"
    - path: "frontend/src/components/notifications/describeNotification.ts"
      provides: "pure pt-BR copy builder consuming { amount, amountState }"
      contains: "describeNotification"
  key_links:
    - from: "frontend/src/hooks/useNotificationInbox.ts"
      to: "GET /api/notifications via fetchNotifications"
      via: "useInfiniteQuery getNextPageParam(last => last.has_more ? last.next_cursor : undefined)"
      pattern: "getNextPageParam"
    - from: "frontend/src/hooks/useNotificationUnreadCount.ts"
      to: "GET /api/notifications/unread-count via fetchNotificationUnreadCount"
      via: "useQuery refetchInterval 60_000, refetchIntervalInBackground false"
      pattern: "QueryKeys.NotificationUnreadCount"
---

<objective>
Build the Phase 25 frontend data foundation: notification types, query keys, the `src/api/notifications.ts` client, the inbox cursor-list and unread-count query hooks, the inbox testId constants, and the pure `describeNotification` copy builder (with unit tests).

Purpose: These artifacts consume the already-shipped Phase 23 inbox API (`GET /api/notifications` cursor, `GET /api/notifications/unread-count`, `POST /:id/read`, `POST /read-all`). They depend on NO backend changes and NO other Phase 25 plan, so they form a parallel Wave-1 foundation that Plans 03/04/05 build on. `describeNotification` is the pure, unit-testable heart of INBOX-01's human-readable copy (and the amountState handling for INBOX-02's read/unread display).

Output: `Notifications` namespace; 4 new `QueryKeys`; notifications api functions; `useNotificationInbox` (useInfiniteQuery), `useNotificationUnreadCount` (60s poll); `NotificationsTestIds` extended with the inbox ids; `describeNotification` + its test.
</objective>

<execution_context>
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/CLAUDE.md
@.planning/phases/25-frontend-notification-inbox/25-RESEARCH.md
@.planning/phases/25-frontend-notification-inbox/25-UI-SPEC.md

<interfaces>
<!-- Verified this session. Use directly. -->

QueryKeys (frontend/src/utils/queryKeys.ts) — current keys end with `PushSubscription: 'push-subscription'`.
ADD (do not remove existing): Notifications, NotificationUnreadCount, NotificationChargesById, NotificationTransactionsById.
(NOTE: do NOT add TransactionById/ChargeById — those belong to the SUPERSEDED UI-SPEC per-id design.)

src/testIds/notifications.ts ALREADY EXISTS with Phase 24 toggle ids:
  export const NotificationsTestIds = {
    SwitchNotifications: 'switch_notifications',
    RowNotifications: 'row_notifications',
    HelperNotifications: 'text_notifications_helper',
  } as const
→ EXTEND this object (keep the three existing keys; do NOT remove them). It is already re-exported from src/testIds/index.ts (no index edit needed).

API fetch-wrapper convention (src/api/charges.ts): `const apiUrl = import.meta.env.VITE_API_URL ?? 'http://localhost:8080'`; fetch with `{ credentials: 'include' }`; throw new Error on !res.ok.

Phase 23 inbox API contract (verified backend):
  GET /api/notifications?cursor=&limit=20 → { notifications: Notification[], next_cursor: string, has_more: boolean }
  GET /api/notifications/unread-count → { count: number }   (field name "count", int64)
  POST /api/notifications/:id/read → 204
  POST /api/notifications/read-all → 204
  Notification = { id, type, entity_type, entity_id, read, created_at } (no amount, no partner_name).

useChargesPendingCount.ts is the precedent for the { query, invalidate } query-hook shape with a select generic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, QueryKeys, notifications api, testIds</name>
  <files>frontend/src/types/notifications.ts, frontend/src/utils/queryKeys.ts, frontend/src/api/notifications.ts, frontend/src/testIds/notifications.ts</files>
  <action>
    1. Create `src/types/notifications.ts` with the `Notifications` namespace EXACTLY as specified in 25-UI-SPEC (Component Inventory → New types): `NotificationType` (`'charge_received' | 'charge_accepted' | 'split_created' | 'split_updated'`), `EntityType` (`'charge' | 'transaction'`), `Notification` (`{ id: number; type: NotificationType; entity_type: EntityType; entity_id: number; read: boolean; created_at: string }`), `NotificationListResponse` (`{ notifications: Notification[]; next_cursor: string; has_more: boolean }`), `UnreadCountResponse` (`{ count: number }`).
    2. In `src/utils/queryKeys.ts`, add 4 keys to the `QueryKeys` const (do NOT remove existing): `Notifications: 'notifications'`, `NotificationUnreadCount: 'notification-unread-count'`, `NotificationChargesById: 'notification-charges-by-id'`, `NotificationTransactionsById: 'notification-transactions-by-id'`. Do NOT add `TransactionById`/`ChargeById` (those are from the superseded UI-SPEC per-id design — see D-25-1 / RESEARCH Conflict Resolution).
    3. Create `src/api/notifications.ts` (follow the api-wrapper convention; NO React, NO hooks): `fetchNotifications({ cursor, limit }: { cursor: string; limit: number }): Promise<Notifications.NotificationListResponse>` → `GET /api/notifications?cursor=<cursor>&limit=<limit>` (omit the `cursor` param when empty string); `fetchNotificationUnreadCount(): Promise<Notifications.UnreadCountResponse>` → `GET /api/notifications/unread-count`; `markNotificationRead(id: number): Promise<void>` → `POST /api/notifications/${id}/read` (204, no body); `markAllNotificationsRead(): Promise<void>` → `POST /api/notifications/read-all` (204, no body). All with `{ credentials: 'include' }`; throw `new Error(...)` on `!res.ok`.
    4. In `src/testIds/notifications.ts`, EXTEND the existing `NotificationsTestIds` object (keep the 3 Phase-24 keys) with the inbox ids from the 25-UI-SPEC testId table: `Drawer: 'drawer_notifications'`, `DrawerClose: 'btn_notifications_close'`, `BtnMarkAllRead: 'btn_mark_all_read'`, `BtnLoadMore: 'btn_notifications_load_more'`, `EmptyState: 'section_notifications_empty'`, `ErrorState: 'section_notifications_error'`, `BtnRetry: 'btn_notifications_retry'`, `Row: (id: number) => \`notification_row_${id}\` as const`, `UnreadDot: (id: number) => \`notification_unread_dot_${id}\` as const`, `NavBellDesktop: 'nav_bell_desktop'`, `MaisTabIndicator: 'mobile_mais_tab_indicator'`, `MoreDrawerNotificationsItem: 'more_drawer_notifications_item'`. Keep `as const`. (Already re-exported from index.ts — no index change.)
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <done>`Notifications` namespace exports all 5 types; `QueryKeys` has the 4 new notification keys (and NOT TransactionById/ChargeById); `src/api/notifications.ts` exports the 4 functions with correct endpoints/methods; `NotificationsTestIds` has both the 3 Phase-24 keys and the 12 inbox keys; `npm run build` (tsc -b && vite build) and `npm run lint` pass.</done>
</task>

<task type="auto">
  <name>Task 2: useNotificationInbox + useNotificationUnreadCount hooks</name>
  <files>frontend/src/hooks/useNotificationInbox.ts, frontend/src/hooks/useNotificationUnreadCount.ts</files>
  <action>
    Per frontend/CLAUDE.md §3 (query hooks return `{ query, invalidate }`, keys from `QueryKeys`, expose a `select` generic; NO useEffect). Follow `useChargesPendingCount.ts` as the precedent.
    1. `useNotificationInbox.ts` — `useInfiniteQuery` (TanStack Query v5):
       - `queryKey: [QueryKeys.Notifications]`
       - `queryFn: ({ pageParam }) => fetchNotifications({ cursor: pageParam, limit: 20 })`
       - `initialPageParam: '' as string`
       - `getNextPageParam: (last) => (last.has_more ? last.next_cursor : undefined)`
       - return `{ query, invalidate }` where `invalidate` invalidates `[QueryKeys.Notifications]`.
       Consumers flatten via `query.data.pages.flatMap(p => p.notifications)`. The "Carregar mais"/"Você está em dia" UI is built in Plan 04 off `query.hasNextPage` / `query.fetchNextPage` / `query.isFetchingNextPage`.
    2. `useNotificationUnreadCount.ts` — `useQuery` with a `select` generic (default `T = Notifications.UnreadCountResponse`):
       - `queryKey: [QueryKeys.NotificationUnreadCount]`, `queryFn: fetchNotificationUnreadCount`
       - `staleTime: 60_000`, `refetchInterval: 60_000`, `refetchIntervalInBackground: false` (pause when tab hidden — Pitfall 5; this is the ONLY polling query in the app, justified by INBOX-02), `refetchOnWindowFocus: true`, `select`.
       - return `{ query, invalidate }` (invalidate `[QueryKeys.NotificationUnreadCount]`).
       The 60s poll is a query option, NOT a useEffect (CLAUDE.md §4).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <done>`useNotificationInbox` uses `useInfiniteQuery` with `initialPageParam: ''` and the `has_more`-driven `getNextPageParam`, returns `{ query, invalidate }`; `useNotificationUnreadCount` uses `useQuery` with `refetchInterval: 60_000` + `refetchIntervalInBackground: false` + a `select` generic, returns `{ query, invalidate }`; no `useEffect` in either; build + lint pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: describeNotification pure copy builder + tests</name>
  <files>frontend/src/components/notifications/describeNotification.ts, frontend/src/components/notifications/describeNotification.test.ts</files>
  <behavior>
    For each type × amountState, the output matches the 25-UI-SPEC Copywriting Contract exactly:
    - charge_received, amountState 'known', amount 5000, partnerName 'Ana', description 'Aluguel' → "Ana te cobrou R$ 50,00: Aluguel"
    - charge_received, 'known', amount 5000, description null → "Ana te cobrou R$ 50,00" (no trailing colon)
    - charge_received, 'loading' → "Ana criou uma cobrança para você"
    - charge_received, 'missing' (404/soft-deleted) → dash in the amount slot: "Ana te cobrou —" (or the no-amount fallback per the UI-SPEC dash rule)
    - charge_accepted, 'known', 5000 → "Ana aceitou sua cobrança de R$ 50,00"; 'loading' → "Ana aceitou sua cobrança"
    - split_created, 'known', 5000 → "Ana adicionou uma transação dividida de R$ 50,00"; 'loading' → "Ana adicionou uma transação dividida"
    - split_updated, 'known', 5000 → "Ana atualizou uma transação dividida (R$ 50,00)"; 'loading' → "Ana atualizou uma transação dividida"; 'missing' → "Ana atualizou uma transação dividida (—)"
    - partnerName unresolved → falls back to "Seu parceiro(a)"
    - unknown type → "Nova notificação"
    `R$ 50,00` formatting comes from `formatBalance(5000)` (existing util) — assert by importing formatBalance, not by hardcoding (keeps the test robust to locale).
  </behavior>
  <action>
    Create `src/components/notifications/describeNotification.ts` as a PURE function (no React, no hooks, no fetch — so it is trivially unit-testable). Signature:
    `describeNotification(n: Notifications.Notification, ctx: { amount: number | null; amountState: 'known' | 'loading' | 'missing'; partnerName: string | null; description?: string | null }): string`
    Implement the per-type templates EXACTLY from the 25-UI-SPEC Copywriting Contract table (binding). Use `formatBalance` from `src/utils/formatCents.ts` for `{amount}` when `amountState === 'known'`. Apply the dash rule: for `missing`, substitute `'—'` in the amount slot where the template embeds an amount; for `loading`, use the no-amount template variant. `partnerName` null → `'Seu parceiro(a)'`. For `charge_received` known with null description, omit the `": {description}"` portion. Unknown/fallback type → `'Nova notificação'`.
    Then write `describeNotification.test.ts` (vitest) covering every row in the behavior block.
    Note: this function consumes the SAME `{ amount, amountState }` contract that Plan 03's batch resolver produces — keep the param shape stable so the resolver output drops straight in.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/notifications/describeNotification.test.ts</automated>
  </verify>
  <done>`describeNotification` is a pure function matching every UI-SPEC copy template per type × amountState (known/loading/missing), uses `formatBalance` for the amount, falls back to "Seu parceiro(a)" and "Nova notificação"; the vitest suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/notifications* | Authenticated reads/writes; server is the source of truth (IDOR handled server-side in Phase 23) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Information Disclosure | inbox list / unread-count fetch | accept | Phase-23 backend already IDOR-scopes notifications to the authed user; this plan only consumes those endpoints with `credentials: 'include'`. No new server surface. |
| T-25-06 | Tampering (XSS via notification copy) | describeNotification output rendered in a row | mitigate | Output is plain text rendered through Mantine `<Text>` (React escapes by default — no `dangerouslySetInnerHTML`). partnerName/description are app-controlled strings; no raw HTML injection path. |
| T-25-07 | Denial of Service (poll storm) | useNotificationUnreadCount 60s poll | mitigate | `refetchIntervalInBackground: false` pauses polling on hidden tabs; single polling query only; 60s interval. |
</threat_model>

<verification>
- `cd frontend && npm run build` — tsc + vite build clean (no `any`, types resolve).
- `cd frontend && npm run lint` — eslint clean.
- `cd frontend && npx vitest run src/components/notifications/describeNotification.test.ts` — copy tests green.
- `QueryKeys` does NOT contain `TransactionById` or `ChargeById` (superseded design avoided).
</verification>

<success_criteria>
- Inbox list and unread count are fetchable through typed `{ query, invalidate }` hooks; inbox paginates newest-first via cursor; unread count polls 60s (focused only).
- describeNotification renders the exact pt-BR copy for every type × amountState.
- No superseded per-id artifacts introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/25-frontend-notification-inbox/25-02-SUMMARY.md`.
</output>
