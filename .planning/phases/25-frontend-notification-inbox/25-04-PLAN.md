---
phase: 25-frontend-notification-inbox
plan: 04
type: execute
wave: 3
depends_on: [25-02, 25-03]
files_modified:
  - frontend/src/components/notifications/NotificationRow.tsx
  - frontend/src/components/notifications/NotificationRow.module.css
  - frontend/src/components/notifications/NotificationInboxContent.tsx
  - frontend/src/components/notifications/NotificationInboxDrawer.tsx
  - frontend/src/pages/NotificationInboxPage.tsx
  - frontend/src/routes/_authenticated.notifications.tsx
autonomous: true
requirements: [INBOX-01, INBOX-02, INBOX-03, INBOX-04]
must_haves:
  truths:
    - "Opening the inbox lists notifications newest-first, each with a human-readable pt-BR description (INBOX-01)"
    - "Unread rows are visually distinguished from read (8px blue dot + blue.0/dark-5 tint); read rows are dimmed (INBOX-02)"
    - "Tapping a row navigates to /charges or /transactions AND marks it read; mobile drawer closes (INBOX-03, INBOX-04)"
    - "Mark-all-read clears all unread in one action; the button hides when no unread remain (INBOX-04)"
    - "Loading shows 3 skeleton rows; empty shows the BellOff empty state; cursor end shows 'Você está em dia'; 'Carregar mais' loads the next page"
  artifacts:
    - path: "frontend/src/components/notifications/NotificationRow.tsx"
      provides: "per-notification row: ThemeIcon tint, unread dot/tint, describeNotification copy, relative timestamp, chevron, tap handler"
      contains: "describeNotification"
    - path: "frontend/src/components/notifications/NotificationInboxContent.tsx"
      provides: "shared list renderer (list/loading/empty/error/load-more/mark-all) used by drawer + page"
      contains: "useNotificationInbox"
    - path: "frontend/src/components/notifications/NotificationInboxDrawer.tsx"
      provides: "mobile bottom-sheet drawer wrapping the shared content"
      contains: "useDrawerContext"
    - path: "frontend/src/pages/NotificationInboxPage.tsx"
      provides: "desktop full page wrapping the shared content"
      contains: "NotificationInboxContent"
    - path: "frontend/src/routes/_authenticated.notifications.tsx"
      provides: "thin /notifications route → NotificationInboxPage"
      contains: "createFileRoute"
  key_links:
    - from: "frontend/src/components/notifications/NotificationRow.tsx"
      to: "router.navigate({ to: deriveDeepLink(...) }) + useMarkNotificationRead"
      via: "tap handler: optimistic mark-read (background) + navigate to list page + (mobile) ctx.close()"
      pattern: "deriveDeepLink|router\\.navigate"
    - from: "frontend/src/components/notifications/NotificationInboxContent.tsx"
      to: "useNotificationInbox + useResolveNotificationAmounts + useNotificationUnreadCount + useMarkAllNotificationsRead"
      via: "flatten pages → resolve amounts → render rows; mark-all button when unread>0"
      pattern: "fetchNextPage|hasNextPage"
---

<objective>
Build the visible inbox surfaces: the shared `NotificationInboxContent` (list, loading/empty/error/load-more/mark-all), the `NotificationRow`, the mobile `NotificationInboxDrawer`, the desktop `NotificationInboxPage`, and the thin `/notifications` route. This is where INBOX-01..04 become observable to the user.

Purpose: Render the inbox per the LOCKED 25-UI-SPEC visual contract (Surfaces 2-5, the design/notif-inbox-components.jsx pixel reference, ThemeIcon tints, unread dot/tint, copy, states, testIds). Wire the Plan 02/03 hooks: cursor list, batch amount resolver, mark-read/mark-all mutations, partner-name from `useAccounts`. Tap navigates to the LIST page via `deriveDeepLink` (Phase 24 helper) — honestly the list, not an entity deep-link, consistent with Phase 24 and the UI-SPEC; it still satisfies INBOX-03 (related screen).

Output: 4 components + 1 page + 1 thin route, sharing one list renderer across mobile drawer and desktop page.
</objective>

<execution_context>
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/CLAUDE.md
@.planning/phases/25-frontend-notification-inbox/25-UI-SPEC.md
@design/notif-inbox-components.jsx
@.planning/phases/25-frontend-notification-inbox/25-02-SUMMARY.md
@.planning/phases/25-frontend-notification-inbox/25-03-SUMMARY.md

<interfaces>
<!-- From Plans 02/03 (shipped before this plan) + existing helpers. Use directly. -->

Hooks (Plan 02/03): useNotificationInbox() → { query, invalidate } (query is useInfiniteQuery; flatten via query.data.pages.flatMap(p => p.notifications)); useNotificationUnreadCount(select?) → { query, invalidate }; useMarkNotificationRead({ onSuccess? }) → { mutation }; useMarkAllNotificationsRead({ onSuccess? }) → { mutation }; useResolveNotificationAmounts(notifications) → Map<`${entity_type}:${entity_id}`, { amount: number|null; amountState: 'known'|'loading'|'missing' }>.

describeNotification(n, { amount, amountState, partnerName, description? }) → string (Plan 02).

Existing helpers (REUSE — do not recreate):
- deriveDeepLink({ type, entity_type }) from '@/utils/pushDeepLink' → '/charges' | '/transactions' | '/' (Phase 24, single source of truth for entity→route).
- router (global) from '@/router' — navigate from inside renderDrawer roots (which lack RouterProvider): `void router.navigate({ to })`.
- formatBalance(cents) from '@/utils/formatCents' (used inside describeNotification, not here directly).
- useAccounts(select) from '@/hooks/useAccounts' — derive partnerName from cached user_connection (from_user_name/to_user_name); NO extra API call.
- renderDrawer + useDrawerContext from '@/utils/renderDrawer' (drawer pattern; NO useDisclosure).
- NotificationsTestIds (Plan 02) — Drawer, DrawerClose, BtnMarkAllRead, BtnLoadMore, EmptyState, ErrorState, BtnRetry, Row(id), UnreadDot(id).

Route convention (frontend/CLAUDE.md §1): route file ONLY wires component. Pattern:
  export const Route = createFileRoute('/_authenticated/notifications')({ component: NotificationInboxPage })

ThemeIcon tints (UI-SPEC, binding): charge_received IconCreditCard color="blue"; charge_accepted IconCircleCheck color="teal"; split_created IconUsers color="violet"; split_updated IconRefresh color="orange". variant="light", radius="md", size={36}, icon 20px stroke 1.5.
Unread dot: Box 8×8 bg="blue.6" radius="50%"; read → transparent 8px spacer. Unread row tint via CSS module class: background light-dark(var(--mantine-color-blue-0), var(--mantine-color-dark-5)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationRow (icon, unread dot/tint, copy, timestamp, tap → mark-read + navigate)</name>
  <files>frontend/src/components/notifications/NotificationRow.tsx, frontend/src/components/notifications/NotificationRow.module.css</files>
  <action>
    Build `NotificationRow` per 25-UI-SPEC Surface 3 + the design/notif-inbox-components.jsx pixel reference (binding). Props: `{ notification: Notifications.Notification; resolved: { amount: number|null; amountState: AmountState }; partnerName: string|null; onAfterTap?: () => void }`.
    Layout (Mantine, mobile-first): `UnstyledButton` wrapper (min-height 48px), `Group align="center" wrap="nowrap" px="sm" py="xs" gap="sm"`:
      - unread dot: `Box` 8×8 `bg="blue.6"` `radius="50%"` when `!notification.read`, else a transparent 8px spacer Box (keep alignment). `data-testid={NotificationsTestIds.UnreadDot(notification.id)}` on the dot. `aria-hidden`.
      - type icon: `ThemeIcon variant="light" radius="md" size={36}` with the per-type color + icon (table above). On READ rows the icon may use the dimmed treatment per UI-SPEC.
      - `Stack gap={2} style={{ flex: 1, minWidth: 0 }}`: `Text size="sm" fw={500} lineClamp={2}` with `c="dimmed"` when read else default — content is `describeNotification(notification, { amount: resolved.amount, amountState: resolved.amountState, partnerName, description })`; then `Text size="xs" c="dimmed"` relative timestamp via `Intl.RelativeTimeFormat('pt-BR')` (fallback `Intl.DateTimeFormat('pt-BR',{dateStyle:'short'})` for >7 days).
      - trailing `IconChevronRight size={16}` dimmed.
    Unread row tint: CSS module class `.unread { background-color: light-dark(var(--mantine-color-blue-0), var(--mantine-color-dark-5)); }` applied when unread (NOT dark-6 in dark mode — must stay distinct, UI-SPEC Color section). Hairline separators handled by the parent list.
    `data-testid={NotificationsTestIds.Row(notification.id)}` on the UnstyledButton. `aria-label={\`${desc}${notification.read ? '' : ' — não lida'}\`}`.
    Tap handler (INBOX-03 + INBOX-04, UI-SPEC Surface 4 — navigate is NOT blocked by mark-read):
      - call the mark-read mutation (passed via prop or invoked through a hook instance owned by the row — prefer: the row receives a `markRead: (id) => void` callback from the parent so the parent owns the mutation hook and its caller-side `onSuccess` invalidation). Fire it (optimistic, background).
      - `void router.navigate({ to: deriveDeepLink({ type: notification.type, entity_type: notification.entity_type }) })` — list page. If deriveDeepLink returns `/` (unknown), still navigate is acceptable per UI-SPEC noop table (or guard to noop on unknown).
      - call `onAfterTap?.()` so the mobile drawer can `ctx.close()`.
    Use partnerName fallback handling inside describeNotification (Plan 02 already does "Seu parceiro(a)"). NO useEffect; NO fetch. Amount loading → describeNotification renders the no-amount template (stable row height — do NOT put a Skeleton inside the clamped text, UI-SPEC).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <done>NotificationRow renders the correct ThemeIcon tint per type, unread dot + `.unread` tint (blue.0/dark-5) for unread / dimmed text for read, the describeNotification copy, a relative timestamp, and a chevron; tap fires mark-read (background), navigates via deriveDeepLink to the list page, and calls onAfterTap; testIds Row(id)/UnreadDot(id) present; build + lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: NotificationInboxContent (shared list: states, load-more, mark-all)</name>
  <files>frontend/src/components/notifications/NotificationInboxContent.tsx</files>
  <action>
    Build the shared list renderer used by BOTH the drawer and the page (UI-SPEC Component Inventory: extract to avoid duplicating state logic). Props: `{ onRowTap?: () => void }` (the mobile drawer passes `() => ctx.close()`; the page passes nothing).
    Owns the hooks: `useNotificationInbox()`, `useNotificationUnreadCount((d) => d.count)`, `useMarkNotificationRead({ onSuccess: invalidateInbox })`, `useMarkAllNotificationsRead({ onSuccess: invalidateInbox })` where `invalidateInbox` is the inbox hook's `invalidate` (this is the caller-side inbox-list invalidation per D-25-2). Flatten notifications: `const notifications = query.data?.pages.flatMap(p => p.notifications) ?? []`. Resolve amounts: `const amounts = useResolveNotificationAmounts(notifications)`. Partner name: `useAccounts(select)` to derive partnerName from the cached connection (one value for the couple).
    Render (UI-SPEC Surface 2 list layout + states):
      - Header row: `Group justify="space-between"` with the title context (the page/drawer supply the heading; Content renders the mark-all action): `Button variant="subtle" size="sm"` "Marcar todas como lidas" shown ONLY when `unreadCount > 0`, `data-testid={NotificationsTestIds.BtnMarkAllRead}`, disabled/Loader while the mark-all mutation is pending, onClick fires `markAll.mutate()`; `aria-label="Marcar todas as notificações como lidas"`.
      - Initial loading (`query.isLoading`): 3 `Skeleton height={56} radius="md" mb="xs"` rows.
      - Error (`query.isError`): `section_notifications_error` container with "Não foi possível carregar as notificações." + `Button` "Tentar novamente" (`BtnRetry`, onClick `query.refetch()`).
      - Empty (`notifications.length === 0`): `EmptyState` container (`section_notifications_empty`) BellOff ThemeIcon + "Nenhuma notificação por enquanto" + body copy.
      - List: map notifications → `NotificationRow` passing `resolved={amounts.get(\`${n.entity_type}:${n.entity_id}\`) ?? { amount: null, amountState: 'loading' }}`, `partnerName`, a `markRead`/tap wiring (parent owns the mutation; pass `markRead.mutate` + `onAfterTap={onRowTap}` into the row). Hairline `Divider` indented per UI-SPEC between rows.
      - Footer: if `query.hasNextPage` → `Button variant="outline" size="sm" fullWidth` "Carregar mais" (`BtnLoadMore`), onClick `query.fetchNextPage()`, Loader/disabled while `query.isFetchingNextPage`; else `Text size="xs" c="dimmed" ta="center"` "Você está em dia". On fetchNextPage error show an inline `Text size="xs" c="red"` adjacent (not a toast) per UI-SPEC.
      - Mark-read / mark-all error toasts: the mutation hooks return `{ mutation }` only; wire the toast at THIS caller via `mutation` `onError` is in-hook (rollback) — show the toast here using the mutation's `isError`/an `onError` passed through, OR use `@mantine/notifications` `notifications.show` in an onError callback supplied to the mutate call (UI-SPEC copy: title "Erro", body "Não foi possível marcar como lida. Tente novamente." / "...todas como lidas..."). Do NOT put the toast inside the hook.
    NO useEffect. All derivation via select / render.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <done>NotificationInboxContent flattens the cursor pages, resolves amounts via the batch resolver, renders rows with the loading/empty/error states, a "Carregar mais"/"Você está em dia" footer driven by hasNextPage, and a "Marcar todas como lidas" button shown only when unread>0; it owns the mutation hooks and passes the inbox `invalidate` as the caller-side `onSuccess`; mark-read/mark-all error toasts fire at the caller; build + lint pass.</done>
</task>

<task type="auto">
  <name>Task 3: NotificationInboxDrawer (mobile) + NotificationInboxPage (desktop) + route</name>
  <files>frontend/src/components/notifications/NotificationInboxDrawer.tsx, frontend/src/pages/NotificationInboxPage.tsx, frontend/src/routes/_authenticated.notifications.tsx</files>
  <action>
    1. `NotificationInboxDrawer.tsx` (mobile bottom sheet, UI-SPEC Surface 2 mobile): use `useDrawerContext<void>()` (`{ opened, close, reject }`). Render a `ResponsiveDrawer`/Mantine `Drawer` `position="bottom"`, `styles={{ content: { height: 'auto', maxHeight: '92dvh' } }}`, `title="Notificações"` (`fw={700}` ~18px), close button `aria-label="Fechar"` `data-testid={NotificationsTestIds.DrawerClose}`, drawer `data-testid={NotificationsTestIds.Drawer}`, `onClose={reject}`. Body = `<NotificationInboxContent onRowTap={() => close()} />` so a row tap closes the sheet after firing mark-read + navigate. Drag handle / safe-area bottom padding per the design reference.
    2. `NotificationInboxPage.tsx` (desktop full page, UI-SPEC Surface 2 desktop): render inside the standard page container; `<Title order={2} size="lg">Notificações</Title>` heading, then `<NotificationInboxContent />` (no onRowTap — page does not close). No `<Paper>` wrapper (bare page background per UI-SPEC).
    3. `src/routes/_authenticated.notifications.tsx` — THIN route only (CLAUDE.md §1): `import { createFileRoute } from '@tanstack/react-router'; import { NotificationInboxPage } from '@/pages/NotificationInboxPage'; export const Route = createFileRoute('/_authenticated/notifications')({ component: NotificationInboxPage })`. No logic in the route.
    After adding the route file, the TanStack Router file-based route tree must pick it up — `npm run build` runs the route generation; confirm `/notifications` compiles into the tree.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <done>The mobile drawer wraps the shared content with `position="bottom"` maxHeight 92dvh, title "Notificações", close testid, and closes on row tap; the desktop page wraps the shared content with the heading; the thin `/notifications` route renders `NotificationInboxPage` and compiles into the router tree; build + lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → router.navigate(deriveDeepLink(...)) | Client-side navigation target derived from notification type/entity_type |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-11 | Tampering (open redirect on tap-navigation) | NotificationRow tap → router.navigate | mitigate | `deriveDeepLink` returns only fixed internal paths (`/charges`, `/transactions`, `/`); no user-supplied URL is ever passed to `router.navigate`. Reuses the Phase 24 helper that already guards internal-only paths. |
| T-25-12 | Tampering (XSS in row copy) | describeNotification string in `<Text>` | mitigate | Rendered as React text (auto-escaped); no `dangerouslySetInnerHTML`. |
| T-25-13 | Information Disclosure | inbox list rendered client-side | accept | Data is already IDOR-scoped server-side (Phase 23); the UI only renders what the authed user's queries return. |
</threat_model>

<verification>
- `cd frontend && npm run build` — tsc + vite + route-tree generation clean.
- `cd frontend && npm run lint` — clean, no `any`, no inline `useEffect`, no `useDisclosure` at the drawer call site.
- `cd frontend && npx vitest run` — existing + Plan 02/03 unit suites still green (no regressions from new components).
- grep confirms the row tap uses `deriveDeepLink` + the global `router` (no hardcoded entity deep-link, no useNavigate inside the drawer root).
</verification>

<success_criteria>
- INBOX-01: opening the inbox lists notifications newest-first with human-readable pt-BR descriptions.
- INBOX-02: unread rows visually distinct (dot + tint) from read (dimmed); mark-all button visible only when unread exist.
- INBOX-03: tapping a row navigates to the related list page and marks it read.
- INBOX-04: mark-read on tap + mark-all-read in one action; states (loading/empty/error/end/load-more) render per the UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/25-frontend-notification-inbox/25-04-SUMMARY.md`.
</output>
