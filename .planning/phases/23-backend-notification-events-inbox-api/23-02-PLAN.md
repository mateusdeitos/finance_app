---
phase: 23-backend-notification-events-inbox-api
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - backend/internal/service/interfaces.go
  - backend/internal/service/notification_service.go
  - backend/internal/handler/notification_handler.go
  - backend/cmd/server/main.go
  - backend/docs/
  - backend/mocks/
autonomous: true
requirements: [NOTIF-05, NOTIF-06]
must_haves:
  truths:
    - "NotificationService.Dispatch persists one inbox row per event (always, even with no subscription), groups by (recipient, type), and sends one coalesced push per group via an injectable PushSender, pruning subscriptions on 404/410 (D-08 coalescing rule, D-09 generalizes to all event sources)"
    - "Push copy is pt-BR with BRL formatting per the D-07 templates, including the bulk summary"
    - "Dispatch recovers from panics so a push failure never crashes the server"
    - "GET /api/notifications returns the authenticated user's notifications newest-first with cursor; GET /api/notifications/unread-count returns the exact count; POST /api/notifications/:id/read and POST /api/notifications/read-all mark read; all IDOR-scoped"
    - "Routes are registered with /unread-count and /read-all BEFORE /:id/read; services.Notification wired in main.go after services.PushSubscription"
  artifacts:
    - path: "backend/internal/service/notification_service.go"
      provides: "notificationService (Dispatch + List/UnreadCount/MarkRead/MarkAllRead), PushSender interface + webPushSender, formatBRL, buildPayload, NewNotificationService"
      contains: "func (s *notificationService) Dispatch"
    - path: "backend/internal/handler/notification_handler.go"
      provides: "NotificationHandler with List/UnreadCount/MarkRead/MarkAllRead + swagger annotations"
      contains: "func (h *NotificationHandler) List"
    - path: "backend/internal/service/interfaces.go"
      provides: "NotificationService interface + Notification field on Services struct"
      contains: "Notification     NotificationService"
    - path: "backend/cmd/server/main.go"
      provides: "DI wiring + 4 routes"
      contains: "services.Notification = service.NewNotificationService"
  key_links:
    - from: "NotificationHandler"
      to: "NotificationService"
      via: "appcontext.GetUserIDFromContext to service method with authenticated userID"
      pattern: "GetUserIDFromContext"
    - from: "notificationService.Dispatch"
      to: "PushSubscriptionRepository.DeleteByEndpointAdmin"
      via: "404/410 status check on sender response"
      pattern: "DeleteByEndpointAdmin"
    - from: "main.go"
      to: "notifHandler routes"
      via: "api.Group /notifications with fixed routes before /:id/read"
      pattern: "notifications.POST\\(\"/read-all\""
---

<objective>
Build the NotificationService (post-commit dispatcher + inbox query methods), the NotificationHandler (4 inbox endpoints), wire both into main.go, and regenerate swagger + mocks. Dispatch persists inbox rows, coalesces pushes by (recipient, type) per D-08, sends pt-BR pushes via an injectable `PushSender` (D-05/D-06/D-07), and prunes stale subscriptions on 404/410.

Purpose: Delivery + API layer. NOTIF-05 (persist always) and NOTIF-06 (best-effort, panic-safe) are realized here; the four inbox endpoints (success criterion 6) are exposed here. Event-source hooks (NOTIF-01..04) come in Plan 03 and call `services.Notification.Dispatch`.
Output: NotificationService + handler + DI + swagger. Plan 03 builds on `services.Notification`.
</objective>

<execution_context>
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-backend-notification-events-inbox-api/23-CONTEXT.md
@.planning/phases/23-backend-notification-events-inbox-api/23-RESEARCH.md
@.planning/phases/23-backend-notification-events-inbox-api/23-PATTERNS.md
@backend/CLAUDE.md
@backend/internal/service/push_subscription_service.go
@backend/internal/handler/push_subscription_handler.go

<interfaces>
<!-- Contracts from Plan 01 (already landed) — no exploration needed. -->

From backend/internal/domain/push_subscription.go (Plan 01):
  NotificationEvent { RecipientUserID, ActorUserID int; Type, EntityType string; EntityID int; Amount int64; Description string }
  NotificationFilter { UserID int; Cursor string; Limit int }
  NotificationListResult { Items []*Notification `json:"notifications"`; NextCursor string `json:"next_cursor"`; HasMore bool `json:"has_more"` }
  NotificationUnreadCountResponse { Count int64 `json:"count"` }
  const NotificationTypeChargeReceived/ChargeAccepted/SplitCreated/SplitUpdated

From backend/internal/repository (Plan 01):
  NotificationRepository: Create / List(filter) / UnreadCount(userID) / MarkRead(userID, id) / MarkAllRead(userID)
  PushSubscriptionRepository: ListByUserID(ctx, userID) ([]*domain.PushSubscription, error); DeleteByEndpointAdmin(ctx, endpoint) error
  Repositories struct has .Notification, .PushSubscription, .User

From backend/internal/service/push_subscription_service.go (constructor/field analog):
  func NewPushSubscriptionService(repos *repository.Repositories, cfg *config.Config) PushSubscriptionService

From backend/internal/service/interfaces.go:
  Services struct fields end with: Charge, Onboarding, PushSubscription  (ADD Notification after PushSubscription)

cfg.VAPID is config.VAPIDConfig with Subject, PublicKey, PrivateKey (env-set in Phase 22; may be empty in tests → push best-effort-fails and is logged).

Handler layer:
  appcontext.GetUserIDFromContext(c.Request().Context()) int   — auth-derived userID, never from body/query
  HandleServiceError(err) error                                 — maps *ServiceError to echo HTTP (internal/handler/errors.go)

webpush-go v1.4.0 (in go.mod):
  webpush.SendNotification(payload []byte, sub *webpush.Subscription, opts *webpush.Options) (*http.Response, error)
  webpush.Subscription{ Endpoint string; Keys webpush.Keys{ Auth, P256dh string } }
  webpush.Options{ Subscriber, VAPIDPublicKey, VAPIDPrivateKey string; TTL int }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationService interface + Services field; implement notification_service.go</name>
  <read_first>
    - backend/internal/service/interfaces.go (PushSubscriptionService ~line 91; Services struct ~line 98)
    - backend/internal/service/push_subscription_service.go (constructor + field style; pkgErrors usage)
    - .planning/phases/23-backend-notification-events-inbox-api/23-RESEARCH.md (Dispatch skeleton; PushSender testing approach; Pattern 1 panic safety; Pattern 9 actor name)
    - .planning/phases/23-backend-notification-events-inbox-api/23-PATTERNS.md (notification_service.go section: full bodies for Dispatch, formatBRL, buildPayload, inbox methods, PushSender)
    - backend/internal/config (VAPIDConfig field names)
    - backend/internal/repository/interfaces.go (UserRepository method to fetch a user by id for actor name lookup)
  </read_first>
  <action>
In `backend/internal/service/interfaces.go`: APPEND after `PushSubscriptionService`:
```go
type NotificationService interface {
    // Dispatch persists inbox rows and sends push notifications best-effort.
    // Always called in a goroutine with context.Background() — never the request ctx.
    Dispatch(ctx context.Context, events []domain.NotificationEvent)
    List(ctx context.Context, userID int, filter domain.NotificationFilter) (*domain.NotificationListResult, error)
    UnreadCount(ctx context.Context, userID int) (int64, error)
    MarkRead(ctx context.Context, userID, notificationID int) error
    MarkAllRead(ctx context.Context, userID int) error
}
```
And ADD `Notification NotificationService` to the `Services` struct after `PushSubscription`.

Create `backend/internal/service/notification_service.go` (`package service`). Imports: `context`, `encoding/json`, `fmt`, `log`, `net/http`, `webpush "github.com/SherClockHolmes/webpush-go"`, config, domain, repository, pkgErrors. Implement EXACTLY as PATTERNS.md specifies:

- struct `notificationService{ notifRepo repository.NotificationRepository; pushSubRepo repository.PushSubscriptionRepository; userRepo repository.UserRepository; vapid config.VAPIDConfig; sender PushSender }`. Confirm the Repositories accessor for users is `.User` and the by-id method name/signature (likely `GetByID(ctx, id) (*domain.User, error)`) — use the real one and the real `domain.User` name field (likely `Name`).
- `NewNotificationService(repos *repository.Repositories, cfg *config.Config) NotificationService` → `&notificationService{notifRepo: repos.Notification, pushSubRepo: repos.PushSubscription, userRepo: repos.User, vapid: cfg.VAPID, sender: &webPushSender{}}`.
- `PushSender` interface + `webPushSender` wrapping `webpush.SendNotification` (one-method interface → hand-written test double in Plan 03, no mockery):
```go
type PushSender interface {
    Send(payload []byte, sub *webpush.Subscription, opts *webpush.Options) (*http.Response, error)
}
type webPushSender struct{}
func (w *webPushSender) Send(p []byte, sub *webpush.Subscription, opts *webpush.Options) (*http.Response, error) {
    return webpush.SendNotification(p, sub, opts)
}
```
- `Dispatch(ctx, events)` per RESEARCH "Dispatch skeleton":
  1. `defer func(){ if r := recover(); r != nil { log.Printf("[notification] dispatch panic: %v", r) } }()`
  2. early return if `len(events) == 0`.
  3. persist EVERY event via `notifRepo.Create` (always — even with no subscription: NOTIF-05 + "inbox != delivery"). Log + continue on per-row error.
  4. resolve actor name ONCE: `userRepo.GetByID(ctx, events[0].ActorUserID)`; fallback `"Parceiro"` on error/nil (Open Question 2).
  5. group events into `map[struct{Recipient int; Type string}][]domain.NotificationEvent` (D-08 coalescing).
  6. per group: `subs, err := pushSubRepo.ListByUserID(ctx, recipient)`; if err or `len(subs)==0` continue (inbox row already saved). Build payload via `buildPayload(actorName, group)`, `json.Marshal`, then per sub call `s.sender.Send(rawPayload, &webpush.Subscription{Endpoint: sub.Endpoint, Keys: webpush.Keys{Auth: sub.Auth, P256dh: sub.P256dh}}, &webpush.Options{Subscriber: s.vapid.Subject, VAPIDPublicKey: s.vapid.PublicKey, VAPIDPrivateKey: s.vapid.PrivateKey, TTL: 30})`. On send error: `log.Printf` (warn) + continue. On non-nil resp: `defer resp.Body.Close()`; if `resp.StatusCode == http.StatusNotFound || resp.StatusCode == http.StatusGone` call `s.pushSubRepo.DeleteByEndpointAdmin(ctx, sub.Endpoint)` (log on prune error).
- `formatBRL(cents int64) string` → pt-BR `R$ 1.234,56`, sign-guarded, period thousands separator, comma decimals (copy PATTERNS.md verbatim).
- `pushPayload`/`pushPayloadData` structs + `buildPayload(actorName string, evGroup []domain.NotificationEvent) pushPayload` with D-07 templates VERBATIM:
  - bulk split_created (`len(evGroup) > 1 && first.Type == split_created`): `fmt.Sprintf("%s adicionou %d transações divididas", actorName, len(evGroup))`
  - charge_received: `fmt.Sprintf("%s te cobrou %s: %s", actorName, formatBRL(first.Amount), first.Description)`
  - charge_accepted: `fmt.Sprintf("%s aceitou sua cobrança de %s", actorName, formatBRL(first.Amount))`
  - split_created (single): `fmt.Sprintf("%s adicionou uma transação dividida de %s", actorName, formatBRL(first.Amount))`
  - split_updated: `fmt.Sprintf("%s atualizou uma transação dividida (%s)", actorName, formatBRL(first.Amount))`
  - default: `fmt.Sprintf("%s enviou uma notificação", actorName)`
  Title `"Finance App"`; `Data{Type, EntityType, EntityID}` from `evGroup[0]`.
- Inbox methods: `List` sets `filter.UserID = userID` (IDOR override) then `notifRepo.List`, wrapping errors with `pkgErrors.Internal("failed to list notifications", err)`. `UnreadCount` wraps with `pkgErrors.Internal("failed to count unread notifications", err)`. `MarkRead`/`MarkAllRead` delegate straight through (userID first).
  </action>
  <verify>
    <automated>cd backend && go build ./internal/service/... && grep -q "func (s \*notificationService) Dispatch" internal/service/notification_service.go && grep -q "te cobrou" internal/service/notification_service.go && grep -q "transações divididas" internal/service/notification_service.go && grep -q "recover()" internal/service/notification_service.go && grep -q "DeleteByEndpointAdmin" internal/service/notification_service.go && grep -q "Notification" internal/service/interfaces.go && echo PASS</automated>
  </verify>
  <done>NotificationService interface + Services.Notification field added; notification_service.go compiles with Dispatch (panic-safe, persist-always, D-08 grouping, 404/410 prune), PushSender, formatBRL, buildPayload (all 5 pt-BR templates), inbox methods. `go build ./internal/service/...` green.</done>
</task>

<task type="auto">
  <name>Task 2: notification_handler.go (4 endpoints + swagger annotations)</name>
  <read_first>
    - backend/internal/handler/push_subscription_handler.go (struct/constructor, swagger style, GetUserIDFromContext, HandleServiceError, NoContent)
    - backend/internal/handler/charge_handler.go (path-param strconv.Atoi + 400 pattern)
    - backend/internal/handler/errors.go (HandleServiceError)
    - .planning/phases/23-backend-notification-events-inbox-api/23-PATTERNS.md (notification_handler.go section: full method bodies + swagger blocks)
    - .planning/phases/23-backend-notification-events-inbox-api/23-RESEARCH.md (swagger annotations block)
  </read_first>
  <action>
Create `backend/internal/handler/notification_handler.go` (`package handler`). Imports: `net/http`, `strconv`, domain, service, appcontext, echo. Struct `NotificationHandler{ notifService service.NotificationService }`; `NewNotificationHandler(services *service.Services) *NotificationHandler { return &NotificationHandler{notifService: services.Notification} }`.

Methods (each starts `userID := appcontext.GetUserIDFromContext(c.Request().Context())`; pass `c.Request().Context()` to the service):

- `List`: `cursor := c.QueryParam("cursor")`; parse `limit` from `c.QueryParam("limit")` with `strconv.Atoi` (default 20 if empty/invalid). Build `domain.NotificationFilter{Cursor: cursor, Limit: limit}` (UserID set by service). `result, err := h.notifService.List(...)`; err → `return HandleServiceError(err)`; success → `return c.JSON(http.StatusOK, result)`.
- `UnreadCount`: `count, err := h.notifService.UnreadCount(...)`; success → `return c.JSON(http.StatusOK, domain.NotificationUnreadCountResponse{Count: count})`.
- `MarkRead`: `id, err := strconv.Atoi(c.Param("id"))`; if `err != nil || id <= 0` return `echo.NewHTTPError(http.StatusBadRequest, "invalid notification id")`. `h.notifService.MarkRead(ctx, userID, id)`; err → `HandleServiceError`; success → `c.NoContent(http.StatusNoContent)`.
- `MarkAllRead`: `h.notifService.MarkAllRead(ctx, userID)`; err → `HandleServiceError`; success → `c.NoContent(http.StatusNoContent)`.

Add the swagger godoc blocks VERBATIM from RESEARCH.md "Swagger annotations" (Tags `notifications`; Security CookieAuth + BearerAuth; routes `/api/notifications [get]`, `/api/notifications/unread-count [get]`, `/api/notifications/{id}/read [post]`, `/api/notifications/read-all [post]`). Match the existing `// @Failure 401 {object} middleware.ErrorResponse` reference style used by push_subscription_handler.go.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/handler/... && grep -q "func (h \*NotificationHandler) List" internal/handler/notification_handler.go && grep -q "func (h \*NotificationHandler) UnreadCount" internal/handler/notification_handler.go && grep -q "func (h \*NotificationHandler) MarkRead" internal/handler/notification_handler.go && grep -q "func (h \*NotificationHandler) MarkAllRead" internal/handler/notification_handler.go && grep -q "GetUserIDFromContext" internal/handler/notification_handler.go && echo PASS</automated>
  </verify>
  <done>notification_handler.go compiles with 4 IDOR-scoped handlers + swagger annotations; userID always from auth context. `go build ./internal/handler/...` green.</done>
</task>

<task type="auto">
  <name>Task 3: main.go DI wiring + routes; regenerate swagger + mocks; full build</name>
  <read_first>
    - backend/cmd/server/main.go (service instantiation ~line 102 services.PushSubscription; handler construction ~line 113; push-subscriptions route group ~line 206)
    - .planning/phases/23-backend-notification-events-inbox-api/23-PATTERNS.md (cmd/server/main.go section + route ordering note)
    - backend/CLAUDE.md (generate-docs to swag mapping; generate-mocks to mockery mapping)
  </read_first>
  <action>
Environment: `just` not installed. Run `export PATH="$PATH:$(go env GOPATH)/bin"` first (GOPATH /root/go; goose/swag/mockery present).

In `backend/cmd/server/main.go`:
1. After `services.PushSubscription = service.NewPushSubscriptionService(repos, cfg)` add:
   `services.Notification = service.NewNotificationService(repos, cfg)`
   (Safe: chargeService/transactionService hold a `*Services` pointer; the field is assigned during single-threaded startup before any request fires Dispatch — RESEARCH Open Question 1.)
2. Near the other handler construction (~line 113) add:
   `notifHandler := handler.NewNotificationHandler(services)`
3. Register routes under the authed `api` group (mirror the push-subscriptions block placement). FIXED ROUTES BEFORE THE `:id` ROUTE:
   ```go
   notifications := api.Group("/notifications")
   notifications.GET("", notifHandler.List)
   notifications.GET("/unread-count", notifHandler.UnreadCount)
   notifications.POST("/read-all", notifHandler.MarkAllRead)
   notifications.POST("/:id/read", notifHandler.MarkRead)
   ```
4. Regenerate swagger (handler annotations changed): from `backend/`, run the swag invocation that CLAUDE.md's `just generate-docs` maps to — read the justfile recipe (or existing docs layout) for the exact `-g` entrypoint and `-o` output dir, e.g. `"$(go env GOPATH)/bin/swag" init -g cmd/server/main.go -o docs`. Confirm `backend/docs/` now references `/api/notifications`.
5. Regenerate mocks (adds MockNotificationService for Plan 03 unit coverage) using the same mockery invocation as Plan 01 Task 3.
6. Full build: `cd backend && go build ./...`.
  </action>
  <verify>
    <automated>cd backend && grep -q "services.Notification = service.NewNotificationService" cmd/server/main.go && grep -q "notifHandler := handler.NewNotificationHandler" cmd/server/main.go && grep -q 'notifications.POST("/read-all"' cmd/server/main.go && awk '/notifications.POST\("\/read-all"/{r=NR} /notifications.POST\("\/:id\/read"/{i=NR} END{exit !(r>0 && i>0 && r<i)}' cmd/server/main.go && grep -rq "/api/notifications" docs/ && go build ./... && echo PASS</automated>
  </verify>
  <done>main.go wires services.Notification (after PushSubscription) + notifHandler + 4 routes (fixed before :id); swagger regenerated referencing /api/notifications; mocks regenerated; `go build ./...` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/notifications* | Untrusted HTTP input (cursor, limit, :id) crosses into the inbox API |
| dispatch goroutine → push service | VAPID-signed payloads sent to per-browser endpoints |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Elevation/Tampering | POST /notifications/:id/read | mitigate | userID from `GetUserIDFromContext` (auth), never from body; service/repo enforce `id = ? AND user_id = ?`; cross-user mark-read → 404 |
| T-23-06 | Info Disclosure | GET /notifications, /unread-count | mitigate | Service overrides `filter.UserID = userID`; UnreadCount/List repo queries are user_id-scoped — a caller can never read another user's inbox or count |
| T-23-07 | Tampering/DoS | List cursor + limit params | mitigate | limit clamped (default 20, capped) in repo; malformed cursor handled in repo (400 or first-page, no 500); `:id` non-int → 400 |
| T-23-08 | Info Disclosure | Push payload routed to wrong subscription | mitigate | Payload built per (recipient,type) group; subscriptions fetched by `ListByUserID(recipient)` — payload only ever sent to that recipient's own endpoints |
| T-23-09 | Info Disclosure | VAPID private key | mitigate | `cfg.VAPID.PrivateKey` used only in `webpush.Options`; never logged, never in any API response |
| T-23-10 | Repudiation/Availability | Push send failure or panic in goroutine | mitigate | `defer recover()` in Dispatch; send errors logged + `continue`; failure never blocks/rolls back the originating request (NOTIF-06) |
| T-23-11 | Elevation | DeleteByEndpointAdmin reachable from HTTP | accept | Admin prune is only invoked inside Dispatch on 404/410; no HTTP route maps to it — verified by route list above containing only the 4 inbox endpoints |
</threat_model>

<verification>
- `cd backend && go build ./...` green (service + handler + main + regenerated mocks).
- `backend/docs/` references `/api/notifications` after swagger regen.
- Route registration order: `/read-all` before `/:id/read` (awk gate).
- Dispatch is panic-safe (recover) and persists rows before push (NOTIF-05/06).
- Integration behavior (push prune, IDOR, persist-without-subscription) is exercised by Plan 03 tests; runnable here via `go build` + (Plan 03) `go vet -tags=integration`.
</verification>

<success_criteria>
- NotificationService implements Dispatch + 4 inbox methods; Services struct exposes Notification.
- 4 endpoints registered behind auth, IDOR-scoped, fixed routes before :id.
- pt-BR D-07 copy + BRL formatting + D-08 coalescing in buildPayload/Dispatch.
- 404/410 prune via DeleteByEndpointAdmin; panic recovery present.
- Swagger + mocks regenerated; `go build ./...` green.
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-notification-events-inbox-api/23-02-SUMMARY.md`.
</output>
