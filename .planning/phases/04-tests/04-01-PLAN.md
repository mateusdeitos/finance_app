---
phase: 04-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/service/transaction_create_test.go
autonomous: true
requirements: [TST-01, TST-02, TST-03, TST-04, TST-05, TST-06, TST-07]

must_haves:
  truths:
    - "Integration test proves current=1,total=5 creates exactly 5 installments numbered 1-5"
    - "Integration test proves current=3,total=10 creates exactly 8 installments numbered 3-10 with TransactionRecurrence.Installments=10"
    - "Integration test proves each installment date equals base_date + (N - current_installment) * interval"
    - "Unit test proves CurrentInstallment=0 is rejected with ErrRecurrenceCurrentInstallmentMustBeAtLeastOne"
    - "Unit test proves CurrentInstallment > TotalInstallments is rejected"
    - "Unit test proves TotalInstallments > 1000 is rejected"
  artifacts:
    - path: "backend/internal/service/transaction_create_test.go"
      provides: "Integration tests (TST-01,02,03) and validation unit tests (TST-04,05,06)"
      contains: "TestCreateRecurringExpenseFrom1of5"
  key_links:
    - from: "backend/internal/service/transaction_create_test.go"
      to: "backend/internal/service/transaction_create.go"
      via: "suite.Services.Transaction.Create"
      pattern: "suite\\.Services\\.Transaction\\.Create"
---

<objective>
Add backend integration tests for the new recurrence create behavior (current_installment + total_installments) and validation unit tests for the three rejection cases.

Purpose: Prove the Phase 1+2 production code correctly creates installment series from the new input model and rejects invalid inputs with distinct error codes.
Output: 5 new test methods in `transaction_create_test.go` — 2 integration (with DB), 3 validation (mocked).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-tests/04-CONTEXT.md
@.planning/phases/04-tests/04-RESEARCH.md

@backend/internal/service/transaction_create_test.go
@backend/internal/service/transaction_create.go
@backend/internal/service/test_setup.go
@backend/internal/service/test_setup_with_db.go
@backend/internal/service/transaction_delete_test.go
@backend/pkg/errors/errors.go
@backend/internal/domain/transaction.go
@backend/internal/repository/interfaces.go

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/pkg/errors/errors.go:
```go
// Error assertion function — use this for ServiceErrors (validation returns ServiceErrors, not single *ServiceError)
func Is(errs error, target ServiceError) bool  // checks by tag match

// Error constants for validation:
ErrRecurrenceCurrentInstallmentMustBeAtLeastOne             = NewWithTag(...)
ErrRecurrenceTotalInstallmentsMustBeGreaterOrEqualToCurrent = NewWithTag(...)
ErrRecurrenceTotalInstallmentsMustBeLessThanOrEqualTo       = func(maxValue int) *ServiceError { ... }

type ServiceErrors []*ServiceError  // returned by Create when validation fails
```

From backend/internal/domain/transaction.go:
```go
type RecurrenceSettings struct {
    Type               RecurrenceType
    CurrentInstallment int
    TotalInstallments  int
}

type TransactionRecurrenceFilter struct {
    IDs            []int
    UserID         int
    TransactionIDs []int
}
```

From backend/internal/repository/interfaces.go:
```go
// Use suite.Repos.TransactionRecurrence.Search for TST-02 Installments assertion
type TransactionRecurrenceRepository interface {
    Search(ctx context.Context, filter domain.TransactionRecurrenceFilter) ([]*domain.TransactionRecurrence, error)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests for recurring expense create (TST-01, TST-02, TST-03)</name>
  <read_first>
    - backend/internal/service/transaction_create_test.go (full file — especially TestRecurringCreateTransfer at ~line 249 for the pattern)
    - backend/internal/service/transaction_create.go (create loop and date offset formula at ~line 189)
    - backend/internal/domain/transaction.go (RecurrenceSettings, TransactionRecurrence, TransactionRecurrenceFilter)
    - backend/internal/repository/interfaces.go (TransactionRecurrenceRepository.Search)
  </read_first>
  <files>backend/internal/service/transaction_create_test.go</files>
  <action>
Add two new methods to `TransactionCreateWithDBTestSuite` in `transaction_create_test.go`. Place them after the existing `TestRecurringCreateTransfer` method (after ~line 339) and before `TestTransferBetweenDifferentUsers`.

**Method 1: `TestCreateRecurringExpenseFrom1of5`** (covers TST-01, TST-03 partial)
- Create user, account, category using suite helpers (follow TestRecurringCreateTransfer pattern exactly)
- Build `domain.TransactionCreateRequest` with `TransactionType: domain.TransactionTypeExpense`, `Amount: 1000`, `Date: now()`, `Description: "TST-01 recurring expense"`, `RecurrenceSettings: &domain.RecurrenceSettings{Type: domain.RecurrenceTypeMonthly, CurrentInstallment: 1, TotalInstallments: 5}`
- Call `suite.Services.Transaction.Create(ctx, user.ID, &req)`
- Search with `domain.TransactionFilter{UserID: &user.ID, SortBy: &domain.SortBy{Field: "installment_number", Order: domain.SortOrderAsc}}`
- Assert `Len(transactions, 5)` — TST-01
- Loop and assert `lo.FromPtr(transactions[i].InstallmentNumber) == i+1` for i=0..4 — TST-01
- Assert dates (TST-03): `transactions[0].Date == baseDate`, `transactions[4].Date == baseDate.AddDate(0, 4, 0)` — verify each `transactions[i].Date == baseDate.AddDate(0, i, 0)`
- Assert each transaction has `TransactionRecurrenceID != nil`

**Method 2: `TestCreateRecurringExpenseFrom3of10`** (covers TST-02, TST-03 partial)
- Same setup pattern (fresh user, account, category)
- `RecurrenceSettings: &domain.RecurrenceSettings{Type: domain.RecurrenceTypeMonthly, CurrentInstallment: 3, TotalInstallments: 10}`
- Call Create, then Search with same filter pattern
- Assert `Len(transactions, 8)` — TST-02
- Assert `lo.FromPtr(transactions[0].InstallmentNumber) == 3` and `lo.FromPtr(transactions[7].InstallmentNumber) == 10` — TST-02
- Assert dates (TST-03): for each i in 0..7, `transactions[i].Date == baseDate.AddDate(0, i, 0)` (installment 3 gets base, installment 4 gets base+1mo, ..., installment 10 gets base+7mo)
- **Assert TransactionRecurrence.Installments = 10** (TST-02): Use `suite.Repos.TransactionRecurrence.Search(ctx, domain.TransactionRecurrenceFilter{IDs: []int{lo.FromPtr(transactions[0].TransactionRecurrenceID)}})`. Assert the returned recurrence has `Installments == 10`. This avoids relying on Search preloading the recurrence relation (per Pitfall 2 in RESEARCH.md).

Follow the exact assertion style from `TestRecurringCreateTransfer` — use `suite.Assert().Equal(...)` with descriptive format strings.
  </action>
  <verify>
    <automated>cd /workspace/backend && go test ./internal/service/ -run "TestTransactionCreateWithDB/TestCreateRecurringExpense" -tags=integration -v -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - grep "TestCreateRecurringExpenseFrom1of5" backend/internal/service/transaction_create_test.go returns a match
    - grep "TestCreateRecurringExpenseFrom3of10" backend/internal/service/transaction_create_test.go returns a match
    - grep "Len(transactions, 5)" backend/internal/service/transaction_create_test.go returns a match (TST-01)
    - grep "Len(transactions, 8)" backend/internal/service/transaction_create_test.go returns a match (TST-02)
    - grep "TransactionRecurrenceFilter" backend/internal/service/transaction_create_test.go returns a match (TST-02 recurrence assertion)
    - grep "AddDate" backend/internal/service/transaction_create_test.go returns matches for date offset assertions (TST-03)
    - go test exits 0 for the integration tests
  </acceptance_criteria>
  <done>Two integration test methods pass: TestCreateRecurringExpenseFrom1of5 asserts 5 installments numbered 1-5 with correct dates; TestCreateRecurringExpenseFrom3of10 asserts 8 installments numbered 3-10 with correct dates and TransactionRecurrence.Installments==10.</done>
</task>

<task type="auto">
  <name>Task 2: Validation unit tests for recurrence rejection cases (TST-04, TST-05, TST-06)</name>
  <read_first>
    - backend/internal/service/transaction_create_test.go (check what suites/runner functions exist)
    - backend/internal/service/transaction_delete_test.go (ServiceTestSuite usage pattern — lines 17-28)
    - backend/internal/service/test_setup.go (ServiceTestSuite struct, SetupTest, defaultMockTx)
    - backend/pkg/errors/errors.go (ServiceErrors type, Is() function, error constants)
    - backend/internal/service/transaction_create.go (validateRecurrenceSettings at ~line 129)
  </read_first>
  <files>backend/internal/service/transaction_create_test.go</files>
  <action>
Add a new test suite and three test methods at the bottom of `transaction_create_test.go`, before the `now()` helper function.

**New suite definition:**
```go
type TransactionCreateValidationTestSuite struct {
    ServiceTestSuite
}
```

**Method 1: `TestValidationRejectsMissingCurrentInstallment`** (TST-04)
- Build `domain.TransactionCreateRequest` with valid fields (`TransactionType: domain.TransactionTypeExpense`, `AccountID: 1`, `CategoryID: 1`, `Amount: 100`, `Date: time.Now()`, `Description: "test"`) and `RecurrenceSettings: &domain.RecurrenceSettings{Type: domain.RecurrenceTypeMonthly, CurrentInstallment: 0, TotalInstallments: 5}`
- Call `suite.Services.Transaction.Create(context.Background(), suite.UserID, &req)`
- Assert `suite.Error(err)`
- Assert `suite.True(pkgErrors.Is(err, *pkgErrors.ErrRecurrenceCurrentInstallmentMustBeAtLeastOne))` — uses the custom `pkgErrors.Is()` which checks by tag match against ServiceErrors

**Method 2: `TestValidationRejectsCurrentGreaterThanTotal`** (TST-05)
- Same request but `CurrentInstallment: 5, TotalInstallments: 3`
- Assert `pkgErrors.Is(err, *pkgErrors.ErrRecurrenceTotalInstallmentsMustBeGreaterOrEqualToCurrent)`

**Method 3: `TestValidationRejectsTotalGreaterThan1000`** (TST-06)
- Same request but `CurrentInstallment: 1, TotalInstallments: 1001`
- Assert `pkgErrors.Is(err, *pkgErrors.ErrRecurrenceTotalInstallmentsMustBeLessThanOrEqualTo(1000))`

**Runner function** (add after `TestTransactionCreateWithDB` runner):
```go
func TestTransactionCreateValidation(t *testing.T) {
    suite.Run(t, new(TransactionCreateValidationTestSuite))
}
```

**Import note:** Ensure `pkgErrors "github.com/finance_app/backend/pkg/errors"` is in the imports (check if it's already there from the integration tests — if not, add it).

**Why pkgErrors.Is():** `Create` wraps validation errors in `pkgErrors.ServiceErrors(errs)` (a `[]*ServiceError` slice type). Direct `suite.Equal` won't work because `ServiceErrors != *ServiceError`. The custom `pkgErrors.Is(err, target)` function (at errors.go:147) type-asserts to `ServiceErrors` and checks tag overlap, which is the correct comparison.
  </action>
  <verify>
    <automated>cd /workspace/backend && go test ./internal/service/ -run "TestTransactionCreateValidation" -short -v -timeout 30s</automated>
  </verify>
  <acceptance_criteria>
    - grep "TransactionCreateValidationTestSuite" backend/internal/service/transaction_create_test.go returns a match
    - grep "TestValidationRejectsMissingCurrentInstallment" backend/internal/service/transaction_create_test.go returns a match
    - grep "TestValidationRejectsCurrentGreaterThanTotal" backend/internal/service/transaction_create_test.go returns a match
    - grep "TestValidationRejectsTotalGreaterThan1000" backend/internal/service/transaction_create_test.go returns a match
    - grep "pkgErrors.Is" backend/internal/service/transaction_create_test.go returns matches
    - grep "TestTransactionCreateValidation" backend/internal/service/transaction_create_test.go returns a match (runner function)
    - go test exits 0 for the validation tests with -short flag
  </acceptance_criteria>
  <done>Three validation unit tests pass: TestValidationRejectsMissingCurrentInstallment asserts ErrRecurrenceCurrentInstallmentMustBeAtLeastOne; TestValidationRejectsCurrentGreaterThanTotal asserts ErrRecurrenceTotalInstallmentsMustBeGreaterOrEqualToCurrent; TestValidationRejectsTotalGreaterThan1000 asserts ErrRecurrenceTotalInstallmentsMustBeLessThanOrEqualTo(1000). All use pkgErrors.Is for correct ServiceErrors comparison.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test DB isolation | Shared PostgreSQL instance across all integration tests — each test creates its own user for isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Shared test DB | accept | Tests create unique users per method; no PII in test data; test DB is ephemeral (testcontainers) |
| T-04-02 | Denial of Service | Integration test spin-up | accept | sync.Once ensures single DB instance; testcontainers auto-cleanup |
</threat_model>

<verification>
1. `cd /workspace/backend && go test ./internal/service/ -run "TestTransactionCreateWithDB/TestCreateRecurringExpense" -tags=integration -v -timeout 120s` exits 0
2. `cd /workspace/backend && go test ./internal/service/ -run "TestTransactionCreateValidation" -short -v -timeout 30s` exits 0
3. `cd /workspace/backend && go test ./internal/service/ -run "TestTransactionCreate" -tags=integration -v -timeout 180s` exits 0 (all create tests including existing ones still pass)
</verification>

<success_criteria>
- 2 new integration test methods pass (TST-01, TST-02, TST-03 covered by date assertions in both)
- 3 new validation unit test methods pass (TST-04, TST-05, TST-06)
- All existing tests in transaction_create_test.go continue to pass
- TST-07 confirmed already done (no action needed per D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests/04-01-SUMMARY.md`
</output>
