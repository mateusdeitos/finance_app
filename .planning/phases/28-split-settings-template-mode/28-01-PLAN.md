---
phase: 28-split-settings-template-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/transactions/form/SplitSettingsFields.tsx
  - frontend/src/testIds/transactions.ts
  - frontend/src/components/transactions/form/SplitSettingsFields.template.test.tsx
autonomous: true
requirements: [MNG-03]

must_haves:
  truths:
    - "templateMode is an additive prop defaulting to false — every existing call site behaves exactly as before (success criterion 3)"
    - "In template mode the per-row '= R$ X' preview and the 'Soma X%' footer are not rendered (success criterion 1)"
    - "In template mode the %/R$ SegmentedControl toggle still renders and switches the per-row input between percentage and amount (success criterion 2)"
  artifacts:
    - path: "frontend/src/components/transactions/form/SplitSettingsFields.tsx"
      provides: "templateMode prop threaded through SplitRow -> SplitRowControls; previews gated on !templateMode; testids on preview + footer"
      contains: "templateMode"
    - path: "frontend/src/testIds/transactions.ts"
      provides: "stable testids for the suppressed preview + footer"
      contains: "SplitSumFooter"
    - path: "frontend/src/components/transactions/form/SplitSettingsFields.template.test.tsx"
      provides: "vitest coverage: template mode hides previews + toggle works; transaction mode still shows previews"
      contains: "templateMode"
  key_links:
    - from: "per-row preview Text"
      to: "templateMode suppression"
      via: "&& !templateMode added to the totalAmount > 0 guard"
      pattern: "!templateMode"
    - from: "Soma footer group"
      to: "templateMode suppression"
      via: "&& !templateMode added to the fields.length > 0 && totalAmount > 0 guard"
      pattern: "!templateMode"
---

<objective>
Make `SplitSettingsFields` template-aware: add an additive `templateMode?: boolean` prop (default `false`) that suppresses the derived-value previews (per-row `= R$ X` and the `Soma X%` footer) while keeping the `%`/`R$` toggle and every other behavior intact. Add stable testids for the suppressed elements and vitest coverage.

Purpose: The Phase 30 `TemplateForm` embeds this shared component but has no `amount` field, so the live split calculation is meaningless there. This prepares the component; the template form itself is out of scope.
Output: modified component + testids + a focused component test. No new route/form.
</objective>

<execution_context>
@/home/user/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-split-settings-template-mode/28-CONTEXT.md
@frontend/CLAUDE.md

<interfaces>
<!-- Current component shape — build against these, no exploration needed -->

`frontend/src/components/transactions/form/SplitSettingsFields.tsx` today:
- `SplitSettingsFieldsProps` (~L266-273): `{ namePrefix?, comboboxWithinPortal?, onlyPercentage? }`. Add `templateMode?: boolean` (default false).
- `SplitRowControls` (~L45-133): renders the per-row input. The preview `Text` (~L110-112) is:
  `{mode === "percentage" && totalAmount > 0 ? `= R$ ${formatCurrency(calculatedAmount)}` : ""}`
- `SplitRow` (~L137-262): passes props down to `SplitRowControls`. Add `templateMode` to its props + the `<SplitRowControls .../>` call.
- `SplitSettingsFields` (~L275-417): the `Soma` footer group (~L406-413) is guarded by `fields.length > 0 && totalAmount > 0`. Add `&& !templateMode`. The `%`/`R$` `SegmentedControl` (~L340-350) must stay (do NOT gate it on templateMode).

Thread path: `SplitSettingsFields` (has `templateMode`) → `SplitRow` (add prop) → `SplitRowControls` (add prop, gate preview).

`frontend/src/testIds/transactions.ts`: split ids live in a `TransactionsTestIds` `as const` object (`InputSplitAmount`, `InputSplitPercentage`, `BtnAddSplitRow`, `InputSplitDate: (rowIndex) => ...`). Add:
- `SplitRowPreview: (rowIndex: number | string) => `text_split_preview_${rowIndex}` as const` (parametric — on the per-row preview Text)
- `SplitSumFooter: 'text_split_sum_footer'` (static — on the footer Group)

Test harness to mirror: `frontend/src/components/transactions/form/TransactionForm.split.test.tsx` (MantineProvider + QueryClientProvider with `qc.setQueryData([QueryKeys.Accounts], [...])` + FormProvider). For the new test, render `SplitSettingsFields` standalone inside a `FormProvider` (it relies on `useFormContext`) with an accepted connection account seeded into the accounts query, and (for the transaction-mode assertion) an `amount` default value > 0 so the previews render.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add templateMode prop, suppress previews, add testids</name>
  <read_first>
    - frontend/src/components/transactions/form/SplitSettingsFields.tsx (full — understand the SplitSettingsFields → SplitRow → SplitRowControls prop threading)
    - frontend/src/testIds/transactions.ts (the TransactionsTestIds object + the parametric-id `as const` factory convention)
    - frontend/CLAUDE.md §"E2E tests / Declaring testid constants" and §"TypeScript: no any"
  </read_first>
  <action>
    In `frontend/src/testIds/transactions.ts`, add to the `TransactionsTestIds` object (near the other Split ids ~L71-74):
    - `SplitRowPreview: (rowIndex: number | string) => `text_split_preview_${rowIndex}` as const,`
    - `SplitSumFooter: 'text_split_sum_footer',`

    In `frontend/src/components/transactions/form/SplitSettingsFields.tsx`:
    1. Add `templateMode?: boolean` to `SplitSettingsFieldsProps` (documented: "No-amount context (template form): suppress derived-value previews.") and destructure it with a `false` default in `SplitSettingsFields`.
    2. Add `templateMode: boolean` to `SplitRowProps` and `SplitRowControlsProps`; thread it: `SplitSettingsFields` passes `templateMode` to each `<SplitRow>`, and `SplitRow` passes it to `<SplitRowControls>`.
    3. In `SplitRowControls`, change the preview to be suppressed in template mode AND carry a testid:
       ```tsx
       <Text
         size="sm"
         c="dimmed"
         className={classes.preview}
         data-testid={TransactionsTestIds.SplitRowPreview(rowIndex)}
       >
         {mode === "percentage" && totalAmount > 0 && !templateMode ? `= R$ ${formatCurrency(calculatedAmount)}` : ""}
       </Text>
       ```
    4. In `SplitSettingsFields`, gate the `Soma` footer on `!templateMode` and add the testid to its `Group`:
       ```tsx
       {fields.length > 0 && totalAmount > 0 && !templateMode && (
         <Group gap={4} align="center" data-testid={TransactionsTestIds.SplitSumFooter}>
           ...
         </Group>
       )}
       ```
    5. Do NOT gate the `%`/`R$` `SegmentedControl` on `templateMode` — it stays visible (only `onlyPercentage` hides it, unchanged).
    6. Do NOT introduce any new `any` or `useEffect`. Do not refactor the file's pre-existing divergences.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tee /tmp/tsc.log; test ! -s /tmp/tsc.log && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'templateMode' frontend/src/components/transactions/form/SplitSettingsFields.tsx` >= 6 (interface + destructure + 2 prop types + 2 threads + 2 guards)
    - `grep -q 'SplitRowPreview' frontend/src/testIds/transactions.ts`
    - `grep -q "SplitSumFooter: 'text_split_sum_footer'" frontend/src/testIds/transactions.ts`
    - `grep -q '&& !templateMode' frontend/src/components/transactions/form/SplitSettingsFields.tsx`
    - the SegmentedControl block is NOT gated on templateMode (only `!onlyPercentage`): `grep -q '!onlyPercentage &&' frontend/src/components/transactions/form/SplitSettingsFields.tsx`
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` reports no NEW errors for SplitSettingsFields.tsx / transactions.ts
  </acceptance_criteria>
  <done>templateMode prop exists and threads through both child components; previews suppressed in template mode; testids added; toggle preserved; type-checks and lints clean with no new any/useEffect.</done>
</task>

<task type="auto">
  <name>Task 2: Component test for template mode + non-template regression</name>
  <read_first>
    - frontend/src/components/transactions/form/TransactionForm.split.test.tsx (harness: providers, seeded accounts query, connected account fixture)
    - frontend/src/components/transactions/form/SplitSettingsFields.tsx (final version from Task 1)
    - frontend/src/testIds/transactions.ts (the new ids)
  </read_first>
  <action>
    Create `frontend/src/components/transactions/form/SplitSettingsFields.template.test.tsx`. Reuse the provider harness pattern from `TransactionForm.split.test.tsx` (MantineProvider + QueryClientProvider seeding `[QueryKeys.Accounts]` with a private account + an `accepted` connection account + the `me` query returning `{ id: 1 }`). Render `SplitSettingsFields` inside a `FormProvider` (a small wrapper component calling `useForm` with `defaultValues` including one `split_settings` row `{ connection_id: 10, amount: 0, date: null }`).

    Write three tests:
    1. **Template mode suppresses previews**: render with `templateMode` and an `amount` default of `0` (no amount field). Assert `queryByTestId(TransactionsTestIds.SplitSumFooter)` is null, and the per-row preview `getByTestId(TransactionsTestIds.SplitRowPreview(0))` has empty text content.
    2. **Toggle still works in template mode**: with `templateMode`, the `%`/`R$` `SegmentedControl` is present and switching to `R$` shows the amount `CurrencyInput` (`getByTestId(TransactionsTestIds.InputSplitAmount)`), switching to `%` shows the percentage input (`getByTestId(TransactionsTestIds.InputSplitPercentage)`). (Use `fireEvent`/`userEvent` on the segmented items; if a segment lacks a testid, select via the input's `value` — do not add production testids beyond those in Task 1 unless needed, and if needed add them to `testIds/`.)
    3. **Non-template unchanged**: render WITHOUT `templateMode`, with an `amount` default > 0 (e.g. `10000`) and a percentage row, and assert the `Soma` footer IS present (`getByTestId(SplitSumFooter)`) and the per-row preview text is non-empty — proving the additive prop did not change default behavior (success criterion 3).

    Keep the test file self-contained and typed (no `any`). If rendering `SplitSettingsFields` standalone surfaces a missing provider/hook, mirror exactly what `TransactionForm.split.test.tsx` sets up.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/transactions/form/SplitSettingsFields.template.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/transactions/form/SplitSettingsFields.template.test.tsx`
    - `grep -c 'templateMode' frontend/src/components/transactions/form/SplitSettingsFields.template.test.tsx` >= 2
    - `grep -q 'SplitSumFooter' frontend/src/components/transactions/form/SplitSettingsFields.template.test.tsx`
    - `cd frontend && npx vitest run src/components/transactions/form/SplitSettingsFields.template.test.tsx` passes (all tests green)
    - `cd frontend && npx vitest run src/components/transactions/form/TransactionForm.split.test.tsx` still passes (no regression)
  </acceptance_criteria>
  <done>Three passing tests prove template-mode suppression, toggle preservation, and non-template regression safety; the existing split test still passes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npm run lint` — no new errors
- `cd frontend && npx vitest run src/components/transactions/form/` — all split tests green
- Success criteria mapping: (1) previews suppressed → Task 1 guards + Task 2 test 1; (2) toggle works → Task 1 keeps SegmentedControl + Task 2 test 2; (3) existing unchanged → additive default + Task 2 test 3.
</verification>

<success_criteria>
- `templateMode` additive prop suppresses per-row preview + Soma footer in template context.
- `%`/`R$` toggle remains functional in template mode.
- Every existing call site is unaffected (default false); vitest proves non-template behavior unchanged.
- Type-checks, lints, and tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/28-split-settings-template-mode/28-01-SUMMARY.md`.
</output>
