---
phase: 24-frontend-permission-subscribe-service-worker
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - frontend/src/components/notifications/NotificationToggleRow.tsx
  - frontend/src/components/notifications/NotificationToggleRow.module.css
  - frontend/src/hooks/useServiceWorkerNavigation.ts
  - frontend/src/components/AppLayout.tsx
  - frontend/src/components/MobileMoreDrawer.tsx
  - frontend/src/components/DesktopSidebar.tsx
  - frontend/e2e/tests/notifications.spec.ts
  - frontend/e2e/pages/NotificationsPage.ts
autonomous: false
requirements: [CTRL-02, CTRL-03, SUB-01, SUB-02]
must_haves:
  truths:
    - "A notification toggle row appears in the mobile Mais drawer (above Sair) and the desktop user menu (after Tema, above the divider)"
    - "The toggle visually reflects all five states with correct icon, helper copy, and disabled behavior"
    - "Tapping the toggle from default triggers the permission/subscribe flow (no prompt on load)"
    - "When the SW posts a NAVIGATE message, the app routes to the derived list page"
  artifacts:
    - path: "frontend/src/components/notifications/NotificationToggleRow.tsx"
      provides: "5-state Switch row consuming usePushSubscription"
      min_lines: 30
      contains: "usePushSubscription"
    - path: "frontend/src/hooks/useServiceWorkerNavigation.ts"
      provides: "SW message → router.navigate (CTRL-03 page side)"
      exports: ["useServiceWorkerNavigation"]
    - path: "frontend/e2e/tests/notifications.spec.ts"
      provides: "toggle-state + permission-gate e2e"
      contains: "switch_notifications"
  key_links:
    - from: "frontend/src/components/MobileMoreDrawer.tsx"
      to: "NotificationToggleRow"
      via: "rendered above the Sair item"
      pattern: "NotificationToggleRow"
    - from: "frontend/src/components/DesktopSidebar.tsx"
      to: "NotificationToggleRow"
      via: "rendered in Menu.Dropdown after Tema, above Menu.Divider"
      pattern: "NotificationToggleRow"
    - from: "frontend/src/components/AppLayout.tsx"
      to: "useServiceWorkerNavigation"
      via: "hook mounted once high in the tree"
      pattern: "useServiceWorkerNavigation"
    - from: "frontend/src/hooks/useServiceWorkerNavigation.ts"
      to: "router.navigate"
      via: "navigator.serviceWorker 'message' listener"
      pattern: "router.navigate"
---

<objective>
Make the feature visible and complete the CTRL-03 page side. Build `NotificationToggleRow` (the 5-state Mantine Switch row), wire it into both surfaces (mobile Mais drawer above "Sair"; desktop user menu after "Tema", above the divider), mount `useServiceWorkerNavigation` so a notification tap routes the open app, and author the Playwright e2e for the toggle states + the CTRL-01 permission gate.

Purpose: This closes CTRL-02 (visible toggle), completes CTRL-03 (page-side navigation), and surfaces SUB-01/SUB-02 through the UI. It depends on Plan 04 (`usePushSubscription`) and Plan 03 (the SW's `postMessage({type:'NAVIGATE'})` contract). A human-verify checkpoint confirms visual fidelity against the locked design prototype across both surfaces and 375px.
Output: component + CSS, page-nav hook, AppLayout mount, two wired surfaces, e2e spec + page object.
</objective>

<execution_context>
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-frontend-permission-subscribe-service-worker/24-UI-SPEC.md
@.planning/phases/24-frontend-permission-subscribe-service-worker/24-RESEARCH.md
@.planning/phases/24-frontend-permission-subscribe-service-worker/design/notif-components.jsx
@.planning/phases/24-frontend-permission-subscribe-service-worker/design/colors_and_type.css
@frontend/CLAUDE.md

<interfaces>
<!-- From Plan 04 -->
import { usePushSubscription, type NotificationState } from "@/hooks/usePushSubscription"
// returns { state, onToggle, helperText } — helperText exposes short (mobile) + full (desktop) copy (confirm exact shape from 24-04-SUMMARY)

<!-- From Plan 02 -->
import { NotificationsTestIds } from "@/testIds"
// SwitchNotifications:'switch_notifications', RowNotifications:'row_notifications', HelperNotifications:'text_notifications_helper'

<!-- From Plan 03 SW -->
// SW posts: client.postMessage({ type: "NAVIGATE", url })  // url ∈ {/charges,/transactions,/}

<!-- Existing global router (verified) -->
import { router } from "@/router"   // router.navigate({ to })

<!-- MobileMoreDrawer (verified): items[] of UnstyledButton rows; "Sair" is the last item (key 'logout', danger). Render the toggle row as a non-button row in the same Stack ABOVE the logout button. -->

<!-- DesktopSidebar Menu.Dropdown (verified, ~line 156-173):
<Menu.Dropdown>
  <Menu.Label>{user.email}</Menu.Label>
  <div className={classes.themeMenuItem}><Group><Text size="sm">Tema</Text><ThemeToggle /></Group></div>
  <!-- INSERT NotificationToggleRow row here, same themeMenuItem div pattern -->
  <Menu.Divider />
  <Menu.Item ...>Sair</Menu.Item>
</Menu.Dropdown>
Add a NotificationToggleRow row in a themeMenuItem-style div AFTER Tema, BEFORE Menu.Divider. -->

<!-- E2E (CLAUDE.md mandatory): use SwitchField from e2e/helpers/formFields.ts; data-testid only; scope to the dropdown to avoid duplicate-testid strict-mode (mobile+desktop both render the row). Pattern reference: e2e/tests/theme-toggle.spec.ts. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationToggleRow component + page-nav hook + AppLayout mount</name>
  <files>frontend/src/components/notifications/NotificationToggleRow.tsx, frontend/src/components/notifications/NotificationToggleRow.module.css, frontend/src/hooks/useServiceWorkerNavigation.ts, frontend/src/components/AppLayout.tsx</files>
  <action>
**NotificationToggleRow.tsx** (UI-SPEC §Surface 1 + Design Reference LOCKED; match `design/notif-components.jsx` colors/spacing/copy where more specific):
- Consume `usePushSubscription()`; the component is dumb (no SW/permission logic here).
- Layout: Mantine `Group justify="space-between" align="center" wrap="nowrap"`, row min-height 44px, icon column 20px, `gap` 16. Left: icon + label/helper Stack. Right: `<Switch size="md">`.
- Per state (UI-SPEC table): icon `IconBell` (enabled/requesting) / `IconBellOff` (default/denied/unsupported), 20px stroke 1.8; `requesting` replaces the icon with `<Loader size="xs" color="blue" />` (OD-1 LOCKED). Switch `checked`/`disabled` per the state table (disabled for requesting/denied/unsupported). Track `blue.6` on / `grey-2` off.
- Helper `<Text>` below label: `denied` → `c="red"`; `unsupported` → `c="dimmed"`; copy from the hook's `helperText` (short on mobile, full on desktop — accept a `variant: 'mobile' | 'desktop'` prop and pass it to the hook's helper selector, per OD-3).
- Switch `onChange={() => onToggle()}` (Switch is the explicit affordance — CTRL-01; no separate modal/banner; no "Como ativar" link on denied).
- Accessibility: `aria-label` "Ativar notificações" when unchecked / "Desativar notificações" when checked; helper `id` linked via `aria-describedby`; 44px touch target.
- testids: Switch=`SwitchNotifications`, outer Group=`RowNotifications`, helper Text=`HelperNotifications`.
- Colocate `NotificationToggleRow.module.css` if needed for the 44px row / icon column.

**useServiceWorkerNavigation.ts** (research Pattern 4 — named hook because no inline component `useEffect`): in a `useEffect`, if `'serviceWorker' in navigator`, add a `message` listener; on `e.data?.type === 'NAVIGATE'` AND `typeof e.data.url === 'string'` AND `e.data.url.startsWith('/')` (spoofing guard T-24-06), call `router.navigate({ to: e.data.url })`; cleanup removes the listener. No `any` — type the handler param as `MessageEvent`.

**AppLayout.tsx**: call `useServiceWorkerNavigation()` once near the top of the `AppLayout` function body (single mount point high in the tree).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -15 && grep -q "usePushSubscription" src/components/notifications/NotificationToggleRow.tsx && grep -q "useServiceWorkerNavigation" src/components/AppLayout.tsx && grep -q "router.navigate" src/hooks/useServiceWorkerNavigation.ts</automated>
  </verify>
  <done>Component renders all 5 states with correct icon/loader/disabled/helper-color and testids; `onToggle` wired to Switch onChange; nav hook validates type+url and navigates; AppLayout mounts the hook; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire into MobileMoreDrawer + DesktopSidebar; author e2e</name>
  <files>frontend/src/components/MobileMoreDrawer.tsx, frontend/src/components/DesktopSidebar.tsx, frontend/e2e/tests/notifications.spec.ts, frontend/e2e/pages/NotificationsPage.ts</files>
  <action>
**MobileMoreDrawer.tsx**: render `<NotificationToggleRow variant="mobile" />` inside the items `Stack`, immediately ABOVE the "Sair" (`logout`) row. Keep it a non-button row (it owns its own Switch). The existing items array drives buttons; insert the toggle row as JSX in the Stack before/after the `items.map(...)` such that visual order is: invite, import, **notifications toggle**, Sair. (If the logout row must stay last, render the map for non-logout items, then the toggle row, then the logout button — restructure minimally.)

**DesktopSidebar.tsx**: insert a `themeMenuItem`-style `<div>` containing `<NotificationToggleRow variant="desktop" />` in the `Menu.Dropdown` AFTER the "Tema" row and BEFORE `<Menu.Divider />` (OD-2 LOCKED).

**e2e** (`e2e/tests/notifications.spec.ts` + `e2e/pages/NotificationsPage.ts`) — follow `theme-toggle.spec.ts` and CLAUDE.md E2E rules (testid-only; `SwitchField` from `e2e/helpers/formFields.ts`; one fresh user per test via `getAuthTokenForUser`/`openAuthedPage`; scope to the dropdown/drawer to avoid duplicate-testid strict-mode since both surfaces render the row). Stub browser push via `page.addInitScript` so no real push service is needed (research Decision 5):
  - **unsupported state:** init-script deletes `window.PushManager` (or `navigator.serviceWorker`) → assert helper testid shows the unsupported copy and the Switch is disabled.
  - **denied state:** init-script stubs `Notification.permission='denied'` → assert denied helper + disabled Switch.
  - **default → permission gate (CTRL-01):** init-script spies on `Notification.requestPermission` (record call count); assert it is NOT called on page load; then `SwitchField(...).set(true)` and assert `requestPermission` WAS called exactly once after the tap. Stub `navigator.serviceWorker.ready.pushManager.subscribe` to return a fake subscription and stub the POST (route fulfill 204) so the state can reach `enabled`; use `context.grantPermissions(['notifications'])` for the granted path.
  - Open the relevant surface (desktop user menu or mobile Mais drawer) and scope all locators to it.
Add a `NotificationsPage` page object exposing `openDesktopMenu()`, `openMobileDrawer()`, `toggle(on)`, `helperText()`, `isSwitchDisabled()`.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -10 && grep -q "NotificationToggleRow" src/components/MobileMoreDrawer.tsx && grep -q "NotificationToggleRow" src/components/DesktopSidebar.tsx && grep -q "switch_notifications" e2e/tests/notifications.spec.ts && npx tsc -b --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Toggle row renders above "Sair" on mobile and after "Tema"/above divider on desktop; build + tsc pass; e2e spec + page object authored covering unsupported/denied/default-gate states using SwitchField and addInitScript stubs (e2e EXECUTION is deferred to UAT — Docker/push service unavailable here; spec must be authored and type-check clean).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The notification toggle row, rendered in both the mobile "Mais" drawer (above "Sair") and the desktop user menu (after "Tema", above the divider), driven by the 5-state machine, plus the SW-tap → app-navigation hook.</what-built>
  <how-to-verify>
1. `cd frontend && npm run build && npm run preview` (the SW is only active in a production build/preview, not `npm run dev` — research Pitfall 4). Open the preview URL.
2. **Desktop:** open the user menu (top-right). Confirm a "Notificações" row appears AFTER "Tema" and ABOVE the divider before "Sair". It should show `IconBellOff` + "Toque para ativar" in the `default` state, Switch off. Compare against `design/notif-desktop.png`.
3. **Mobile (resize to 375px or device toolbar):** open the "Mais" drawer. Confirm the "Notificações" row appears ABOVE "Sair", helper text not truncated. Compare against `design/notif-mobile2.png`.
4. **Permission gate (CTRL-01):** with a fresh browser profile (permission not yet asked), confirm NO permission prompt on page load. Tap the Switch from `default` → the browser permission prompt appears only now. Grant it → row transitions through `requesting` (Loader spinner) to `enabled` ("Ativadas..."), teal success toast top-right.
5. **Disable (SUB-02):** tap the enabled Switch → returns to `default`, teal "Notificações desativadas" toast.
6. **Denied:** in browser settings block notifications for the origin, reload → row shows `denied` (red helper "Bloqueadas — ...", Switch disabled, no "Como ativar" link).
7. (Optional, real device — also on the deferred UAT list) trigger a backend push and tap the OS notification → app focuses and lands on `/charges` or `/transactions`.
Confirm colors/spacing/copy match the locked `design/` prototype.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/behavioral issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SW postMessage → useServiceWorkerNavigation | Navigation instruction enters the page |
| user gesture → toggle → permission/subscribe | Explicit opt-in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Spoofing | `useServiceWorkerNavigation` message handler | mitigate | Navigate only when `e.data.type==='NAVIGATE'` AND `e.data.url` is a string starting with `/` (internal-path allow-list), preventing a spoofed `postMessage` from redirecting the SPA off-origin. |
| T-24-10 | Elevation / Annoyance | toggle `onChange` | mitigate | Permission prompt fires only on the user-gesture toggle from `default`; the `denied`/`unsupported`/`requesting` states render the Switch `disabled`, so no repeated prompting is possible. |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds; `cd frontend && npx tsc -b --noEmit` clean; `cd frontend && npm run lint` clean.
- Both surfaces render the row in the specified position (grep confirms wiring; checkpoint confirms placement/fidelity).
- e2e spec authored and type-checks; EXECUTION deferred to UAT (no Docker/push service here).
- Human-verify checkpoint passed (visual fidelity + CTRL-01 gate + subscribe/unsubscribe + denied state).
- Deferred-to-UAT (add to `24-HUMAN-UAT.md`): real OS push render (SC4), real notification tap → list (SC5/CTRL-03), iOS installed-PWA path (research Pitfall 1), full Playwright run on the Docker e2e stack.
</verification>

<success_criteria>
- CTRL-02: toggle visible on both surfaces, all 5 states render correctly (checkpoint-verified).
- CTRL-03: page-side navigation hook wired; full tap→land verified at UAT.
- SUB-01/SUB-02: subscribe/unsubscribe reachable and working from the UI (checkpoint-verified for the granted path).
</success_criteria>

<output>
After completion, create `.planning/phases/24-frontend-permission-subscribe-service-worker/24-05-SUMMARY.md` and a `.planning/phases/24-frontend-permission-subscribe-service-worker/24-HUMAN-UAT.md` checklist aggregating all deferred device/real-push items (SC4 OS render, SC5 real tap, iOS installed-PWA, full e2e on Docker stack).
</output>
