---
phase: 24-frontend-permission-subscribe-service-worker
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - frontend/vite.config.ts
  - frontend/src/sw.ts
  - frontend/package.json
autonomous: true
requirements: [CTRL-03]
must_haves:
  truths:
    - "The build produces a custom service worker (sw.ts) instead of an auto-generated one"
    - "The app shell still precaches and deep routes still resolve on hard refresh (navigateFallback preserved)"
    - "The auth-boot NetworkFirst cache still serves /api/auth/me and /api/onboarding/status within 2s"
    - "On a push event the SW shows an OS notification using the server-provided title and body"
    - "Tapping a notification focuses an open client (postMessage) or opens a new window at the derived list route"
  artifacts:
    - path: "frontend/src/sw.ts"
      provides: "custom SW: precache + auth-boot cache + push + notificationclick"
      min_lines: 50
      contains: "addEventListener(\"push\""
    - path: "frontend/vite.config.ts"
      provides: "injectManifest strategy pointing at src/sw.ts"
      contains: "injectManifest"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "src/sw.ts"
      via: "strategies:'injectManifest', srcDir:'src', filename:'sw.ts'"
      pattern: "injectManifest"
    - from: "frontend/src/sw.ts"
      to: "deriveDeepLink"
      via: "import from @/utils/pushDeepLink"
      pattern: "deriveDeepLink"
    - from: "frontend/src/sw.ts notificationclick"
      to: "open page client"
      via: "postMessage({type:'NAVIGATE', url})"
      pattern: "postMessage"
---

<objective>
Switch vite-plugin-pwa from `generateSW` to `injectManifest` with a hand-written `src/sw.ts`, then implement the push receipt and notificationclick behavior (SC4 render + CTRL-03 routing). Critically, the strategy switch means nothing is auto-generated except the precache manifest — so this plan must REIMPLEMENT the existing `navigateFallback` and the `auth-boot` NetworkFirst runtime cache verbatim, or the app regresses (white screens, broken deep-link refresh).

Purpose: `generateSW` cannot host custom `push`/`notificationclick` handlers. Registration is UNCHANGED — `PWAUpdateNotifier`'s `useRegisterSW(virtual:pwa-register/react)` with `injectRegister:false` keeps registering `/sw.js`. Zero new runtime deps (workbox is transitive; add `workbox-*` as pinned devDeps only if types don't resolve).
Output: modified `vite.config.ts`, new `src/sw.ts`, possibly added workbox devDeps.
</objective>

<execution_context>
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-frontend-permission-subscribe-service-worker/24-RESEARCH.md
@.planning/phases/24-frontend-permission-subscribe-service-worker/24-CONTEXT.md
@frontend/CLAUDE.md

<interfaces>
<!-- CURRENT vite.config.ts VitePWA block to convert (verbatim) -->
```ts
VitePWA({
  registerType: "prompt",
  injectRegister: false,
  devOptions: { enabled: false },
  workbox: {
    globPatterns: ["**/*.{js,css,html,ico,png,svg,woff2}"],
    cleanupOutdatedCaches: true,
    navigateFallback: "/index.html",
    runtimeCaching: [{
      urlPattern: ({ url }) =>
        url.pathname === "/api/auth/me" || url.pathname === "/api/onboarding/status",
      handler: "NetworkFirst",
      options: {
        cacheName: "auth-boot",
        networkTimeoutSeconds: 2,
        expiration: { maxEntries: 8, maxAgeSeconds: 60 * 60 * 24 },
        cacheableResponse: { statuses: [0, 200] },
      },
    }],
  },
  manifest: { /* name FinanceApp, icons 192/512, display standalone — UNCHANGED */ },
})
```

Push payload (Phase 23, verified, after Plan 01's title change):
`{ title: string, body: string, data: { type: string, entity_type: 'charge'|'transaction', entity_id: number } }`
— payload has NO `url` field; derive it.

From Plan 02: `import { deriveDeepLink } from "@/utils/pushDeepLink"` → returns `/charges` | `/transactions` | `/`.

Icons exist at `/icon-192.png`, `/icon-512.png` (manifest verified).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vite.config.ts — generateSW → injectManifest</name>
  <files>frontend/vite.config.ts, frontend/package.json</files>
  <action>
Convert the `VitePWA(...)` options to `injectManifest` (research Pattern 1):
- Add `strategies: "injectManifest"`, `srcDir: "src"`, `filename: "sw.ts"`.
- KEEP `registerType: "prompt"` and `injectRegister: false` (PWAUpdateNotifier still registers).
- KEEP `devOptions: { enabled: false }` (do not flip it on in committed config — see research Pitfall 4; SW dev is via `npm run preview` / e2e on port 3100).
- REPLACE the `workbox: { ... }` block with `injectManifest: { globPatterns: ["**/*.{js,css,html,ico,png,svg,woff2}"] }`. The `cleanupOutdatedCaches`, `navigateFallback`, and `runtimeCaching` behaviors move INTO `src/sw.ts` (Task 2) — they are NOT auto-generated in injectManifest mode.
- KEEP the `manifest: { ... }` block exactly as-is.

After editing, run `npm install` then `cd frontend && npm run build`. If TypeScript cannot resolve `workbox-precaching`/`workbox-routing`/`workbox-strategies`/`workbox-expiration`/`workbox-cacheable-response` types when building `sw.ts` (Task 2), add them to `package.json` `devDependencies` pinned to the versions vite-plugin-pwa 1.x bundles (check `npm ls workbox-precaching`), then `npm install` again. Do NOT add any runtime dependency.
  </action>
  <verify>
    <automated>cd frontend && grep -q "injectManifest" vite.config.ts && grep -q 'filename: "sw.ts"' vite.config.ts && ! grep -q "runtimeCaching" vite.config.ts</automated>
  </verify>
  <done>vite.config.ts uses `strategies:'injectManifest'` pointing at `src/sw.ts`; the declarative `workbox.runtimeCaching`/`navigateFallback` block is removed from the config; manifest + registration settings unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: src/sw.ts — precache + preserved auth-boot cache + push + click</name>
  <files>frontend/src/sw.ts</files>
  <action>
Create the custom service worker (research Pattern 2). It MUST reimplement everything the old generateSW config provided, THEN add push handling. Use TS SW typing: `/// <reference lib="webworker" />` + `declare const self: ServiceWorkerGlobalScope`. No `any` (CLAUDE.md §7).

Sections, in order:
1. **Precache + cleanup:** `cleanupOutdatedCaches()` then `precacheAndRoute(self.__WB_MANIFEST)` from `workbox-precaching`.
2. **navigateFallback → /index.html:** `registerRoute(new NavigationRoute(createHandlerBoundToURL("/index.html")))` (workbox-routing + workbox-precaching). This replaces the old `navigateFallback` so deep-route hard-refresh still resolves (research Pitfall 5).
3. **Preserved auth-boot NetworkFirst** (VERBATIM behavior of the removed `runtimeCaching` block — same cacheName `"auth-boot"`, `networkTimeoutSeconds: 2`, expiration `maxEntries: 8`/`maxAgeSeconds: 60*60*24`, cacheable statuses `[0, 200]`):
   ```ts
   registerRoute(
     ({ url }) => url.pathname === "/api/auth/me" || url.pathname === "/api/onboarding/status",
     new NetworkFirst({
       cacheName: "auth-boot",
       networkTimeoutSeconds: 2,
       plugins: [
         new CacheableResponsePlugin({ statuses: [0, 200] }),
         new ExpirationPlugin({ maxEntries: 8, maxAgeSeconds: 60 * 60 * 24 }),
       ],
     }),
   );
   ```
   (If `workbox-cacheable-response`/`workbox-expiration` are unresolved, install as devDeps per Task 1.)
4. **push handler (SC4):** guard `if (!event.data) return;` then `event.data.json()` wrapped in try/catch (research Pitfall 2 — a thrown handler under `userVisibleOnly:true` triggers a generic "background update" notification). Parse the typed payload `{ title, body, data: { type, entity_type, entity_id } }`. Derive `url = deriveDeepLink(payload.data)` (import from `@/utils/pushDeepLink`). Build `tag = ${payload.data.entity_type}-${payload.data.entity_id}` (dedupe per entity, e.g. `charge-123`). Then:
   ```ts
   event.waitUntil(self.registration.showNotification(payload.title, {
     body: payload.body, icon: "/icon-192.png", badge: "/icon-192.png", tag, data: { url },
   }));
   ```
   Render `payload.title`/`payload.body` AS-IS — no per-type copy logic in the SW (D-24-2). In the catch, show a minimal fallback notification (title `"Finance App"`, generic body) so the userVisibleOnly contract is honored.
5. **notificationclick (CTRL-03):** `event.notification.close()`; read `url` from `event.notification.data` (default `"/"`); `event.waitUntil(self.clients.matchAll({ type: "window", includeUncontrolled: true }).then(...))`: if a client exists, `client.focus()` + `client.postMessage({ type: "NAVIGATE", url })` and return; else `self.clients.openWindow(url)`. Phase 24 lands on the LIST page — do NOT attempt to open an entity drawer from the SW (UI-SPEC §Surface 4).
6. **SKIP_WAITING message** handler to keep `PWAUpdateNotifier`'s prompt flow working: `self.addEventListener("message", e => { if (e.data?.type === "SKIP_WAITING") self.skipWaiting(); })`.

Reuse the canonical skeleton in research Pattern 2 §1–5 — match cache params exactly.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20 && grep -q 'addEventListener("push"' src/sw.ts && grep -q "deriveDeepLink" src/sw.ts && grep -q "auth-boot" src/sw.ts && grep -q 'type: "NAVIGATE"' src/sw.ts</automated>
  </verify>
  <done>`npm run build` (tsc -b && vite build) succeeds and emits `/sw.js`; sw.ts has push + notificationclick handlers, the preserved auth-boot NetworkFirst route with identical params, and a navigation fallback route; renders server title/body as-is; derives URL via deriveDeepLink.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| push service → sw.ts push handler | Untrusted push payload parsed and rendered as an OS notification |
| sw.ts → page client | postMessage navigation instruction crosses into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Denial of Service / Tampering | sw.ts `push` handler | mitigate | Guard `if(!event.data)` and try/catch `event.data.json()`; always `showNotification` (fallback notification on parse failure) so the `userVisibleOnly` contract is honored and Chrome does not penalize the origin (Pitfall 2). |
| T-24-06 | Spoofing | sw.ts → page `postMessage` | mitigate | SW posts only `{type:'NAVIGATE', url}` where url is from `deriveDeepLink` (fixed internal allow-list); the page-side handler (Plan 05) additionally validates `type==='NAVIGATE'` and that url starts with `/`. |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds, `/sw.js` emitted in `dist/`.
- `cd frontend && npm run lint` clean (no `any`; SW globals typed).
- Manual/preview (deferred-to-UAT verification, documented in SUMMARY): `npm run preview`, load a deep route (`/charges`) and hard-refresh → resolves (navigateFallback intact); DevTools → Application → Cache Storage shows `auth-boot` after visiting boot routes (Pitfall 5).
- Real push delivery + OS render (SC4) and real tap (SC5) CANNOT run here — author the handlers; defer the live render/tap to device UAT (`24-HUMAN-UAT.md`).
</verification>

<success_criteria>
- SC4 (OS notification with title+body) implemented in the `push` handler (live render verified at UAT).
- SC5 / CTRL-03 (tap lands on related list) implemented in `notificationclick` (live tap verified at UAT).
- No regression: app shell precache, deep-route fallback, and auth-boot cold-start cache preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/24-frontend-permission-subscribe-service-worker/24-03-SUMMARY.md`. Record whether workbox devDeps had to be added and add the preview/UAT checks (deep-route refresh, auth-boot cache, real push render/tap) to the deferred UAT list.
</output>
