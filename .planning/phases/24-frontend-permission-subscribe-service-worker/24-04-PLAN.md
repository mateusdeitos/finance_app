---
phase: 24-frontend-permission-subscribe-service-worker
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - frontend/src/api/pushSubscriptions.ts
  - frontend/src/api/pushSubscriptions.test.ts
  - frontend/src/hooks/usePushSubscriptionStatus.ts
  - frontend/src/hooks/usePushSubscription.ts
  - frontend/src/hooks/usePushSubscription.test.tsx
autonomous: true
requirements: [SUB-01, SUB-02, CTRL-01, CTRL-02]
must_haves:
  truths:
    - "On a default toggle tap, the browser permission prompt is requested (only then) and a subscription is created and POSTed to the backend"
    - "On an enabled toggle tap, the device subscription is DELETEd from the backend and unsubscribed locally"
    - "The hook derives exactly five states (default/requesting/enabled/denied/unsupported) without prompting on mount"
    - "Backend subscribed:true is the toggle's truth (field is `subscribed`, not `active`)"
  artifacts:
    - path: "frontend/src/api/pushSubscriptions.ts"
      provides: "fetch wrappers: vapid key, status, subscribe, unsubscribe"
      exports: ["fetchVapidPublicKey", "fetchSubscriptionStatus", "postSubscription", "deleteSubscription"]
    - path: "frontend/src/hooks/usePushSubscription.ts"
      provides: "5-state machine + onToggle (the ONLY place requestPermission/subscribe fire)"
      exports: ["usePushSubscription"]
      contains: "NotificationState"
    - path: "frontend/src/hooks/usePushSubscriptionStatus.ts"
      provides: "TanStack query for GET status"
      exports: ["usePushSubscriptionStatus"]
  key_links:
    - from: "frontend/src/hooks/usePushSubscription.ts"
      to: "Notification.requestPermission"
      via: "called only inside onToggle when state==='default'"
      pattern: "requestPermission"
    - from: "frontend/src/hooks/usePushSubscription.ts"
      to: "frontend/src/api/pushSubscriptions.ts"
      via: "fetchVapidPublicKey + postSubscription + deleteSubscription"
      pattern: "postSubscription|deleteSubscription|fetchVapidPublicKey"
    - from: "frontend/src/api/pushSubscriptions.ts"
      to: "/api/push-subscriptions"
      via: "fetch with credentials:'include'"
      pattern: "push-subscriptions"
---

<objective>
Build the non-visual logic core: the push API client (raw fetch, lives in `src/api/` per CLAUDE.md §3) and the `usePushSubscription` 5-state machine that the toggle component (Plan 05) consumes. This is where SUB-01/SUB-02 subscribe/unsubscribe and CTRL-01 (permission only on explicit toggle) and CTRL-02 (5-state derivation) actually live.

Purpose: The component must be dumb — it reads `state`/`helperText` and calls `onToggle()`. All SW/permission/subscription orchestration is encapsulated here, with the status GET via TanStack Query (no `useEffect` in the component). Depends on Plan 01's VAPID endpoint and Plan 02's `urlBase64ToUint8Array` + `QueryKeys.PushSubscription`.
Output: tested api client, `usePushSubscriptionStatus` query hook, `usePushSubscription` state machine hook.
</objective>

<execution_context>
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-frontend-permission-subscribe-service-worker/24-RESEARCH.md
@.planning/phases/24-frontend-permission-subscribe-service-worker/24-UI-SPEC.md
@.planning/phases/24-frontend-permission-subscribe-service-worker/24-CONTEXT.md
@frontend/CLAUDE.md

<interfaces>
<!-- Backend contract (verified) -->
GET  /api/push-subscriptions/vapid-public-key → 200 { "key": string }   (Plan 01)
GET  /api/push-subscriptions?endpoint=<enc>   → 200 { "subscribed": boolean }   (field is `subscribed`, NOT `active`)
POST /api/push-subscriptions  body { endpoint, keys:{ p256dh, auth } } → 204
DELETE /api/push-subscriptions?endpoint=<enc> → 204

<!-- From Plan 02 -->
import { urlBase64ToUint8Array } from "@/utils/urlBase64ToUint8Array"
QueryKeys.PushSubscription === 'push-subscription'

<!-- Project conventions (verified) -->
// api base, mirroring src/api/charges.ts:
const apiUrl = import.meta.env.VITE_API_URL ?? "http://localhost:8080";
// query hook shape, mirroring src/hooks/useChargesPendingCount.ts:
export function useX<T = D>(select?: (d: D) => T) {
  const qc = useQueryClient();
  const query = useQuery({ queryKey: [QueryKeys.X], queryFn: fetchX, select });
  const invalidate = () => qc.invalidateQueries({ queryKey: [QueryKeys.X] });
  return { query, invalidate };
}
// toasts: notifications.show({ color, title, message }) from @mantine/notifications (autoClose 3000 global)

<!-- 5-state machine (UI-SPEC §Interaction Behavior + research Pattern 3) -->
type NotificationState = "default" | "requesting" | "enabled" | "denied" | "unsupported";
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Push API client (src/api/pushSubscriptions.ts) + tests</name>
  <files>frontend/src/api/pushSubscriptions.ts, frontend/src/api/pushSubscriptions.test.ts</files>
  <behavior>
    - fetchVapidPublicKey: GET .../vapid-public-key with credentials:'include'; returns parsed { key }; throws on !res.ok
    - fetchSubscriptionStatus(endpoint): GET .../push-subscriptions?endpoint=<url-encoded>; returns { subscribed }; throws on !res.ok
    - postSubscription(sub): POST .../push-subscriptions with JSON body { endpoint, keys:{ p256dh, auth } } built from sub.toJSON() shape; sets Content-Type + credentials; throws res on !res.ok
    - deleteSubscription(endpoint): DELETE .../push-subscriptions?endpoint=<url-encoded>; throws res on !res.ok
    - endpoint query param is URL-encoded exactly once (use URLSearchParams)
  </behavior>
  <action>
Create `src/api/pushSubscriptions.ts` — raw fetch only, no React (CLAUDE.md §3 forbids fetch in components/hooks/effects; HTTP lives here). Mirror `src/api/charges.ts` conventions (`apiUrl`, `credentials:'include'`). Implement the four functions exactly as research §Code Examples shows. Type the POST body input as `PushSubscriptionJSON` (lib.dom type) — read `sub.endpoint`, `sub.keys?.p256dh`, `sub.keys?.auth`. No `any`.

Write `src/api/pushSubscriptions.test.ts` mocking global `fetch` with `vi.fn()`: assert the request URL (including single url-encoding of `endpoint`), method, headers, and body shape for each function; assert thrown error on non-ok. (`.test.ts` is now discovered thanks to Plan 02's vitest include widening.)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/api/pushSubscriptions.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Four exported functions with correct URLs/methods/bodies; `endpoint` URL-encoded once; tests pass with mocked fetch; reads `subscribed` from status response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: usePushSubscription state machine + status query (with tests)</name>
  <files>frontend/src/hooks/usePushSubscriptionStatus.ts, frontend/src/hooks/usePushSubscription.ts, frontend/src/hooks/usePushSubscription.test.tsx</files>
  <behavior>
    State derivation (no prompt on mount):
    - !('serviceWorker' in navigator) || !('PushManager' in window) → 'unsupported'
    - Notification.permission === 'denied' → 'denied'
    - pending (in-flight) → 'requesting'
    - localEnabled ?? query.data?.subscribed truthy → 'enabled'
    - otherwise → 'default'
    onToggle:
    - state 'default': await Notification.requestPermission(); if !granted → return (re-renders to denied, NO toast); else set pending, ready→fetchVapidPublicKey→subscribe({userVisibleOnly:true, applicationServerKey:urlBase64ToUint8Array(key)})→postSubscription(sub.toJSON())→localEnabled=true + teal success toast; on error → red 'Erro ao ativar' toast, stay default
    - state 'enabled': set pending, ready→getSubscription(); if sub → deleteSubscription(sub.endpoint) then sub.unsubscribe(); localEnabled=false + teal toast; on error → localEnabled=true (stay enabled) + red 'Erro ao desativar' toast
    - state 'requesting'/'denied'/'unsupported': noop
    - requestPermission is NEVER called on mount/in an effect (CTRL-01)
  </behavior>
  <action>
Create two hooks (research Pattern 3, UI-SPEC §Interaction Behavior + §Initial state resolution; honor D-24-2/research reconciliation notes).

`src/hooks/usePushSubscriptionStatus.ts` — TanStack query (mirror `useChargesPendingCount`): resolve the current endpoint first via `(await navigator.serviceWorker.ready).pushManager.getSubscription()`, then call `fetchSubscriptionStatus(endpoint)`; if no subscription, resolve `{ subscribed: false }` without calling the API. Accept `{ enabled }` so the query only runs when `supported && !permissionDenied`. Use `queryKey: [QueryKeys.PushSubscription]`, `staleTime` ~60s, return `{ query, invalidate }`.

`src/hooks/usePushSubscription.ts` — the 5-state machine. Export `type NotificationState`. Derive `supported`/`permissionDenied` synchronously during render (NO effect). Hold `pending` and `localEnabled: boolean | null` via `useState`. Read `subscribed` (NOT `active`) from the status query. Implement `onToggle` exactly per the behavior block — `Notification.requestPermission()` and `pushManager.subscribe()` fire ONLY inside `onToggle` when transitioning from `default` (CTRL-01). No optimistic flip (show `requesting`, transition on result — UI-SPEC explicit). Toast copy verbatim from UI-SPEC §Surface 3 (teal "Notificações ativadas"/"Notificações desativadas"; red "Erro ao ativar"/"Erro ao desativar"; NO toast on permission-denied). Build the VAPID key via `urlBase64ToUint8Array` (Plan 02). Treat backend `subscribed` as toggle truth; re-subscribe is idempotent (backend upserts); backend prunes stale endpoints on send. Return `{ state, onToggle, helperText }` where `helperText` is a state→copy map (the component will pick short vs full — pass both or expose a `variant`; expose `helperText(variant: 'short' | 'full')` or return both maps so Plan 05 renders OD-3 copy).

`src/hooks/usePushSubscription.test.tsx` — render the hook with `@testing-library/react` + a `QueryClientProvider` wrapper, using the Plan 02 jsdom stubs. Cover: each of the 5 derived states (set `Notification.permission`, toggle `PushManager`/`serviceWorker` presence, mock the status query result); that `requestPermission` is NOT called on mount; that tapping from `default` with granted permission calls subscribe + postSubscription and ends `enabled`; that a denied permission ends `denied` with no toast; that an enabled→toggle calls deleteSubscription + unsubscribe and ends `default`; that a subscribe API error stays `default` with the error toast. Mock the api-client functions and `@mantine/notifications`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/usePushSubscription.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>`usePushSubscription` derives all 5 states without prompting on mount; `onToggle` implements both subscribe and unsubscribe flows with verbatim toasts; reads `subscribed`; no optimistic flip; tests cover the 5 states + the CTRL-01 no-prompt-on-mount invariant + subscribe/unsubscribe success+error paths; no `any`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser PushSubscription → POST /api/push-subscriptions | Browser-provided endpoint/keys cross to the backend |
| component → usePushSubscription | User gesture triggers permission + subscribe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Information Disclosure | subscribe/unsubscribe/status | accept | Backend scopes every operation by userID from the auth context (verified Phase 22 handler); the endpoint string is device-specific and the api client always sends `credentials:'include'`. No cross-user access path is introduced client-side. |
| T-24-08 | Tampering | `onToggle` permission gate | mitigate | `Notification.requestPermission()` is only ever invoked inside `onToggle` on a user gesture from the `default` state — never on mount or in an effect — satisfying CTRL-01 and preventing silent prompt abuse. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/api/pushSubscriptions.test.ts src/hooks/usePushSubscription.test.tsx` passes.
- `cd frontend && npm run lint` clean.
- `cd frontend && npx tsc -b --noEmit` no new errors.
- CTRL-01 invariant asserted by a test (`requestPermission` not called on mount).
</verification>

<success_criteria>
- SUB-01: subscribe path (requestPermission → vapid key → pushManager.subscribe → POST) implemented + tested.
- SUB-02: unsubscribe path (DELETE + unsubscribe) implemented + tested.
- CTRL-01: permission requested only inside onToggle from default (test-enforced).
- CTRL-02: 5-state derivation implemented + tested (visual rendering in Plan 05).
</success_criteria>

<output>
After completion, create `.planning/phases/24-frontend-permission-subscribe-service-worker/24-04-SUMMARY.md`. Document the local-vs-backend drift handling chosen and the `helperText` API shape Plan 05 should consume.
</output>
