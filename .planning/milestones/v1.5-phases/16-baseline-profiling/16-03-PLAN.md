---
phase: 16-baseline-profiling
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md
  - .planning/phases/16-baseline-profiling/16-DIAGNOSIS.md
autonomous: false
requirements:
  - PROF-03

must_haves:
  truths:
    - "A 100-row deterministic CSV fixture is generated and successfully imported in the production preview build"
    - "Commit duration (ms) and rendered component count are captured for all 4 scenarios on row 50 of the 100-row fixture"
    - "All 4 measurements are recorded in the Baseline Measurements section of 16-PERF-BASELINE.md"
    - "The Hypothesis Verdict section states CONFIRMED or CONTRADICTED with profiler evidence"
    - "If CONTRADICTED, 16-DIAGNOSIS.md is produced naming the actual culprit and recommending replan changes"
    - "The Profiling Runbook section is filled with reproducible step-by-step instructions"
    - "ROADMAP.md is NOT auto-edited (replan is a developer/orchestrator decision)"
  artifacts:
    - path: ".planning/phases/16-baseline-profiling/16-PERF-BASELINE.md"
      provides: "Full baseline doc — Runbook + 4-row measurement table + Hypothesis Verdict"
      contains: "## Hypothesis Verdict"
    - path: ".planning/phases/16-baseline-profiling/16-DIAGNOSIS.md"
      provides: "Conditional — only if hypothesis contradicted; names actual culprit + replan recommendation"
      contains: "Replan recommendation"
  key_links:
    - from: ".planning/phases/16-baseline-profiling/16-PERF-BASELINE.md"
      to: "frontend/scripts/genImportFixture.ts"
      via: "Runbook step references the generator command"
      pattern: "genImportFixture\\.ts"
    - from: ".planning/phases/16-baseline-profiling/16-PERF-BASELINE.md"
      to: "frontend/src/pages/ImportTransactionsPage.tsx"
      via: "Hypothesis Verdict cites the page-level useWatch line"
      pattern: "ImportTransactionsPage\\.tsx"
---

<objective>
Capture the 4-scenario React DevTools Profiler baseline against the 100-row fixture in production preview mode and validate (or contradict) the page-level `useWatch({ name: 'rows' })` hypothesis. Fill the Profiling Runbook, Baseline Measurements, and Hypothesis Verdict sections of `16-PERF-BASELINE.md`. If the hypothesis is contradicted, also produce `16-DIAGNOSIS.md`.

Purpose: PROF-03 is the gating deliverable for the entire v1.5 milestone. Phase 17 cannot proceed without (a) numeric baseline numbers to compare against and (b) a validated hypothesis pointing at the right code. This is also the only Phase 16 plan that requires a human at a browser — there is no automated profiler API for React DevTools.

Output: A complete `16-PERF-BASELINE.md`, optionally a `16-DIAGNOSIS.md`, and zero source code changes.
</objective>

<execution_context>
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-baseline-profiling/16-CONTEXT.md
@.planning/phases/16-baseline-profiling/16-PERF-BASELINE.md
@CLAUDE.md
@frontend/CLAUDE.md
@frontend/src/pages/ImportTransactionsPage.tsx
@frontend/src/components/transactions/import/ImportReviewRow.tsx
@frontend/scripts/genImportFixture.ts
@frontend/e2e/helpers/csv.ts

<interfaces>
<!-- Hypothesis target — extracted from CONTEXT.md and pre-existing scout facts -->

frontend/src/pages/ImportTransactionsPage.tsx (line 70):
```ts
const rows = useWatch({ name: 'rows' });
```
This is the broad subscription suspected of causing all 100 rows to re-render on every per-row keystroke.

frontend/src/pages/ImportTransactionsPage.tsx (line 90 area):
`handleToggleSelect` — handles shift-click row select (one of the 4 scenarios).

The 4 scenarios (locked in CONTEXT.md > Profiling scenarios), all on row 50 of the 100-row fixture:
1. Description keystroke — type one character into row 50's description input
2. Amount keystroke — type one digit into row 50's CurrencyInput
3. Checkbox toggle — single-click row 50's selection checkbox
4. Shift-click row select — shift-click on row 50 to multi-select

Per scenario, capture:
- Commit duration (ms) — top of the React DevTools Profiler "Ranked" view
- Rendered component count — count of components in the commit's flamegraph
- Whether `ImportTransactionsPage` itself appears in the rendered set
- Whether all 100 `ImportReviewRow` instances appear in the rendered set

Hypothesis verdict logic:
- CONFIRMED if at least one of {description keystroke, amount keystroke} causes BOTH `ImportTransactionsPage` to re-render AND all (or nearly all) 100 `ImportReviewRow` instances to re-render. The page-level `useWatch({ name: 'rows' })` is the dominant culprit.
- CONTRADICTED if the dominant re-render trigger is some other component (e.g. the CurrencyInput's controlled-input cycle is local to the row, but a shared context or unstable Select option array still re-renders neighbors). In that case, name the actual culprit and recommend replan.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Generate fixture, run preview, and verify import flow accepts the CSV end-to-end</name>
  <read_first>
    - .planning/phases/16-baseline-profiling/16-CONTEXT.md (decisions: "Profiling environment", "Fixture size")
    - frontend/scripts/genImportFixture.ts (created in 16-01)
    - frontend/e2e/helpers/csv.ts (created in 16-01)
    - .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md (created in 16-02 — confirm Compiler Verification section is filled)
  </read_first>
  <what-built>
    Fixture generator + CSV helper (16-01) and the baseline doc skeleton with compiler verdict (16-02) are in place. This task verifies the production preview build accepts the deterministic 100-row fixture before the developer starts measuring.
  </what-built>
  <how-to-verify>
1. From the repo root, generate the fixture:
   ```bash
   cd frontend && npx tsx scripts/genImportFixture.ts > /tmp/fixture-100.csv
   wc -l /tmp/fixture-100.csv   # expect 101
   head -1 /tmp/fixture-100.csv # expect: Data;Descrição;Valor;Categoria
   ```
2. Build and serve the production preview:
   ```bash
   cd frontend && npm run build && npm run preview
   ```
   The preview server starts on port 5173 (or `$PORT`).
3. In Chrome (with the React DevTools extension installed):
   - Open `http://localhost:5173/`
   - Log in (or use whatever fast-login path you normally use locally)
   - Navigate to the import flow and upload `/tmp/fixture-100.csv`
   - Confirm: the review screen renders 100 rows, no parse errors are reported, the importer's column mapping treats `Categoria` as recognized
4. Note the exact Chrome version (`chrome://version`) for the `**Browser:**` line in the baseline doc
5. Leave the preview server running — the next task uses the same session

**Resume signals:**
   - Type `approved` if all 100 rows render and the file imports cleanly
   - Type `parse error: <message>` if the importer rejects the CSV (means 16-01 is wrong; abort and revise)
   - Type `cannot login: <reason>` if local auth blocks you (this is an environmental issue outside Phase 16 scope)
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue (parse error, login blocker, etc.)</resume-signal>
  <acceptance_criteria>
    - `/tmp/fixture-100.csv` exists with exactly 101 lines and the 4-column header
    - `npm run preview` is running and serving the production build
    - The import flow renders 100 rows with no parse errors
    - Chrome version is noted for the baseline doc
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture 4-scenario profiler measurements on row 50 and write Runbook + Measurements + Hypothesis Verdict to 16-PERF-BASELINE.md</name>
  <read_first>
    - .planning/phases/16-baseline-profiling/16-CONTEXT.md (decisions: "Profiling scenarios", "Baseline artifact", "Hypothesis-contradicted contingency")
    - frontend/src/pages/ImportTransactionsPage.tsx (lines 60-100 — the `useWatch` at line 70 and `handleToggleSelect` near line 90)
    - frontend/src/components/transactions/import/ImportReviewRow.tsx (memoized row component — confirm whether it appears in profiler commit trees)
    - .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md (skeleton from 16-02 — sections to fill)
  </read_first>
  <what-built>
    Phase 16 is set up. This task captures the actual profiler numbers and writes the verdict.
  </what-built>
  <how-to-verify>

**A. Setup (one-time per session)**

1. With the preview server running and the 100-row fixture imported (from Task 1), open Chrome DevTools → React DevTools → Profiler tab
2. Settings cog → enable "Record why each component rendered while profiling"
3. Identify row 50: scroll the review table so row index 50 (0-indexed: the 51st row, "Mercado #051" or similar based on the fixture's deterministic ordering) is in the viewport. Click into row 50's description field once to put focus there before recording

**B. Capture each of the 4 scenarios**

For each scenario below, follow the exact recording pattern:
   1. Press the blue Record circle in the Profiler tab
   2. Perform the action ONCE (one keystroke, one click, one shift-click)
   3. Press Record again to stop
   4. The Profiler shows a list of commits. Read the FIRST commit triggered by the action (ignore any preceding setup commits)
   5. Note: commit duration in ms (top of "Ranked" view), rendered component count (count entries in the flamegraph), whether `ImportTransactionsPage` is in the rendered set, whether `ImportReviewRow` shows 1 instance vs ~100 instances

**Scenario 1 — Description keystroke (row 50)**: focus row 50's description input → press one letter (e.g. `x`)
**Scenario 2 — Amount keystroke (row 50)**: focus row 50's CurrencyInput → press one digit (e.g. `5`)
**Scenario 3 — Checkbox toggle (row 50)**: single-click row 50's selection checkbox
**Scenario 4 — Shift-click row select**: with row 0's checkbox already checked (click it first OUTSIDE the recording), start recording, then shift-click row 50's checkbox to multi-select rows 0..50

**C. Edit `16-PERF-BASELINE.md`**

Replace the "_Status: pending — 16-03._" placeholders in three sections.

**Profiling Runbook section** (replace placeholder with):
```markdown
### How to reproduce these measurements

1. Generate the fixture:
   `cd frontend && npx tsx scripts/genImportFixture.ts > /tmp/fixture-100.csv`
2. Build + serve production preview:
   `cd frontend && npm run build && npm run preview`
3. Open Chrome at `http://localhost:5173/`, log in, navigate to import, upload `/tmp/fixture-100.csv`
4. Verify 100 rows render with no parse errors
5. Open Chrome DevTools → React DevTools → Profiler tab. Enable "Record why each component rendered while profiling"
6. For each scenario, focus the relevant input on row 50 BEFORE recording, then:
   - Press Record → perform the action ONCE → press Record to stop
   - Read the first commit in the resulting list
   - Note commit duration (ms), rendered component count, whether `ImportTransactionsPage` re-rendered, whether all 100 `ImportReviewRow` instances re-rendered
7. The 4 scenarios are: description keystroke, amount keystroke, checkbox toggle, shift-click row select — all on row 50 of the 100-row fixture
```

**Baseline Measurements section** (replace placeholder with — fill the actual numbers):
```markdown
| # | Scenario | Commit duration (ms) | Rendered components | Page re-rendered? | All 100 rows re-rendered? | Notes |
|---|---|---|---|---|---|---|
| 1 | Description keystroke (row 50) | <ms> | <count> | <yes/no> | <yes/no/N rows> | <e.g. flamegraph dominated by ImportReviewRow ×100 + ImportTransactionsPage> |
| 2 | Amount keystroke (row 50)      | <ms> | <count> | <yes/no> | <yes/no/N rows> | <notes> |
| 3 | Checkbox toggle (row 50)       | <ms> | <count> | <yes/no> | <yes/no/N rows> | <notes> |
| 4 | Shift-click row select (row 50) | <ms> | <count> | <yes/no> | <yes/no/N rows> | <notes> |
```

**Hypothesis Verdict section** (replace placeholder with one of the two templates):

If CONFIRMED:
```markdown
**Verdict:** CONFIRMED — page-level `useWatch({ name: 'rows' })` is the dominant re-render trigger.

**Evidence:** In scenarios 1 and 2 (single keystroke into a row's description / amount input), `ImportTransactionsPage` re-rendered AND <N> `ImportReviewRow` instances re-rendered (out of 100). The Profiler's "why did this render" reason for `ImportTransactionsPage` cites the `useWatch` subscription at `frontend/src/pages/ImportTransactionsPage.tsx:70`. Phase 17's planned scope (replacing this with `compute`-scoped subscriptions) is therefore the correct intervention. No `16-DIAGNOSIS.md` produced.
```

If CONTRADICTED:
```markdown
**Verdict:** CONTRADICTED — the dominant re-render trigger is `<actual culprit>`, not the page-level `useWatch`.

**Evidence:** In scenarios <X, Y>, `ImportTransactionsPage` <did/did not> re-render, but `<actual culprit component / hook>` re-rendered <N> instances of `ImportReviewRow` because `<root-cause description, e.g. categoryOptions reference identity changed>`. Detailed analysis and replan recommendation in `16-DIAGNOSIS.md`.
```

**D. If CONTRADICTED — also create `16-DIAGNOSIS.md`** at `.planning/phases/16-baseline-profiling/16-DIAGNOSIS.md` with this structure:

```markdown
# Phase 16 — Diagnosis: Hypothesis Contradicted

**Date:** <YYYY-MM-DD>
**Triggered by:** PROF-03 contradicted the page-level useWatch hypothesis

## Actual culprit

<File and line citation. E.g. `frontend/src/components/transactions/import/ImportReviewRow.tsx:42 — categoryOptions is recreated on every render via inline categories.map(...)`>

## Profiler evidence

<Cite which scenario, which commit, what the "why did this render" reason was, and which components were in the rendered set>

## Replan recommendation

The current ROADMAP scope of phases 17–21 should be amended as follows:

- **Phase 17** (`Eliminate Page-Level useWatch Cascade`): <recommend keep/skip/reorder/refocus, with reasoning>
- **Phase 18** (`Move Select Options to Query select`): <recommend>
- **Phase 19** (`Scope & Debounce Duplicate Check`): <recommend>
- **Phase 20** (`Virtualize Import Review Table`): <recommend>
- **Phase 21** (`Verification & E2E Coverage`): <recommend>

ROADMAP.md is **not** auto-edited. The orchestrator (developer + next session) reviews this file
and decides whether to invoke `/gsd:phase` to amend phases 17–21.
```

If CONFIRMED, do NOT create `16-DIAGNOSIS.md`. The verify command checks both branches.

**E. Update the document header**

In `16-PERF-BASELINE.md`, replace `<YYYY-MM-DD when 16-03 runs the profiler>` with today's date and `<fill in version when 16-03 runs>` with the Chrome version recorded in Task 1.

**Resume signals:**
   - Type `confirmed` if hypothesis confirmed, baseline doc complete, no diagnosis needed
   - Type `contradicted: <one-line summary of actual culprit>` if hypothesis contradicted and `16-DIAGNOSIS.md` is written
   - Type `blocker: <description>` if a step cannot be completed (e.g. React DevTools not loading, profiler crashes)
  </how-to-verify>
  <resume-signal>Type "confirmed", "contradicted: ...", or "blocker: ..."</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/16-baseline-profiling/16-PERF-BASELINE.md` no longer contains any `_Status: pending — 16-03._` placeholders
    - `grep -c "^| [0-9] |" .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md` returns 4 (one row per scenario in the measurement table)
    - `grep -E "^\\*\\*Verdict:\\*\\* (CONFIRMED|CONTRADICTED)" .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md` finds exactly one match in the Hypothesis Verdict section
    - The document header `**Captured:**` and `**Browser:**` lines are filled (no `<...>` placeholders for these two)
    - If verdict is CONTRADICTED: `.planning/phases/16-baseline-profiling/16-DIAGNOSIS.md` exists with sections "## Actual culprit", "## Profiler evidence", "## Replan recommendation"
    - If verdict is CONFIRMED: `.planning/phases/16-baseline-profiling/16-DIAGNOSIS.md` does NOT exist
    - `.planning/ROADMAP.md` is unchanged (`git diff --stat .planning/ROADMAP.md` empty) — no auto-edit
    - No file under `frontend/src/` is modified
    - No file under `frontend/scripts/`, `frontend/e2e/`, or `frontend/vite.config.ts` is modified
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer Chrome session → preview server | Local-only HTTP traffic on 5173; no production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Spoofing | n/a | accept | Local-only profiling session |
| T-16-03-02 | Tampering | 16-PERF-BASELINE.md, 16-DIAGNOSIS.md | accept | Markdown files in `.planning/`, reviewed in PR like any other artifact |
| T-16-03-03 | Repudiation | n/a | accept | Single-author commit |
| T-16-03-04 | Information Disclosure | profiler captures | accept | Profiler exports (if any) contain only the synthetic 100-row fixture, no real user data |
| T-16-03-05 | Denial of Service | n/a | accept | Manual session, bounded |
| T-16-03-06 | Elevation of Privilege | n/a | accept | Local developer privileges only |
| T-16-03-07 | Injection | importer ← /tmp/fixture-100.csv | accept | Same as 16-01: synthetic, deterministic CSV consumed by the developer's own local dev server |

Minimal-surface assessment: this plan only edits markdown files and runs a local profiler session. No production code path, no auth boundary, no network surface beyond localhost. All threats `accept` with concrete justifications.
</threat_model>

<verification>
- `16-PERF-BASELINE.md` is fully populated (no `_Status: pending — 16-03._` placeholders remain)
- Measurement table has 4 numeric rows for the 4 locked scenarios
- Hypothesis Verdict is either CONFIRMED or CONTRADICTED with cited evidence
- If CONTRADICTED, `16-DIAGNOSIS.md` exists with culprit + replan recommendation
- ROADMAP.md is NOT modified
- No file under `frontend/` is modified by this plan
</verification>

<success_criteria>
- PROF-03 deliverable complete: numeric baseline + validated hypothesis (or documented contradiction)
- Phase 17 has either (a) green light to proceed against the planned scope or (b) a written diagnosis recommending replan
- All Phase 16 success criteria from ROADMAP.md are met across plans 01/02/03
</success_criteria>

<output>
After completion, create `.planning/phases/16-baseline-profiling/16-03-SUMMARY.md` documenting:
- The 4 measurement values (commit duration, component count) from the table
- The hypothesis verdict and one-line justification
- Whether `16-DIAGNOSIS.md` was created (yes/no)
- Confirmation that no `frontend/src/`, `frontend/scripts/`, `frontend/e2e/`, or `frontend/vite.config.ts` files were modified
- Confirmation that ROADMAP.md was not auto-edited
</output>
