---
phase: 16-baseline-profiling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md
autonomous: true
requirements:
  - PROF-02

must_haves:
  truths:
    - "The vite.config.ts plugin chain is empirically inspected for babel-plugin-react-compiler wiring and the finding is recorded"
    - "The production build artifact is grepped for compiler-emitted markers (e.g. cache-slot patterns) and the finding is recorded"
    - "The Compiler Verification section of 16-PERF-BASELINE.md states active OR inactive with concrete evidence (file:line citation + grep result)"
    - "vite.config.ts is NOT modified in this phase (wiring is deferred to Phase 17 per CONTEXT.md decisions)"
  artifacts:
    - path: ".planning/phases/16-baseline-profiling/16-PERF-BASELINE.md"
      provides: "Skeleton baseline doc + Compiler Verification section"
      contains: "## Compiler Verification"
      min_lines: 40
  key_links:
    - from: ".planning/phases/16-baseline-profiling/16-PERF-BASELINE.md"
      to: "frontend/vite.config.ts"
      via: "cited file:line in Compiler Verification section"
      pattern: "vite\\.config\\.ts"
    - from: ".planning/phases/16-baseline-profiling/16-PERF-BASELINE.md"
      to: "frontend/dist/assets/*.js"
      via: "grep result for compiler markers"
      pattern: "dist/assets"
---

<objective>
Empirically determine whether `babel-plugin-react-compiler` is active in the production build and document the finding (with evidence) in the Compiler Verification section of `16-PERF-BASELINE.md`. This plan ALSO scaffolds the rest of the baseline document (sections that 16-03 will fill in) so 16-03 only edits, never creates.

Purpose: PROF-02 is a binary fact that downstream phases (especially 17) need before they touch anything. The compiler being inactive is the single largest hidden variable in the v1.5 hypothesis — if it's inactive, phase 17 must wire it before measuring its `useWatch` change. Per locked CONTEXT.md decision, this phase only DOCUMENTS; wiring belongs to phase 17.

Output: `.planning/phases/16-baseline-profiling/16-PERF-BASELINE.md` exists with all required sections, and the Compiler Verification section contains a definitive verdict + evidence.
</objective>

<execution_context>
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Mateus.Campos/Documents/github/finance_app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-baseline-profiling/16-CONTEXT.md
@CLAUDE.md
@frontend/CLAUDE.md
@frontend/vite.config.ts
@frontend/package.json

<interfaces>
<!-- Pre-existing facts from scout (DO NOT re-discover) -->

frontend/vite.config.ts (lines 21-23):
```ts
plugins: [
  tanstackRouter({ routesDirectory: "./src/routes", generatedRouteTree: "./src/routeTree.gen.ts" }),
  react(),                          // <-- no babel.plugins option; compiler NOT wired
  VitePWA({ ... }),
]
```

frontend/package.json line 46:
```json
"babel-plugin-react-compiler": "^1.0.0"
```

The plugin is in dependencies but NOT wired. This plan empirically confirms that.

Compiler-emitted markers to grep for in `frontend/dist/assets/*.js` after `npm run build`:
- `useMemoCache` (the runtime helper React Compiler injects)
- `_c =` (cache-slot assignment pattern in transformed output)
- `react-compiler-runtime` (module specifier the compiler emits)

Absence of all three across the bundle is strong evidence the compiler is inactive.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run production build and grep dist for React Compiler markers</name>
  <read_first>
    - frontend/vite.config.ts (full file — confirm `react()` has no `babel.plugins` array)
    - frontend/package.json (confirm `babel-plugin-react-compiler` is listed)
    - .planning/phases/16-baseline-profiling/16-CONTEXT.md (decisions: "React Compiler wiring (contingency)")
    - .planning/REQUIREMENTS.md (PROF-02 wording)
  </read_first>
  <files>
    .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md
  </files>
  <action>
This task has TWO sub-steps. Both must complete before writing the document.

**Sub-step A: Inspect vite.config.ts plugin chain.**

Run:
```bash
grep -n "babel-plugin-react-compiler\|babel:" frontend/vite.config.ts
grep -n "react(" frontend/vite.config.ts
```

Record the output verbatim. Expected: zero matches for `babel-plugin-react-compiler`, one match for `react(` showing it has no arguments. If either expectation is violated, record the actual finding — do not edit `vite.config.ts`.

**Sub-step B: Build and grep the bundle.**

Run:
```bash
cd frontend && npm run build
```

This populates `frontend/dist/assets/*.js`. After it succeeds, grep the bundle for compiler markers:
```bash
grep -l "useMemoCache" frontend/dist/assets/*.js 2>/dev/null | head -5
grep -l "react-compiler-runtime" frontend/dist/assets/*.js 2>/dev/null | head -5
grep -c "_c\[[0-9]" frontend/dist/assets/*.js 2>/dev/null | head -5
```

Record exit codes / line counts / matched filenames verbatim. The `_c[<digit>]` pattern is the compiler's cache-slot indexing in minified output; >0 matches across the bundle is a positive signal. Zero matches across all three greps is a strong negative signal (compiler inactive).

**Sub-step C: Write `.planning/phases/16-baseline-profiling/16-PERF-BASELINE.md` from scratch with this exact skeleton.** Fill in the Compiler Verification section using the recorded findings. Leave the Runbook/Measurements/Hypothesis sections as defined placeholders (16-03-PLAN.md will fill them):

```markdown
# Phase 16 — Performance Baseline

**Phase:** 16 — Baseline Profiling & Diagnostics
**Milestone:** v1.5 Import Transactions Performance
**Captured:** <YYYY-MM-DD when 16-03 runs the profiler>
**Build:** `npm run build && npm run preview` (production preview)
**Browser:** Chrome <fill in version when 16-03 runs>
**Fixture:** `frontend/scripts/genImportFixture.ts` (deterministic, mulberry32 SEED=0x16ba5e1e, 100 rows)

## Compiler Verification

**Verdict:** <ACTIVE | INACTIVE — fill from sub-step findings>

**Evidence — vite.config.ts plugin chain:**

- File: `frontend/vite.config.ts`
- `grep -n "babel-plugin-react-compiler" frontend/vite.config.ts` → `<paste exact output or "no matches">`
- `grep -n "react(" frontend/vite.config.ts` → `<paste exact output>`
- The `react()` plugin call <does | does not> include a `babel: { plugins: [...] }` option. Citation: `frontend/vite.config.ts:<line>`.

**Evidence — production bundle inspection:**

- Build command: `cd frontend && npm run build`
- Build exit code: `<0 | non-zero>`
- `grep -l "useMemoCache" frontend/dist/assets/*.js` → `<paste output or "no matches">`
- `grep -l "react-compiler-runtime" frontend/dist/assets/*.js` → `<paste output or "no matches">`
- `grep -c "_c\[[0-9]" frontend/dist/assets/*.js` → `<paste output or "no matches">`

**Conclusion:** <one paragraph summarizing the verdict, e.g. "All three compiler markers are absent from the production bundle and `vite.config.ts` does not pass `babel-plugin-react-compiler` to the `react()` plugin. The compiler is therefore INACTIVE in the production build at the time of this measurement.">

**Per locked Phase 16 decision (`16-CONTEXT.md` → "React Compiler wiring (contingency)"), no wiring change is performed in this phase.** Phase 17 absorbs the wiring as a sub-task before any `useWatch` change so post-Phase-17 measurements are on the same compiler regime as post-Phase-21 measurements.

## Profiling Runbook

> Filled by 16-03-PLAN.md execution. This section will document the step-by-step procedure
> a developer follows to capture the 4-scenario baseline (Chrome → React DevTools → Profiler tab
> → press Record → perform scenario → stop → record commit duration + rendered count).

_Status: pending — 16-03._

## Baseline Measurements

> Filled by 16-03-PLAN.md execution. Markdown table — columns: scenario, commit duration (ms),
> rendered component count, notes.

_Status: pending — 16-03._

## Hypothesis Verdict

> Filled by 16-03-PLAN.md execution. Either CONFIRMED (page-level useWatch is the dominant
> re-render trigger) or CONTRADICTED (in which case `16-DIAGNOSIS.md` is also produced).

_Status: pending — 16-03._

## P19 → P20 Gate Threshold

Per the locked Phase 20 (Virtualization) gate decision in `16-CONTEXT.md`:

> If the 100-row fixture is fluid after Phase 19 (description-keystroke commit duration < ~16ms,
> no perceptible lag), Phase 20 is **skipped or downsized** at the orchestrator's call.

Phase 21 will compare its re-measured numbers against this baseline and apply the gate.
```

When writing the file, replace ALL `<...>` placeholders for the Compiler Verification section with the literal command output you recorded. Do NOT replace placeholders for sections marked "_Status: pending — 16-03._" — those are intentional handoffs.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md && grep -q "^## Compiler Verification" .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md && grep -q "^## Profiling Runbook" .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md && grep -q "^## Baseline Measurements" .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md && grep -q "^## Hypothesis Verdict" .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md && grep -qE "Verdict:.*(ACTIVE|INACTIVE)" .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md && test "$(grep -c "<.*>" .planning/phases/16-baseline-profiling/16-PERF-BASELINE.md | head -1)" -lt 12</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-baseline-profiling/16-PERF-BASELINE.md` exists
    - Section headers present: `## Compiler Verification`, `## Profiling Runbook`, `## Baseline Measurements`, `## Hypothesis Verdict`, `## P19 → P20 Gate Threshold`
    - The Compiler Verification section contains a `Verdict:` line with either `ACTIVE` or `INACTIVE` (no placeholder)
    - The Compiler Verification section cites `frontend/vite.config.ts:<line>` for the `react()` plugin call
    - The Compiler Verification section contains the verbatim output of the three `grep` commands run against `frontend/dist/assets/*.js` (or "no matches" when applicable)
    - `frontend/vite.config.ts` is NOT modified by this task (`git diff --stat frontend/vite.config.ts` empty)
    - No file under `frontend/src/` is modified by this task
    - The Runbook / Measurements / Hypothesis sections are present but explicitly marked `_Status: pending — 16-03._` (handoff to 16-03)
  </acceptance_criteria>
  <done>The baseline doc exists with the compiler verdict locked in, leaving runbook + measurements + hypothesis sections for 16-03 to fill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-local build → filesystem | `npm run build` writes to `frontend/dist/`; this is local-only and never published as an artifact of this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Spoofing | n/a | accept | No identities crossed |
| T-16-02-02 | Tampering | 16-PERF-BASELINE.md | accept | Markdown file under `.planning/`; tampering caught by code review like any source artifact |
| T-16-02-03 | Repudiation | n/a | accept | n/a — single-author markdown commit |
| T-16-02-04 | Information Disclosure | frontend/dist/ | accept | Local build artifact, gitignored, contains only the same code committed to git |
| T-16-02-05 | Denial of Service | n/a | accept | One-shot build + grep |
| T-16-02-06 | Elevation of Privilege | n/a | accept | Local developer privileges only |
| T-16-02-07 | Injection | n/a | accept | No user input flows into this plan; all greps run against deterministic files |

Minimal-surface assessment: this plan only adds a markdown document and runs read-only inspection (one build, three greps). No production code path, no auth boundary, no network surface. All threats appropriately `accept`.
</threat_model>

<verification>
- `16-PERF-BASELINE.md` exists with all 5 required section headers
- Compiler Verification section has a concrete `ACTIVE|INACTIVE` verdict with grep evidence pasted in
- `vite.config.ts` is NOT modified
- `frontend/src/` is NOT modified
</verification>

<success_criteria>
- PROF-02 deliverable: empirical compiler-presence check is documented with file:line citations and bundle-grep output
- The baseline doc is initialized with all sections; 16-03 only fills handoff sections
- Phase 17 has the binary fact it needs to decide whether to wire the compiler
</success_criteria>

<output>
After completion, create `.planning/phases/16-baseline-profiling/16-02-SUMMARY.md` documenting:
- Verdict (ACTIVE or INACTIVE)
- Verbatim grep outputs that drove the verdict
- Confirmation that `vite.config.ts` was not touched (git status / git diff stat output)
</output>
