# @tanstack/zod-adapter still declares a peer on zod@^3, while this app is on
# zod@4. Without this flag every `npm install`/`npm ci` fails with ERESOLVE, so
# CI already passes --legacy-peer-deps everywhere; keeping it here makes local
# installs resolve exactly like CI.
legacy-peer-deps=true
