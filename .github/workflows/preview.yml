name: Preview Frontend (PR)

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: "PR number to deploy as preview"
        type: string
        required: true

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: preview-pr-${{ inputs.pr_number }}
  cancel-in-progress: true

jobs:
  preview:
    name: Build & deploy preview
    runs-on: ubuntu-latest
    environment: prod

    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4
        with:
          ref: refs/pull/${{ inputs.pr_number }}/head

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci --legacy-peer-deps

      - name: Build frontend (against prod backend)
        working-directory: frontend
        env:
          VITE_API_URL: ${{ vars.VITE_API_URL }}
        run: npm run build

      - name: Resolve Cloudflare Pages project name
        id: cf-project
        run: |
          NAME=$(printf '%s' "${{ vars.CLOUDFLARE_PAGES_PROJECT_NAME_FRONTEND }}" | tr -d '\n\r')
          echo "name=${NAME}" >> "$GITHUB_OUTPUT"

      - name: Deploy preview to Cloudflare Pages
        id: deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy frontend/dist --project-name=${{ steps.cf-project.outputs.name }} --branch=pr-${{ inputs.pr_number }}

      - name: Comment preview URL on PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ inputs.pr_number }}
          URL: ${{ steps.deploy.outputs.deployment-url }}
        run: |
          gh pr comment "$PR_NUMBER" --body "Preview frontend (apontando para o backend de prod): $URL

          Branch: \`pr-$PR_NUMBER\` — sem expiração automática (diferente do canal do Firebase; delete manualmente no dashboard do Cloudflare Pages se necessário)."
